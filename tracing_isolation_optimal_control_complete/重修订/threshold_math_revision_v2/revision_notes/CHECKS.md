# 本轮实际检查记录

检查入口：`checks/check_revision.py`。机器报告：`checks/check_report.json`；完整标准输出：`checks/check_stdout.log`。

总体结果：**PASS**。以下各类检查的含义不同，不互相替代。

| 检查 | 实际结果 | 不能据此声称什么 |
|---|---|---|
| 源码与输入哈希 | 基线 Git blob 与本轮读取的仓库文件相同；修订文件与 manifest 相同 | 不代表远端已更新 |
| 关键公式/定义保护 | 14 个指定 LaTeX 环境逐字节不变 | 不等于整篇证明已形式化验证 |
| 标签与引用 | 164 个标签，全部旧标签保留，无重复、未定义引用或缺失文献键 | 不替代 XeLaTeX/BibTeX 编译 |
| 轻量语法 | 环境、分组花括号、显示数学定界符配对通过 | 不是完整 TeX 解析器 |
| 控制类 | 可测控制定义不变，未添加右连续相容性或逐点唯一性表述 | 不研究其他控制类的独立问题 |
| 补丁 | 应用检查、真实应用、逐字节比对和反向恢复通过 | 不保证将来有改动的工作树无冲突 |
| 新分区对应样本 | 8 组参数、440 个辅助条带样本通过 | 有限样本不证明全域命题 |
| 原公式回归 | 17 个符号恒等式、13 个检查组通过；640 个正物理状态、128 个切换点 | 不证明最优性/唯一性中的全称量词 |
| 完整工程编译 | **本轮未执行** | 没有新的编译通过结论或 PDF |

新增 440 个样本由以下部分组成：界面上方 128、下方 128、界面点 128、h<s≤e 的不安全点 32、容量线 s_B 左侧点 24。它们并不全是物理三角形内的点，部分用于核对主稿明确采用的辅助条带；与原回归的 640 个正物理状态分开报告。

新增样本中，切换标号逆映射的最大绝对误差约 `8.871e-14`，Φ−φ_Γ−χ 恒等式的最大浮点残差约 `3.886e-16`。
原回归的梯度有限差分最大归一化误差约 `2.521e-09`，抽样 min(H₀,H₁) 最大绝对残差约 `4.117e-16`。

## 手工数学核对

水平集引理采用解析证明而不是用样本验证绝对连续函数的一般性质。分区对应的三个符号方向、容量角点与合并端点左侧的适用域、横穿局部化、容量迹复合的绝对连续性，均按 `PROOF_CHECK.md` 中的推导逐项检查。

## 明确未执行

没有运行 XeLaTeX、BibTeX 或完整带图编译；没有生成新的理论校样或全文 PDF；没有运行 MATLAB、Octave、OpenOCL 或时变 SLSQP；没有重绘十张图、更新旧 CSV 或证明网格/正则化极限。

交付包保留原图的文件名和相对路径，但不含图文件。静态报告中的十个 `missing_local_figures` 是本包缺少完整工程资源的提示，不是把图改成了占位框；合并回原工程后应使用真实图片编译。

## 检查工具的一次修正

最初的显示公式配对检查用子串计数，误把原稿标题中的 `\\[2mm]` 换行命令计为 `\[` 数学开定界符。随后将检查器改为按 TeX 控制序列分词，重新运行全部检查后通过；没有为满足错误的检查器而改动标题的 LaTeX 命令。上述 PASS 是修正检查器后的实际结果。

## 数据与日志来源

原公式回归模块从此前修订包中原样复制，SHA-256 记录在 `MANIFEST.json`；本轮确实执行了它，没有将旧的 PASS 日志改名后充作新结果。本轮新增检查由 `check_revision.py` 实现。

补丁统计为一个文件、146 行插入、51 行删除。`git diff --check` 已通过。所有 Git 应用与回退试验均在一次性临时本地仓库完成，没有修改或推送用户的远端分支。
