# 实际检查记录

检查对象：本包 `latex/main.tex`、其同源理论入口，以及独立脚本。
日期：2026-09-18（UTC）。

## 结果分层

| 检查类别 | 本次结果 | 能说明什么，不能说明什么 |
|---|---|---|
| 解析证明内部复核 | 按 PROOF_AUDIT.md 逐项核对并在主稿展开 | 核对的是定义域、量词和推理链；不是形式化证明或外部同行审稿 |
| 符号恒等式 | 17/17 项 SymPy 化简为零 | 检查列出的代数公式；不自动检查其定义域或全局充分性 |
| HJB 与分区数值样本 | 640 个正物理状态，8 组参数 | 有限样本检查，不能穷尽所有参数或可行控制 |
| 切换几何数值样本 | 128 个非平凡切换点 | 支持参数化计算；不是横截性的解析证明 |
| 候选状态方程积分 | 4 条不安全候选轨道积分，加 1 个安全分支的零成本直接检查 | 核对候选成本和正终点，不检查所有竞争控制 |
| 源码引用 | 159 个标签，无重复标签、未定义引用或缺失文献键 | 只检查源码及编译交叉引用 |
| 常接触率理论构建 | XeLaTeX/BibTeX 成功，19 页 | 可直接阅读的真实编译校样 |
| 全文语法诊断构建 | 成功，26 页；仅临时诊断副本用图形框替代原图 | 检查全文文本、公式及引用；不是完整图文排版通过 |
| 原图依赖的完整构建 | 未通过：工作区缺少原 10 张 JPG，首个缺失为 Figure_1_phase_geometry.jpg | 没有宣称完整带原图论文已编译 |
| 版面查看 | 理论校样 19 页均已渲染并通过 5 张联系表逐页查看 | 未发现裁切、乱码或公式重叠；不涉及原图形布局 |

最终理论日志和全文语法诊断日志均无未定义引用、未定义引文、Overfull box 或 Missing character 警告。
不把多轮构建中正常出现、随后消失的首次未定义引用当成最终问题。
初次构建另遇到本环境 `bibtex` 链接失效，使用实际存在的 `bibtex.original` 后重新强制构建成功。

## 数值检查详情

8 组参数以 `(p,h,K)` 表示，均使用 c=2、gamma=pc*h：

```text
(0.50, 0.30, 0.15)
(0.25, 0.20, 0.08)
(0.75, 0.20, 0.30)
(0.80, 0.65, 0.03)
(0.20, 0.08, 0.40)
(0.90, 0.15, 0.01)
(0.50, 1.20, 0.15)
(0.40, 0.80, 0.35)
```

随机种子固定为 `20260918`。640 个主要样本中：安全状态 393 个，
等待到容量 143 个，等待到内部切换 83 个，立即完全跟踪 21 个。
安全样本的 U 和梯度为零；不安全样本共 247 个。
脚本另做容量与物理斜边的有限检查，未将其重复计入 640 个主要样本。

- 解析梯度与中心差分的最大缩放误差：`2.52101591587e-09`；
  误差按 `abs(numerical-analytic)/(1+abs(analytic))` 计算。
- 样本中 `abs(min(H0,H1))` 的最大值：`4.11661300328e-16`。
- 候选 ODE 积分成本最大绝对误差：`2.34954278255e-11`。
- 候选 ODE 网格记录的最大容量超出：`2.03759231709e-12`，属于积分容差。
- 候选终点的最大安全边界残差：`2.05990779989e-12`。

所有上述容差和判定条件写在脚本中，13 个检查组全部通过。

## 专门针对旧错误的回归检查

1. `(s,i)=(0.6,a(0.6)+1e-4)` 被归入内部切换等待区。错误容量公式给出负 Sigma，而真实候选等待公式满足正性。
2. `(0.5,0.14)` 被归入立即完全跟踪区。自然轨道的切换交点在 `s≈0.5120932578>0.5`，属于过去方向。
3. 安全边界 `(0.6,a(0.6))` 的不安全侧导数趋向等待分支给出的
   `13.6240872656`，而不是错误的 W 导数
   `19.1755514782`。

## 本次基准值

```json
{
  "sK": 0.7073030021837694,
  "e": 0.46908414634252626,
  "s1": 0.7775221610386437,
  "sB": 0.5476951246795451,
  "sR": 0.4516637862455485,
  "iR": 0.12108289155035945,
  "tau1": 4.349191557719972,
  "TB": 1.5203109780974815,
  "T1": 0.7138664296892887,
  "Jstar": 1.5295111601514724,
  "Jfilling": 1.5783855039561376,
  "Jdirect": 1.6364797199042445
}
```

## 未执行、未声称执行的检查

没有重新运行仓库 `verify_all.py`、`verify_supplement.py`、MATLAB/OpenOCL 或时变 SLSQP 求解。
没有重绘十张图、重新生成参数扫描 CSV，或检查原图像的像素内容与排版。
没有以离散数值优化排除所有其他局部极小；没有证明网格收敛、epsilon 极限或所有时变轨道的终端条件。
没有执行形式化定理证明系统。

交付源码正常使用原图路径，用户放回含 `figures/` 的工程后应再运行完整主稿构建。
临时图形框仅存在于本次内部语法诊断副本，不在交付 main.tex 中，也没有交付该占位图 PDF。

## 日志与版本

`checks/revision_results.json`：完整数值和静态检查结果。
`checks/verification_run.log`：本次脚本运行输出。
`checks/theory_build.log` 与 `theory_final_xelatex.log`：理论入口的构建与最终日志。
`checks/full_original_attempt.log`：原图缺失导致的完整构建失败记录。
`checks/full_syntax_build.log` 与 `full_syntax_final_xelatex.log`：临时副本的全文语法诊断。
`checks/build_status.json`：机器可读的构建结果分层。

运行环境：Python 3.13.5、NumPy 2.3.5、SciPy 1.17.0、SymPy 1.14.0。
