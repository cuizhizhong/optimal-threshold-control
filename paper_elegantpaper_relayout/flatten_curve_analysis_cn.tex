%!TEX program = xelatex
\documentclass[lang=cn,a4paper,newtx]{elegantpaper}

\title{ICU约束下流行病阈值控制策略分析}
\author{ XXX \and XXX}
\institute{School of Mathematics and Statistics, Shaanxi Normal University}
\version{0.1}
\date{\today}

\usepackage{amsmath,amssymb,mathrsfs}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage{placeins}
\usepackage{array}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{threeparttable}
\usepackage{xurl}
\usepackage{algorithm}
\usepackage{algpseudocode}

\graphicspath{{../}{../figures/}{../table/}}
\numberwithin{equation}{section}
% 类默认将"推论"定义为 \newtheorem*（无编号），导致 \ref{cor:*} 落到杂散计数器且两条推论撞号。
% 这里另建带编号的推论环境，与 定理/引理/命题 共用 theorem 计数器（依次 8.4、8.5）。
\theoremstyle{plain}
\newtheorem{corollaryn}[theorem]{推论}
% 同理：类默认 \newtheorem*{remark} 亦无编号，需按号引用的注记改用 remarkn。
\newtheorem{remarkn}[theorem]{注记}
\allowdisplaybreaks[3]
\renewcommand{\arraystretch}{1.12}
\emergencystretch=2em
\newcommand{\qinf}{q_{\mathrm{inf}}}
\newcommand{\Itcum}{I_{t_{\mathrm{cum}}}}
\addbibresource{references.bib}

\begin{document}

\maketitle

\begin{abstract}
\textit{（摘要待撰写。）}
\end{abstract}

\section{引言}
\textit{（引言与文献综述待撰写。）}

\section{模型与医疗容量约束}
\label{sec:model}
\subsection{包含接触追踪与隔离的扩展 SIR 型模型}
我们将人群划分为易感者 ($S$)、隔离中的易感者 ($S_q$)、非隔离感染者 ($I$)、隔离中的感染者 ($I_q$)、康复者 ($R$) 这几个仓室，我们记传播概率为 $\beta$，接触率为 $c(t)$。同时，我们将接触者追踪比例记为 $q(t)$，表示被追踪到的人群将根据其是否感染而被隔离（若易感则进入 $S_q$，若感染则进入 $I_q$）。同时，未被追踪到的人群（比例为 $1-q(t)$）是指暴露于病毒但被接触追踪遗漏的个体；如果他们被感染，将进入 $I$ 仓室。我们统一考虑$I$与$I_q$的康复者都进入同一个$R$仓室。两者的恢复率分别为 $\gamma$ 和 $\delta_q$。因此，系统的传播动态由以下方程组控制：       

该系统可视为带接触追踪与隔离的扩展 SIR 型模型；
为简便，下文亦称其为 SIQR 型模型。


\begin{equation}
\begin{cases}
\dfrac{dS(t)}{dt} = -\dfrac{\beta c(t) + c(t)q(t)(1-\beta)}{N} S(t)I(t), \\[.8em] % Add some space after the first line
\dfrac{dI(t)}{dt} = \dfrac{\beta c(t)(1-q(t))}{N} S(t)I(t)  - \gamma I(t), \\[.8em]
\dfrac{dS_q(t)}{dt} = \dfrac{(1-\beta)c(t)q(t)}{N} S(t)I(t), \\[.8em]
\dfrac{dI_q(t)}{dt} = \dfrac{\beta c(t) q(t)}{N} S(t)I(t) - \delta_q I_q(t), \\[.8em]
\dfrac{dR(t)}{dt} = \gamma I(t) + \delta_q I_q(t).
\end{cases}
\label{eq:model:full}
\end{equation}

相应的仓室转移结构见图~\ref{fig:model_schematic}。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/Fig1.pdf}
    \caption{含接触追踪与隔离的仓室转移结构：
    $S$ 为社区易感者，$S_q$ 为隔离易感者，
    $I$ 为社区感染者，$I_q$ 为隔离感染者，
    $R$ 为移除者；箭头标注相应转移通道的主要系数。}
    \label{fig:model_schematic}
\end{figure}
\subsection{模型控制}
在传染病流行期间，如何设计控制策略以防止非隔离感染者($I$)数量超过重症监护室($ICU$)的数量，是一个重要的公共卫生问题。为解决这一关键问题，我们将控制策略引入到接触率 $c(t)$ 和隔离率 $q(t)$ 中。

我们假设控制分三个阶段：
\begin{itemize}
    \item 常规防控阶段 ($t<t_1$);
    \item 加强防控阶段 ($t_1 \leq t \leq t_2$);
    \item 常规防控阶段 ($t > t_2$).
\end{itemize}

相应地，我们令 $c_0$ 和 $q_0$ 分别表示常规防控阶段下的接触率和隔离率，其中 $c_0>0$、$0\le q_0<1$。为了避免符号混淆，记加强防控阶段采用的控制轨迹为 $c_c(t)$ 和 $q_c(t)$。一旦在 $t=t_1$ 时启动加强控制，系统由常规水平 $(c_0,q_0)$ 切换到加强轨迹 $(c_c(t),q_c(t))$；当 $t=t_2$ 时解除加强控制，系统恢复到常规防控水平 $(c_0,q_0)$。因此，一个作用于系统的控制方案可以表述如下：
\begin{equation}
c(t) = 
\begin{cases} 
c_0, & t < t_1, \\
c_c(t), & t_1 \leq t \leq t_2, \\
c_0, & t > t_2, 
\end{cases}
\quad \text{and} \quad 
q(t) = 
\begin{cases} 
q_0, & t < t_1, \\
q_c(t), & t_1 \leq t \leq t_2, \\
q_0, & t > t_2. 
\end{cases}
\label{eq:model:piecewise-control}
\end{equation}

传统的控制策略通常通过持续性限制措施降低各时段的瞬时感染人数，从而压低感染峰值。然而，持续性强限制不仅难以长期维持，也会对正常社会生活和经济活动造成影响。因此，本文关注一种“精确压平曲线”理想控制策略：在不预设最优控制成本泛函的前提下，将 $I(t)\le \eta$ 作为医疗容量硬约束，并在系统首次触及红线后施加刚好使 $\dot I=0$ 的加强控制。


\textbf{策略定义}：设定ICU的医疗资源上限为 $I(t) \le \eta$。社会规划者的策略阶段定义为：
\begin{enumerate}
    \item \textbf{前期常规控制 ($0 \le t < t_1$)}：系统在背景防控水平 $(c_0,q_0)$ 下演化，直至$I$首次触碰容量上限 $\eta$。
    \item \textbf{中期加强控制 ($t_1 \le t \le t_2$)}：启动并动态调节加强控制 $(c_c(t),q_c(t))$，施加刚好的控制力使得$I$严格维持在天花板（$\dot{I}=0, I=\eta$），直至$S$降至背景防控水平下的临界阈值。
    \item \textbf{后期恢复常规控制 ($t > t_2$)}：系统跨越背景临界点，疫情进入自然衰退期，解除加强控制并恢复到 $(c_0,q_0)$。
\end{enumerate}

针对我们的控制策略，对于给定初值$(S_0, I_0)$，我们设在$t \in [0, t_1)$期间系统以常规防控水平演化，即$c(t) = c_0$,$q(t) = q_0$。在$t = t_1$时刻，系统首次触碰医疗上限，即 $I(t_1) = \eta$。令此时对应的易感人数（干预启动阈值）为 $S^* = S(t_1)$。

在控制期间 $t \in [t_1, t_2]$ 内，为精准维持 $I(t) \equiv \eta$，系统的演化必须满足条件 $\dot{I} = 0$，此时的控制为 $c_c(t)$ 和 $q_c(t)$。

设在$t = t_2$时刻，解除加强控制，此时易感人数下降到常规防控水平下的临界阈值：
\begin{equation}
S(t_2)=S_c\doteq \frac{ \gamma N}{\beta c_0(1-q_0)}.
\end{equation}
当$t > t_2$系统跨越该临界点，进入常态防控下的衰退期。此时的控制为$c(t)=c_0$,$q(t)=q_0$。


因此，我们的分析将围绕以下核心问题展开：
\begin{itemize}
    \item 解析求解干预启动点 $S^*$ 与时间 $t_1$；
    \item 推导阈值控制期的动态控制律 $c_c(t)$和$q_c(t)$ ；
    \item 推导阈值控制期的时间演化 $S(t)$ 和 控制结束时间 $t_2$；
    \item 分析这样的控制策略的效果，即分析时间演化 $S(t)$ 和 $I(t)$。以及参数对控制策略的影响。                                                   
\end{itemize}


\section{常规阶段首次积分与触发条件}
由于式~\eqref{eq:model:full} 中 $S$ 与 $I$ 的演化方程
不显含 $S_q$、$I_q$ 和 $R$，故 $(S,I)$ 构成一个封闭的二维子系统。
对于干预触发点及阈值控制轨迹的分析，只需考虑以下二维系统
\begin{equation}
\begin{cases}
\dot{S}(t) = - \beta_1(t) S(t) I(t) \\
\dot{I}(t) =   \beta_2(t) S(t) I(t) - \gamma I(t)
\end{cases}
\label{eq:model:reduced-si}
\end{equation}
其中
\begin{equation}
\beta_1(t)=\frac{c(t)\big[\beta+q(t)(1-\beta)\big]}{N},
\qquad
\beta_2(t)=\frac{\beta c(t)(1-q(t))}{N}.
\label{eq:model:beta12-general}
\end{equation}
这里 $\gamma$ 表示非隔离感染者 $I$ 的移出率。按照式 \eqref{eq:model:piecewise-control}，三阶段控制可等价写成
\begin{equation}
\beta_i(t)=
\begin{cases}
\beta_i^0, & t<t_1,\\
\beta_i^1(t), & t_1\le t\le t_2,\\
\beta_i^0, & t>t_2,
\end{cases}
\qquad i=1,2,
\end{equation}
其中常规防控阶段的系数为
\begin{equation}
\beta_1^0=\frac{c_0\big[\beta+q_0(1-\beta)\big]}{N},
\qquad
\beta_2^0=\frac{\beta c_0(1-q_0)}{N}.
\label{eq:model:beta12-baseline}
\end{equation}
加强防控阶段的 $\beta_1^1(t)$ 与 $\beta_2^1(t)$ 则由具体情景中的控制轨迹 $c_c(t)$、$q_c(t)$ 决定。

当 $\beta_1$ 与 $\beta_2$ 为常数时，特别是在前期和后期的常规防控阶段，可以通过消去时间变量得到首次积分。为统一记号，定义
\begin{equation}
\rho_1=\frac{\gamma}{\beta_1^0},
\qquad
S_c=\frac{\gamma}{\beta_2^0}
=\frac{\gamma N}{\beta c_0(1-q_0)}.
\label{eq:model:baseline-symbols}
\end{equation}
其中 $S_c$ 是常规防控水平 $(c_0,q_0)$ 下的临界易感人数。给定初始状态 $(S_0,I_0)$，常规防控阶段的首次积分为
\begin{equation}
I(t)+\frac{\beta_2^0}{\beta_1^0} S(t)-\rho_1\ln S(t)
=
I_0+\frac{\beta_2^0}{\beta_1^0} S_0-\rho_1\ln S_0
\doteq C_0.
\label{eq:model:first-integral}
\end{equation}
在 $S_0>S_c$ 时，常规防控轨道上的感染人数可写为
\begin{equation*}
I(S)=I_0+\frac{\beta_2^0}{\beta_1^0}(S_0-S)
+\rho_1\ln\frac{S}{S_0}.
\end{equation*}
由于常规控制感染峰值发生在 $\dot I=0$，即 $S=S_c$ 处，故定义常规控制感染峰值:
\begin{equation}
I_{\max}^{no}
=
I_0+\frac{\beta_2^0}{\beta_1^0}(S_0-S_c)
+\rho_1\ln\frac{S_c}{S_0}=C_0-\rho_1+\rho_1\ln S_c.
\label{eq:s1:Imax-no}
\end{equation}

为了确定干预策略的关键时间节点与对应的人口状态，需利用首次积分来求解临界易感人数$S^*$及其对应的首次到达时间$t_1$，以及通过$[t_1,t_2]$这段时间$I(t) = \eta(\dot{I}=0)$去计算$S(t),t \in [t_1,t_2]$和结束时间$t_2$以及控制函数$q_c(t)$,$c_c(t)$。

\section{情景一：精确压平曲线的闭式解（$c(t)\equiv c_0$，只加强 $q$ 控制）}
\label{sec:s1}
本情景考虑在常规防控水平 $(c_0,q_0)$ 的基础上，不进一步调节接触率 $c(t)$，而只通过提高隔离率 $q(t)$ 来维持医疗红线。也就是说，整个过程中
\begin{equation*}
c(t)=c_0,\qquad t\ge 0,
\end{equation*}
而隔离率在前期和后期保持常规水平 $q_0$，只在加强控制期内切换为动态加强隔离率 $q_c(t)$。

此时动力系统为：
\begin{equation}
\begin{cases}
\dot{S}(t) = - \beta_1(t) S(t) I(t) \\
\dot{I}(t) = \beta_2(t) S(t) I(t) - \gamma I(t)
\end{cases}
\label{eq:s1:dynamics}
\end{equation}
其中
\begin{equation}
\beta_i(t)=
\begin{cases}
\beta_i^0, & t<t_1,\\
\beta_i^1(t), & t_1\le t\le t_2,\\
\beta_i^0, & t>t_2,
\end{cases}
\quad i=1,2,
\end{equation}
其中$\beta_1^0$和$\beta_2^0$由式 \eqref{eq:model:beta12-baseline} 给出
\begin{equation}
\beta_1^1(t)=\frac{c_0\big[\beta+q_c(t)(1-\beta)\big]}{N},
\quad
\beta_2^1(t)=\frac{\beta c_0(1-q_c(t))}{N}.
\label{eq:s1:beta12-control}
\end{equation}
在后续推导中记
\begin{equation}
\bar S
=
\frac{\gamma N(1-\beta)}{\beta c_0}.
\label{eq:s1:barS}
\end{equation}
由于
\[
\frac{\bar S}{S_c}
=
(1-\beta)(1-q_0)<1,
\]
故 $\bar S<S_c$。

\subsection{干预启动点 $S^*$ 与时间 $t_1$ }

\begin{theorem}[干预启动点与首次触碰时间]
\label{thm:s1:trigger}
当\(S_0>S_c,I_0<\eta<I_{\max}^{no}\),定义:
\begin{equation}
z_\eta:=
-\frac{1}{S_c}
\exp\left[-\frac{C_0-\eta}{\rho_1}\right] \quad z_\eta\in\left(-\frac{1}{e},0\right)
\label{eq:s1:lambert-argument}
\end{equation}
则常规防控轨道在感染上升阶段首次触碰医疗阈值时的易感人数唯一:
\begin{equation}
S^*=-S_cW_{-1}(z_\eta),
\qquad
S_c<S^*<S_0.
\label{eq:s1:Sstar-lambert}
\end{equation}
开始控制的时间 $t_1>0$，且
\begin{equation}
t_1
=
\int_{S_0}^{S^*}
\frac{1}{
-\beta_1^0S\left[
C_0-\frac{\beta_2^0}{\beta_1^0}S+\rho_1\ln S
\right]
}\,dS.
\label{eq:s1:t1}
\end{equation}
\end{theorem}
\begin{proof}
在加强控制启动前，系统处于常规防控阶段,有首次积分\eqref{eq:model:first-integral},已知 $I(t_1)=\eta$，$S^*=S(t_1)$。将该边界条件代入式\eqref{eq:model:first-integral}得
\[
\frac{\beta_2^0}{\beta_1^0}S^*-\rho_1\ln S^*
=C_0-\eta.
\]
这构成了关于 $S^*$ 的非线性超越方程。为了利用 Lambert $W$ 函数求解，注意到
\[
\frac{\beta_2^0}{\beta_1^0\rho_1}
=
\frac{\beta_2^0}{\gamma}
=
\frac{1}{S_c}.
\]
于是上式可化为
\[
-\frac{1}{S_c}S^*
\exp\left(-\frac{1}{S_c}S^*\right)
=
-\frac{1}{S_c}
\exp\left[-\frac{C_0-\eta}{\rho_1}\right].
\]
由式~\eqref{eq:s1:lambert-argument}，上述方程可写为
\[
-\frac{S^*}{S_c}
\exp\left(-\frac{S^*}{S_c}\right)
=z_\eta.
\]
由于$I_{\max}^{no}=C_0-\rho_1+\rho_1\ln S_c$,则$z_{\eta}=-\frac{1}{e}\exp\left[-\frac{I_{\max}^{no}-\eta}{\rho_1}\right]$ ,从而由$\eta< I_{\max}^{no}$,可得$z_\eta\in(-1/e,0)$，Lambert $W$ 函数有两个实值分支，并且
\[
W_0(z_\eta)\in(-1,0),
\qquad
W_{-1}(z_\eta)\in(-\infty,-1).
\]
因此
\[
S^*=-S_cW_0(z_\eta)\in(0,S_c),
\]
或者:
\[
S^*=-S_cW_{-1}(z_\eta)>S_c.
\]
前者对应同一常规防控轨道在感染峰值后的下降阶段交点，后者对应感染上升阶段交点。
另一方面，在 $S\in(S_c,S_0)$ 上有
$\dot I=I(\beta_2^0S-\gamma)>0$。
结合 $I_0<\eta<I_{\max}^{no}$，常规防控轨道在到达峰值前恰有一次满足
$I(t)=\eta$，且该时刻满足 $S_c<S(t)<S_0$。
因此首次触碰根唯一，必须选择 $W_{-1}$ 分支，从而得到
式~\eqref{eq:s1:Sstar-lambert}。

在确定状态阈值 $S^*$ 后，启动加强控制的具体时间 $t_1$ 可由常规防控阶段的易感者的动态方程积分得到。由式\eqref{eq:model:first-integral} 有
\[
I(S)=
C_0-\frac{\beta_2^0}{\beta_1^0}S+\rho_1\ln S,
\]
因此
\[
t_1
=
\int_{S_0}^{S^*}
\frac{1}{
-\beta_1^0S\left[
C_0-\frac{\beta_2^0}{\beta_1^0}S+\rho_1\ln S
\right]
}\,dS.
\]
该定积分可通过数值求积求解。
\end{proof}

\begin{remark}
定理~\ref{thm:s1:trigger} 描述的是非退化的首次触碰情形。其边界与不可行情形如下：
\begin{itemize}
\item 若 $\eta=I_0$，则系统在初始时刻触碰阈值，$t_1=0$ 且 $S^*=S_0$；
\item 若 $\eta=I_{\max}^{no}$，则
$z_\eta=-1/e$、$W_{-1}(z_\eta)=-1$ 且 $S^*=S_c$，
触碰发生在常规防控轨道的峰值处，阈值控制区间退化；
\item 若 $\eta>I_{\max}^{no}$，则 $z_\eta<-1/e$，
Lambert 方程不存在实根，常规防控轨道不会触碰医疗阈值；
\item 若 $\eta<I_0$，则初始状态已经超过医疗阈值，
不属于本文讨论的“首次触碰后启动控制”情形。
\end{itemize}
\end{remark}

\subsection{阈值控制期的状态反馈控制 $q_c(S(t))$}
\begin{theorem}
\label{thm:s1:qc-structure}
对系统\eqref{eq:s1:dynamics}，如果定理~\ref{thm:s1:trigger} 的非退化触发条件(\(S_0>S_c,I_0<\eta<I_{\max}^{no}\))成立。在阈值控制期 $t\in[t_1,t_2]$ 内精准维持 $I(t)\equiv\eta$（即 $\dot I=0$）所需的隔离强度由如下状态反馈律唯一确定：
\begin{equation}
q_c(t)=q_c(S(t))=1-\frac{\gamma N}{\beta c_0 S(t)} \quad S\in [S_c,S^*].
\label{eq:s1:q-control}
\end{equation}
从而完整的隔离控制律为
\begin{equation}
q(t)=
\begin{cases}
q_0, & t<t_1,\\
q_c(t)=1-\dfrac{\gamma N}{\beta c_0 S(t)}, & t_1\le t\le t_2,\\
q_0, & t>t_2.
\end{cases}
\label{eq:s1:q-piecewise}
\end{equation}
控制函数具有以下结构：
\begin{itemize}
\item[(i)] \textbf{瞬时启动}：$q(t)$ 在 $t=t_1$ 处发生向上跳跃间断，
\[
q(t_1^-)=q_0<q(t_1)=q_{\max}=1-\frac{\gamma N}{\beta c_0 S^*},
\qquad
\Delta q:=q_{\max}-q_0=\frac{\gamma N}{\beta c_0}\left(\frac{1}{S_c}-\frac{1}{S^*}\right)>0.
\]
\item[(ii)] \textbf{单调松弛}：$q_c(t)$ 在 $(t_1,t_2)$ 上严格单调递减。
\item[(iii)] \textbf{连续衔接}：$q(t)$ 在 $t=t_2$ 处连续，$q_c(t_2)=q_0=q(t_2^+)$，不存在跳跃。
\end{itemize}
\end{theorem}
\begin{proof}
在控制期 $t\in[t_1,t_2]$ 内，为精准维持 $I(t)\equiv\eta$，系统的演化必须满足条件 $\dot I=0$。代入方程\eqref{eq:s1:dynamics}第二个方程：
\[
\frac{\beta c_0(1-q)S}{N}\eta-\gamma\eta=0,
\]
解得\eqref{eq:s1:q-control}。将其看作 $S$ 的函数 $q_c(S)=1-\gamma N/(\beta c_0 S)$，则
\[
\frac{dq_c}{dS}=\frac{\gamma N}{\beta c_0 S^2}>0,
\]
即 $q_c$ 关于 $S$ 严格递增。

\emph{(i)} 代入 $S=S_c=\gamma N/[\beta c_0(1-q_0)]$ 直接验证 $q_c(S_c)=q_0$。由假设 $S^*>S_c$ 及上述严格单调性，得
\[
q_{\max}=q_c(S^*)>q_c(S_c)=q_0,
\]
$\Delta q$ 的表达式由定义代入即得。

\emph{(ii)} 将\eqref{eq:s1:q-control}代入 $\dot S$ 的动态方程，得
\[
\dot S=-\frac{c_0\eta}{N}(S-\bar S),\qquad \bar S=\frac{(1-\beta)\gamma N}{\beta c_0}.
\]
从而:
\[
q'_c(t)=\frac{\gamma N}{\beta c_0} \frac{\dot{S}(t)}{S^{2}(t)}=- \frac{\frac{\gamma \eta}{\beta}(S-\bar S)}{S^{2}(t)}<0.\quad S\in [S_c,S^*]
\]
由于 $S^*>S_c>\bar S$（后者由 $S_c-\bar S=\frac{\gamma N}{\beta c_0}\left[\frac{q_0}{1-q_0}+\beta\right]>0$ 验证）知 $S-\bar S>0$，即 $q_c(t)$ 关于 $t$ 严格单调递减，且 $q_c(t_1)=q_c(S^*)=q_{\max}$，$q_c(t_2)=q_c(S_c)=q_0$。

\emph{(iii)} 由 (ii) 中 $q_c(t_2)=q_c(S_c)=q_0$，与常规阶段的 $q(t_2^+)=q_0$ 相符，故 $q(t)$ 在 $t=t_2$ 处连续。
\end{proof}
\begin{remark}
该定理表明，情景一构造的隔离控制律呈现"阈值触发时刻立即施加最大隔离强度、随后随传播压力减弱而单调放松、直至与常规防控无缝衔接"的结构。
\end{remark}



\begin{theorem}[阈值隔离控制的拐点]\label{thm:s1:inflection}
    设 $S_0>S_c$、$I_0<\eta<I_{\max}^{no}$（由定理~\ref{thm:s1:trigger}，$z_\eta\in(-1/e,0)$，故下式中的实值分支 $W_{-1}(z_\eta)$ 良定义）。则阈值控制期内 $q_c(t)$ 存在唯一内部拐点 $t_{\mathrm{inf}}$，当且仅当
    \[
    S_c<2\bar S<S^*.
    \]
    等价地，
    \[
    \frac{1}{2}
    <(1-\beta)(1-q_0)
    <-\frac{1}{2}W_{-1}(z_\eta).
    \]
    当内部拐点存在时，其对应的易感人数和发生时间分别为：
    \[
    S(t_{\mathrm{inf}})=2\bar S,\quad
    t_{\mathrm{inf}}
    =
    t_1+\frac{N}{c_0\eta}\ln\frac{S^*-\bar S}{\bar S},
    \]
    以及拐点处的隔离强度 $\qinf:=q_c(2\bar S)=1-\dfrac{1}{2(1-\beta)}$，只依赖 $\beta$。

    此外:
    \[
    q_c''(t)<0,
    \qquad t_1<t<t_{\mathrm{inf}},
    \]
    而
    \[
    q_c''(t)>0,
    \qquad t_{\mathrm{inf}}<t<t_2.
    \]
\end{theorem}
\begin{proof}
    由定理~\ref{thm:s1:qc-structure}，
    \[
    q_c''(t)
    =
    \frac{\frac{\gamma c_0\eta^2}{\beta N}(S-\bar S)(2\bar S-S)}{S^3}.
    \]
    由于 \(\frac{\gamma c_0\eta^2}{\beta N}>0,\ S>\bar S\)，所以 $q_c''(t)$ 的符号只由 $2\bar S-S(t)$ 决定：
    \[
    S(t)>2\bar S
    \quad\Rightarrow\quad
    q_c''(t)<0,
    \quad S(t)<2\bar S
    \quad\Rightarrow\quad
    q_c''(t)>0.
    \]
    所以$q_c(t)$在$S(t)=2\bar S$ 处可能发生拐点。又因为控制期内 \(S(t)\) 在 \([t_1,t_2]\) 上严格单调递减，
    且 \(S(t_1)=S^*, S(t_2)=S_c\)。
    因此方程 \(S(t)=2\bar S\) 在控制期内至多有一个解。
    它存在且位于内部当且仅当
    \[
    S_c<2\bar S<S^*.
    \]
    由于:
    \[
    \frac{2\bar{S}}{S_c}=2(1-\beta)(1-q_0)
    \]
    所以 $2\bar S>S_c$ 等价于 $(1-\beta)(1-q_0)>\frac{1}{2}$，且拐点相对 $S_c$ 的位置只受 $\beta$ 和 $q_0$ 的影响。
    
    同时，由式~\eqref{eq:s1:Sstar-lambert}，
    \[
    \frac{S^*}{2\bar S}
    =
    -\frac{W_{-1}(z_\eta)}
    {2(1-\beta)(1-q_0)}.
    \]
    所以 $2\bar S<S^*$ 等价于
    \[
    (1-\beta)(1-q_0)<-\frac{1}{2}W_{-1}(z_\eta).
    \]

    综上，内部拐点 $t_{\mathrm{inf}}$ 存在，当且仅当
    \[
    \frac{1}{2}
    <(1-\beta)(1-q_0)
    <-\frac{1}{2}W_{-1}(z_\eta),
    \]
    此时 $S(t_{\mathrm{inf}})=2\bar S$。
    
    由 $\dot{S}=-\frac{c_0\eta}{N}(S-\bar S)$，求解并代入初值条件 $S(t_1)=S^*$，可得
    \[
    S(t)=\bar S+(S^*-\bar S)
    e^{-\frac{c_0\eta}{N}(t-t_1)}
    \]

    又因为\(S(t_{\mathrm{inf}})=2\bar S\)，得到
    \[
    t_{\mathrm{inf}}
    =
    t_1+
    \frac{N}{c_0\eta}
    \ln\frac{S^*-\bar S}{\bar S}.
    \]
\end{proof}
由于 $q_c(S)=1-\gamma N/(\beta c_0 S)$ 关于 $S$ 严格递增，定理~\ref{thm:s1:inflection} 中的拐点存在条件 $S_c<2\bar S<S^*$ 可等价地写在隔离强度轴上（其中 $\qinf=q_c(2\bar S)$）：
\begin{equation}
q_0<\qinf<q_{\max}.
\label{eq:s1:inflection-exist}
\end{equation}
式~\eqref{eq:s1:inflection-exist} 的两个不等式各由不同参数控制。下界 $\qinf>q_0$ 等价于 $(1-\beta)(1-q_0)>\tfrac12$，\emph{只}取决于 $\beta,q_0$，与 $c_0,\eta,N$ 无关；上界 $\qinf<q_{\max}$ 等价于 $2\bar S<S^*$，还牵涉 $c_0,\eta$ 与初值（均经启动点 $S^*$ 进入）。这一区分在下文用于判断拐点从哪一端消失。


\subsection{拐点的位置与两端消失}
\label{sec:s1:inflection-position}
上一小节给出拐点存在判据~\eqref{eq:s1:inflection-exist} 及其位置 $S(t_{\mathrm{inf}})=2\bar S$。这里进一步刻画拐点在控制区间内的相对位置，并统一说明拐点在两个边界处如何消失。

由定理~\ref{thm:s1:inflection} 证明中的轨迹
$S(t)=\bar S+(S^*-\bar S)e^{-c_0\eta(t-t_1)/N}$，
拐点前、后两段时长分别为
\begin{equation}
t_{\mathrm{inf}}-t_1
=
\frac{N}{c_0\eta}
\ln\frac{S^*-\bar S}{\bar S},
\label{eq:s1:seg-high}
\end{equation}
\begin{equation}
t_2-t_{\mathrm{inf}}
=
\frac{N}{c_0\eta}
\ln\frac{\bar S}{S_c-\bar S}.
\label{eq:s1:seg-low}
\end{equation}
两式相加即为控制时长 $\Delta t$（式~\eqref{eq:s1:t2}）。
需区分实际时长与无量纲因子：两段实际时长均含公共尺度
$N/(c_0\eta)$；只有式~\eqref{eq:s1:seg-low} 中的后段无量纲因子满足
\[
\ln\frac{\bar S}{S_c-\bar S}
=
\ln
\frac{(1-\beta)(1-q_0)}
{1-(1-\beta)(1-q_0)},
\]
因而仅依赖 $\beta,q_0$。前段无量纲因子还经 $S^*$ 依赖
$c_0,\eta$ 与初值。

前、后两段共享同一时间尺度，故定义拐点在控制区间内的归一化相对位置
\begin{equation}
\lambda
:=
\frac{t_{\mathrm{inf}}-t_1}{t_2-t_1}
=
\frac{
\ln\dfrac{S^*-\bar S}{\bar S}
}{
\ln\dfrac{S^*-\bar S}{S_c-\bar S}
}
\in(0,1).
\label{eq:s1:lambda}
\end{equation}
$\lambda\to0$ 表示归一化位置靠近 $t_1$ 端，
$\lambda\to1$ 表示靠近 $t_2$ 端。
这不表示绝对时刻 $t_{\mathrm{inf}}$ 具有相同的变化方向；
第~\ref{sec:num}节参数下 $\lambda=0.458$。

$\lambda$ 描述隔离强度减弱的特点. $q_c(t)$ 从 $q_{\max}$ 到 $q_0$ 近乎直线（其偏离首尾连线的幅度由第~\ref{sec:s1:inflection-curvature}小节的曲率上界~\eqref{eq:s1:qcpp-bound} 封顶），拐点是这条近直线朝哪一端轻微鼓出的位置，也是释放速率 $\lvert q_c'\rvert$ 最大的时刻。$\lambda\to1$ 时曲线偏向 $t_2$ 端（``后重型''：$q_c$ 在窗口大部分时间略高于线性时程，回落集中在末段），$\lambda\to0$ 时偏向 $t_1$ 端（``前重型''：回落集中在前段，随后略低于线性时程贴近 $q_0$）。由下文命题~\ref{prop:s1:lambda-sensitivity}，传染性增强（$\beta,c_0\uparrow$）使释放后重（$\lambda\uparrow$），医疗容量增大（$\eta\uparrow$）使其前重（$\lambda\downarrow$），基线隔离率 $q_0$ 的作用一般不单调。两点需强调：其一，$\lambda$ 是控制期内的归一化占比，绝对的控制快慢与代价由控制时长 $\Delta t$、清零时间 $t_{\mathrm{end}}$ 与成本 $J$ 承担，二者可脱钩（沿 $c_0$ 有 $\lambda_{c_0}>0$ 单调，而 $\Delta t_{c_0}$ 由推论~\ref{cor:s1:duration-sensitivity} 的 $\Theta$ 判据定、符号不定）；其二，$\lambda$ 同时被传染性（$\beta,c_0$）、医疗容量（$\eta$）与基线管控强度（$q_0$）三类输入牵动，后两者并非疾病严重程度，故 $\lambda$ 刻画的是释放节奏的偏向，而非疫情是否严重。

\begin{lemma}[启动点的四参数敏感性]
\label{lem:s1:Sstar-sensitivity}
当\(S_0>S_c,I_0<\eta<I_{\max}^{no}\)时,固定其余参数，
启动点 $S^*$ 满足
\[
S^*_\eta<0,
\qquad
S^*_{c_0}>0,
\qquad
S^*_\beta>0,
\qquad
S^*_{q_0}<0.
\]
若固定 $N,S_0,I_0$ 并以 $\theta=\eta/N$ 为参数，则
\[
S^*_\theta=NS^*_\eta<0.
\]
\end{lemma}

\begin{proof}
由常规控制轨道的首次积分，在本证明中暂记阈值方程左端为
\begin{equation}
\begin{aligned}
F(S^*;\eta,c_0,\beta,q_0)
:={}&
I_0+
\frac{\beta(1-q_0)}
{\beta+q_0(1-\beta)}
(S_0-S^*) \\
&+
\rho_1\ln\frac{S^*}{S_0}
-\eta
=0.
\end{aligned}
\label{eq:s1:F-Sstar-param}
\end{equation}
由于 $\rho_1/S_c=\beta(1-q_0)/[\beta+q_0(1-\beta)]$，
\[
F_{S^*}
=
\rho_1
\left(
\frac1{S^*}-\frac1{S_c}
\right)<0,
\]
其中使用了上升支条件 $S^*>S_c$。隐函数定理因而适用。

首先，$F_\eta=-1$，故
\begin{equation}
S^*_\eta
=
\frac{1}{
\rho_1
\left(
\dfrac1{S^*}-\dfrac1{S_c}
\right)
}
<0.
\label{eq:s1:Sstar-eta-sign}
\end{equation}
其次，$\rho_{1,c_0}=-\rho_1/c_0$，从而
\[
F_{c_0}
=
-\frac{\rho_1}{c_0}\ln\frac{S^*}{S_0}>0,
\]
并有
\begin{equation}
S^*_{c_0}
=
\frac{\ln(S^*/S_0)}
{c_0
\left(
\dfrac1{S^*}-\dfrac1{S_c}
\right)
}
>0.
\label{eq:s1:Sstar-c0-sign}
\end{equation}

对 $\beta$，固定 $S^*$ 时
\[
\frac{\partial}{\partial\beta}
\frac{\beta(1-q_0)}
{\beta+q_0(1-\beta)}
=
\frac{q_0(1-q_0)}
{\left[\beta+q_0(1-\beta)\right]^2},
\]
\[
\rho_{1,\beta}
=
-\frac{\rho_1(1-q_0)}
{\beta+q_0(1-\beta)}.
\]
因此
\begin{equation}
S^*_\beta
=
\frac{
\displaystyle
\frac{q_0(1-q_0)}
{\left[\beta+q_0(1-\beta)\right]^2}
(S_0-S^*)
-
\frac{\rho_1(1-q_0)}
{\beta+q_0(1-\beta)}
\ln\frac{S^*}{S_0}
}{
\displaystyle
\rho_1
\left(
\frac1{S_c}-\frac1{S^*}
\right)
}
>0.
\label{eq:s1:Sstar-beta-sign}
\end{equation}
上式分母为正；分子两项均为正，因为
$S_0>S^*$ 且 $\ln(S^*/S_0)<0$。

最后，对 $q_0$ 有
\[
\frac{\partial}{\partial q_0}
\frac{\beta(1-q_0)}
{\beta+q_0(1-\beta)}
=
-\frac{\beta}
{\left[\beta+q_0(1-\beta)\right]^2},
\qquad
\rho_{1,q_0}
=
-\frac{\rho_1(1-\beta)}
{\beta+q_0(1-\beta)}.
\]
利用
\[
\frac{\rho_1(1-\beta)}
{\beta+q_0(1-\beta)}
=
\frac{\beta\bar S}
{\left[\beta+q_0(1-\beta)\right]^2},
\]
可得
\begin{equation}
S^*_{q_0}
=
-\frac{
\beta
\left[
(S_0-S^*)
-\bar S\ln\frac{S_0}{S^*}
\right]
}{
\left[\beta+q_0(1-\beta)\right]^2
\rho_1
\left(
\dfrac1{S_c}-\dfrac1{S^*}
\right)
}
<0.
\label{eq:s1:Sstar-q0-sign}
\end{equation}
事实上，由 $\bar S<S^*<S_0$ 及 $\ln x<x-1$（$x>1$）知
\[
\bar S\ln\frac{S_0}{S^*}
<
S^*\ln\frac{S_0}{S^*}
<
S_0-S^*,
\]
故式~\eqref{eq:s1:Sstar-q0-sign} 方括号内严格为正。
当固定 $N$ 且 $\eta=N\theta$ 时，链式法则给出
$S^*_\theta=NS^*_\eta<0$。
\end{proof}

\begin{proposition}[拐点相对位置的参数敏感性]
\label{prop:s1:lambda-sensitivity}
当\(S_0>S_c,I_0<\eta<I_{\max}^{no}\)以及$S_c<2\bar S<S^*$ ,固定其余参数,则
\[
\lambda_\eta<0,
\qquad
\lambda_{c_0}>0,
\qquad
\lambda_\beta>0.
\]
关于常规控制隔离率 $q_0$，偏导 $\lambda_{q_0}$ 一般没有固定符号；
其驻点满足
\begin{equation}
\frac{
\ln\dfrac{S^*-\bar S}{\bar S}
}{
(1-q_0)\bigl[\beta+q_0(1-\beta)\bigr]
}
=
-
\frac{
\ln\dfrac{\bar S}{S_c-\bar S}
}{
S^*-\bar S
}
S^*_{q_0}.
\label{eq:s1:lambda-q0-stationary}
\end{equation}
若固定 $N,S_0,I_0$，则
$\lambda_\theta=N\lambda_\eta<0$。
\end{proposition}

\begin{proof}
为简化本证明中的偏导计算，暂记
\[
A:=
\ln\frac{S^*-\bar S}{\bar S},
\qquad
B:=
\ln\frac{\bar S}{S_c-\bar S}.
\]
内部拐点条件保证 $A,B>0$，且
\[
\lambda=\frac{A}{A+B},
\qquad
\lambda_p
=
\frac{BA_p-AB_p}{(A+B)^2}.
\]
由引理~\ref{lem:s1:Sstar-sensitivity}，
\[
A_\eta
=
\frac{S^*_\eta}{S^*-\bar S}<0,
\qquad
B_\eta=0,
\]
故 $\lambda_\eta=BA_\eta/(A+B)^2<0$。

由于 $\bar S_{c_0}=-\bar S/c_0$，直接整理得
\[
A_{c_0}
=
\frac{S^*_{c_0}+S^*/c_0}{S^*-\bar S}>0,
\qquad
B_{c_0}=0,
\]
从而 $\lambda_{c_0}>0$。

又有
\[
\bar S_\beta
=
-\frac{\bar S}{\beta(1-\beta)},
\]
\[
A_\beta
=
\frac{
S^*_\beta+\dfrac{S^*}{\beta(1-\beta)}
}{
S^*-\bar S
}>0,
\qquad
B_\beta
=
-\frac{1}{
(1-\beta)\bigl[\beta+q_0(1-\beta)\bigr]
}<0.
\]
因此
\[
\lambda_\beta
=
\frac{BA_\beta-AB_\beta}{(A+B)^2}>0.
\]

最后，$\bar S$ 不含 $q_0$，故
\[
A_{q_0}
=
\frac{S^*_{q_0}}{S^*-\bar S}<0,
\qquad
B_{q_0}
=
-\frac{1}{
(1-q_0)\bigl[\beta+q_0(1-\beta)\bigr]
}<0.
\]
于是
\[
\lambda_{q_0}
=
\frac{1}{(A+B)^2}
\left[
\frac{B S^*_{q_0}}{S^*-\bar S}
+
\frac{A}{
(1-q_0)\bigl[\beta+q_0(1-\beta)\bigr]
}
\right].
\]
方括号内第一项为负、第二项为正，故总符号不能统一确定。
令其等于零并展开本证明内的临时记号 $A,B$，即得
式~\eqref{eq:s1:lambda-q0-stationary}。固定 $N$ 时，
$\eta=N\theta$ 给出 $\lambda_\theta=N\lambda_\eta<0$。
\end{proof}

\begin{corollaryn}[控制时长的参数方向]
\label{cor:s1:duration-sensitivity}
当\(S_0>S_c,I_0<\eta<I_{\max}^{no}\),固定其余参数有:
\[
\Delta t_\eta<0,
\qquad
\Delta t_\beta>0,
\qquad
\Delta t_{q_0}<0.
\]
若固定 $N,S_0,I_0$，则
$\Delta t_\theta=N\Delta t_\eta<0$。
另一方面，$\Delta t_{c_0}$ 没有固定符号。令
\[
\Theta
=
\frac{S^*-S_c}{S_c}
\left[
\frac{S^*-\bar S}{S^*}
\ln\frac{S^*-\bar S}{S_c-\bar S}
-1
\right],
\]
则
\[
\frac{\partial\Delta t}{\partial c_0}
\gtrless0
\quad\Longleftrightarrow\quad
\ln\frac{S_0}{S^*}
\gtrless\Theta,
\]
等号情形亦对应成立。特别地，若 $\Theta\le0$，则
$\partial\Delta t/\partial c_0>0$。
\end{corollaryn}

\begin{proof}
由
\[
\Delta t
=
\frac{N}{c_0\eta}
\ln\frac{S^*-\bar S}{S_c-\bar S}
\]
及 $S^*_\eta<0$，有
\[
\frac{\partial\Delta t}{\partial\eta}
=
-\frac{N}{c_0\eta^2}
\ln\frac{S^*-\bar S}{S_c-\bar S}
+
\frac{N}{c_0\eta}
\frac{S^*_\eta}{S^*-\bar S}
<0.
\]
固定 $N$ 时 $\eta=N\theta$，故
$\Delta t_\theta=N\Delta t_\eta<0$；等价地，
\[
\frac{\partial\Delta t}{\partial\theta}
=
\frac1{c_0}
\left[
\frac1{\theta}
\frac{S^*_\theta}{S^*-\bar S}
-
\frac1{\theta^2}
\ln\frac{S^*-\bar S}{S_c-\bar S}
\right]<0.
\]

对 $\beta$，
\[
\frac{\partial}{\partial\beta}
\ln\frac{S^*-\bar S}{S_c-\bar S}
=
\frac{S^*_\beta-\bar S_\beta}{S^*-\bar S}
-
\frac{(S_c)_\beta-\bar S_\beta}{S_c-\bar S}.
\]
其中
\[
\bar S_\beta
=
-\frac{\bar S}{\beta(1-\beta)}<0,
\qquad
(S_c)_\beta-\bar S_\beta
=
-\frac{q_0S_c}{\beta}\le0.
\]
第一项严格为正，第二项非负，故 $\Delta t_\beta>0$。

由于 $\bar S$ 不含 $q_0$，
\[
\frac{\partial}{\partial q_0}
\ln\frac{S^*-\bar S}{S_c-\bar S}
=
\frac{S^*_{q_0}}{S^*-\bar S}
-
\frac{(S_c)_{q_0}}{S_c-\bar S},
\qquad
(S_c)_{q_0}
=
\frac{S_c}{1-q_0}>0.
\]
结合 $S^*_{q_0}<0$，得到 $\Delta t_{q_0}<0$。

最后，由
$\bar S_{c_0}=-\bar S/c_0$、
$(S_c)_{c_0}=-S_c/c_0$ 及
式~\eqref{eq:s1:Sstar-c0-sign}，可得
\[
\frac{\partial\Delta t}{\partial c_0}
=
\frac{N}{c_0^2\eta}
\left[
\frac{S^*}{S^*-\bar S}
\left(
1+
\frac{S_c}{S^*-S_c}
\ln\frac{S_0}{S^*}
\right)
-
\ln\frac{S^*-\bar S}{S_c-\bar S}
\right].
\]
将方括号与零比较并等价变形，正好得到
$\ln(S_0/S^*)\gtrless\Theta$。由于
$\ln(S_0/S^*)>0$，当 $\Theta\le0$ 时导数必为正。
\end{proof}

引理~\ref{lem:s1:Sstar-sensitivity}、命题~\ref{prop:s1:lambda-sensitivity} 与推论~\ref{cor:s1:duration-sensitivity} 的定号结论分列两表，避免混读：表~\ref{tab:s1:lambda-sensitivity} 给出启动点 $S^*$ 与相对位置 $\lambda$ 的方向，表~\ref{tab:s1:inflection-roles} 给出两端存在裕量与控制时长 $\Delta t$ 的方向。

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{启动点与拐点归一化相对位置的解析方向}
  \label{tab:s1:lambda-sensitivity}
  \begin{tabular}{lccc}
    \toprule
    参数 $p$ & $S^*_p$ & $\lambda_p$ & 解释\\
    \midrule
    $\eta$  & $<0$ & $<0$ & 归一化位置向 $t_1$ 端移动\\
    $c_0$   & $>0$ & $>0$ & 归一化位置向 $t_2$ 端移动\\
    $\beta$ & $>0$ & $>0$ & 归一化位置向 $t_2$ 端移动\\
    $q_0$   & $<0$ & 符号不定 & 由式~\eqref{eq:s1:lambda-q0-stationary} 判定\\
    \bottomrule
  \end{tabular}
\end{table}

对于 $q_0$，解析结论是偏导符号一般不能统一确定，并不意味着每一组固定参数下
$\lambda(q_0)$ 都必然非单调。作为数值展示，在
$N=763,S_0=762,I_0=1,\gamma=0.3504,c_0=10,\eta/N=0.05$
且 $q_0\in[0,0.39]$ 的指定扫描区间内，驻点残差发生变号的位置为
\[
\begin{array}{c|ccc}
\beta & 0.10 & 0.12 & 0.155\\
\hline
q_0^\dagger
&0.0736525&0.1969606&0.3890712
\end{array}
\]
三个检测根两侧均为 $\lambda_{q_0}$ 由正变负，因而在相应参数切片上表现为
局部极大。以 $10^{-7}$ 为残差阈值，本次扫描未检测到不变号的近零候选点。
上述根由变号区间上的数值求根得到，不构成驻点存在性、唯一性或搜索完备性的证明。

判据~\eqref{eq:s1:inflection-exist} 的两个不等式恰对应拐点从两端掉出的两种结构：
\begin{itemize}
\item 当 $(1-\beta)(1-q_0)\le\tfrac12$（即 $2\bar S\le S_c$、$\qinf\le q_0$）时，控制期内恒有 $S\ge S_c\ge 2\bar S$，故 $q_c''<0$ 全程成立（$q_c$ 凹）：拐点经 $t_2$ 端掉出。
\item 当 $2\bar S\ge S^*$（即 $\qinf\ge q_{\max}$）时，控制期内恒有 $S\le S^*\le 2\bar S$，故 $q_c''>0$ 全程成立（$q_c$ 凸）：拐点经 $t_1$ 端掉出。
\end{itemize}
这把两种``无内部拐点''的情形统一为``拐点从哪一端掉出''，其参数方向汇总于表~\ref{tab:s1:inflection-roles}。

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{拐点结构量与控制时长的参数方向（固定 $N,S_0,I_0$ 及未被求导参数）。$+$、$-$ 分别表示相应量或存在裕量的局部导数为正、负，$\times$ 表示不依赖该参数；$c_0$ 对 $\Delta t$ 的方向须由推论~\ref{cor:s1:duration-sensitivity} 的 $\Theta$ 判据确定。}
  \label{tab:s1:inflection-roles}
  \begin{tabularx}{\textwidth}{lccccX}
    \toprule
    影响对象 & $\beta$ & $q_0$ & $c_0$ & $\theta=\eta/N$ & 解析依据\\
    \midrule
    $\qinf$ 的值
      & $-$ & $\times$ & $\times$ & $\times$
      & $\qinf=1-[2(1-\beta)]^{-1}$\\
    $t_2$ 端存在裕量（$\qinf-q_0$）
      & $-$ & $-$ & $\times$ & $\times$
      & 式~\eqref{eq:s1:inflection-exist} 的下界\\
    $t_1$ 端存在裕量（$S^*-2\bar S$）
      & $+$ & $-$ & $+$ & $-$
      & 引理~\ref{lem:s1:Sstar-sensitivity} 及 $\bar S$ 的显式式\\
    控制时长 $\Delta t$
      & $+$ & $-$ & 判据 & $-$
      & 推论~\ref{cor:s1:duration-sensitivity}\\
    \bottomrule
  \end{tabularx}
\end{table}

图~\ref{fig:s1:lambda-sensitivity} 直接绘制归一化位置 $\lambda$。前三个面板对应命题~\ref{prop:s1:lambda-sensitivity} 的三个定号导数；$q_0$ 面板则展示竞争项可以形成驻点，且检测位置随 $\beta$ 改变。灰色区域把``控制不可行''与``控制可行但无内部拐点''分开，避免把不存在的 $\lambda$ 延拓到边界之外。在图示基准参数下（其余固定为 $N=763,\,S_0=762,\,I_0=1,\,\gamma=0.3504$，未被扫描者取 $\beta=0.155,\,c_0=10,\,q_0=0.01526,\,\eta=0.05N$），控制可行——常规轨道能触及医疗红线 $\eta$ 且初值处于增长支——要求
\[
\frac{\eta}{N}\in\Big(\frac{I_0}{N},\,\frac{I_{\max}^{no}}{N}\Big)\approx(0.0013,\,0.394),\quad c_0\gtrsim3.33,\quad \beta\gtrsim0.053,\quad q_0\lesssim0.398;
\]
越出该范围加强控制不被触发。$c_0,\beta,\eta/N$ 的不可行区都一直延伸到 $0$（图中灰色仅为落在扫描窗口内的部分），只有 $q_0$ 的不可行区位于上界一侧（$q_0>0.398$）；$q_0$ 面板的灰色阴影只对应基准 $\beta=0.155$ 的不可行区。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{scenario1_inflection_lambda_sensitivity.pdf}
    \caption{拐点归一化位置 $\lambda$ 的四参数响应。前三幅在基准参数 $N=763$、$S_0=762$、$I_0=1$、$\gamma=0.3504$、$\beta=0.155$、$c_0=10$、$q_0=0.01526$、$\eta/N=0.05$ 下分别扫描 $\eta/N,c_0,\beta$；右下面板沿 $q_0$ 扫描并比较 $\beta=0.10,0.12,0.155$。浅灰表示控制不可行，深灰表示控制可行但无内部拐点（灰色区间的两侧即拐点经 $t_1$、$t_2$ 端消失的边界）。空心圆为驻点残差发生变号后数值精化得到的检测根（本组容差下未检出不变号的近零候选点）。数值搜索不构成驻点存在性、唯一性或完备性的证明。}
    \label{fig:s1:lambda-sensitivity}
\end{figure}


图~\ref{fig:s1:inflection-landscape} 各行反映的是相应基准参数和扫描范围内的边界行为,把存在性与两段实际时长沿四个参数各画一行。由于 $\qinf$ 只含 $\beta$，它在 $q_0,c_0,\eta/N$ 三行均为水平线，只在 $\beta$ 行随 $\beta$ 下降。各行的消失行为并不相同：沿 $\beta$ 扫描可分别跨越 $t_1$ 端和 $t_2$ 端边界；沿 $c_0$ 扫描在左端跨越 $S^*=2\bar{S}$ 的 $t_1$-exit 边界。在本图采用的 $q_0$ 与 $\eta/N$ 扫描范围内，可行域内部未跨越该上边界，因此拐点保持存在。这只是当前参数切片的数值现象，并不表示 $q_0$ 或 $\eta/N$ 不进入存在性判据。一般而言，下界 $S_c<2\bar{S}$ 仅由 $\beta,q_0$ 决定，而上界 $2\bar{S}<S^*$ 还经 $S^*$ 受到 $c_0,\eta,\beta,q_0$ 及初值影响。$q_0$ 行在可行区内拐点始终存在，右端的截断是控制本身不可行（常规控制触不到红线）而非拐点消失；图~\ref{fig:s1:inflection-scan} 以真实时间为横轴，仅作为绝对启动时刻、解除时刻与控制时长的补充；
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{scenario1_inflection_landscape.pdf}
    \caption{拐点的存在性、位置与两段时长图谱。四行分别沿 $q_0,\beta,c_0,\eta/N$ 扫描，其余参数固定为 $N=763$、$S_0=762$、$I_0=1$、$\gamma=0.3504$，以及未被扫描者取 $\beta=0.155$、$q_0=0.01526$、$c_0=10$、$\eta=0.05N$。左列为 $q_{\max}$、$\qinf$、$q_0$ 三条曲线，填色区即 $q_0<\qinf<q_{\max}$ 成立（存在内部拐点）的区间，灰虚线为存在性边界；右列为前段 $t_{\mathrm{inf}}-t_1$（深）与后段 $t_2-t_{\mathrm{inf}}$（浅）的时长堆叠，虚线为 $\Delta t$。}
    \label{fig:s1:inflection-landscape}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{scenario1_inflection_scan_t.pdf}
    \caption{四个参数各取低、中、高三值时的隔离控制律 $q_c(t)$，横轴为真实时间 $t$（天）；圆点为拐点，短竖线标出控制解除时刻 $t_2$。参数固定同图~\ref{fig:s1:inflection-landscape}。须注意每条曲线的水平跨度是控制时长 $\Delta t$，而曲线整体的左右位置差来自启动时刻 $t_1$ 不同（传播越快 $t_1$ 越早），两者不可混读：例如 $c_0=13$ 的曲线靠左且短（$t_1\approx2.3$ 天），$c_0=5$ 的靠右且长（$t_1\approx9.3$ 天）。}
    \label{fig:s1:inflection-scan}
\end{figure}

图~\ref{fig:s1:inflection-landscape}（沿 $\beta$、$c_0$ 等单参数扫描）从一维刻画内部拐点的存在性。将存在判据 $s_c<2\bar s<s^*$ 直接画在 $(c_0,\beta)$ 平面上（图~\ref{fig:c0-beta-existence}，附录~\ref{app:c0_beta}，固定 $\theta=0.002$、$q_0=0.3230$），可统一读出两端消失：上界 $2\bar s<s^*$（$t_1$ 端，随 $c_0,\beta$ 弯曲）与下界 $\beta<\beta_{\max}=0.2614$（$t_2$ 端，只由 $\beta,q_0$ 定的水平线）围出内部拐点存在域。图~\ref{fig:c0-phase} 是本图沿 $c_0$ 轴的截面；西安参数 $(c_0,\beta)=(12.8872,0.1498)$ 落在域内。

\subsection{拐点处的曲率幅度}
\label{sec:s1:inflection-curvature}
存在拐点并不意味着 $q_c(t)$ 在视觉上明显弯曲。将定理证明中 $q_c''$ 的表达式按 $x=S/\bar S$ 改写，可分离出时间尺度与形状两个因子：
\begin{equation}
q_c''(t)=\underbrace{\left(\frac{c_0\eta}{N}\right)^{2}\frac{1}{1-\beta}}_{\text{时间尺度因子}}\;\underbrace{\frac{(x-1)(2-x)}{x^{3}}}_{g(x)},\qquad x=\frac{S}{\bar S}.
\label{eq:s1:qcpp-factor}
\end{equation}
形状因子 $g$ 满足 $g(2)=0$（拐点处），在 $x=3\pm\sqrt3\approx1.27,\,4.73$ 取极值，且 $|g|\le\frac{1}{6\sqrt3}\approx0.096$，与 $c_0,\eta,N,\beta$ 无关。由此得 $q_c''$ 的一致上界
\begin{equation}
\lvert q_c''(t)\rvert\le\frac{1}{6\sqrt3\,(1-\beta)}\Big(\frac{c_0\eta}{N}\Big)^{2},
\label{eq:s1:qcpp-bound}
\end{equation}
其实际最大值还取决于控制期覆盖的区间 $x\in[S_c/\bar S,\,S^*/\bar S]$ 是否含 $3\pm\sqrt3$。

式~\eqref{eq:s1:qcpp-factor} 中的时间尺度因子 $(c_0\eta/N)^2/(1-\beta)$ 只决定横轴单位：减小 $\eta/N$ 使 $\lvert q_c''\rvert\propto(\eta/N)^2$ 变小，但控制期 $\Delta t\propto N/(c_0\eta)$ 同步变长，等价于把同一条曲线沿天数轴拉宽——曲线形状（相对首尾连线的弯曲）几乎不变，而非变直\footnote{可用归一化弦偏差 $\max_{t\in[t_1,t_2]}\lvert q_c(t)-L(t)\rvert/(q_{\max}-q_0)$ 量度弯曲，其中 $L$ 为连接 $(t_1,q_{\max})$ 与 $(t_2,q_0)$ 的直线；该量与横轴单位无关。即便第~\ref{sec:xian}节西安参数一组也仅约 $9.6\%$，拐点虽严格存在，$q_c$ 在视觉上仍接近直线。}。数值上，取 $N=763,\ \beta=0.155,\ c_0=10,\ q_0=0.01526$，把 $\eta/N$ 由 $0.05$ 降到 $0.005$，则 $\Delta t$ 由约 $5.9$ 天拉长到约 $60.7$ 天、$\max\lvert q_c''\rvert$ 降为约 $1/100$，而弯曲程度（弦偏差）仅由 $4.44\%$ 变到 $4.17\%$。

从展示角度看，拐点在 $q_c$ 上是曲率过零（最不显眼的特征），在 $q_c'$ 上则是极小值（最显眼的特征）：上述参数下 $q_c'$ 谷底比两端深约 $1.5$--$1.8$ 倍。因此要呈现拐点应画 $q_c'$，而非试图通过调参把 $q_c$ 掰弯——弯曲幅度受式~\eqref{eq:s1:qcpp-bound} 的上界约束。相应三面板诊断见图~\ref{fig:s1:inflection-diagnose}（第~\ref{sec:num}节）。

\subsection{阈值控制期的$\dot{S}$和控制解除时间$t_2$以及动态控制 $q_c(t)$}
已知在阈值控制阶段内，我们有$\dot{I}=0,I=\eta$,因此可以直接求解出所需的控制函数的状态反馈解析式 $q_c(t)=1-\frac{\gamma N}{\beta c_0 S(t)}$,显然$q_c(t)$是由$S(t)$决定的。我们将上述条件其带入 $\dot{S}$ 的动态方程中,可以得到易感者的动态方程$\dot{S}=-(c_0\eta/N)(S-\bar S)$,
因此$S(t)$如何变化是确定的.
\begin{theorem}
    对于系统\eqref{eq:s1:dynamics},阈值控制解除的时间$t_2$由下式给出
    \begin{equation}
    t_2
    =
    t_1+
    \frac{N}{c_0 \eta}
    \ln \left[
    \frac{S^*-\bar S}
    {S_c-\bar S}
    \right]
    \label{eq:s1:t2}
    \end{equation}
    控制持续时间定义为:
        \begin{equation}
        \Delta t = \frac{N}{c_0 \eta}
        \ln \left[
        \frac{S^*-\bar S}
        {S_c-\bar S}
        \right]
    \end{equation}
\end{theorem}
\begin{proof}   
    利用阈值控制期内 $\dot{S}$ 的显式表达式积分，并注意加强控制解除时应恢复到常规防控水平下的临界阈值 $S_c$，也就是在$t_1$到$t_2$的这个时间段内,$S^*$下降到$S_c$,于是：
    \begin{align*}
        \Delta t=t_2-t_1=\int_{t_1}^{t_2} dt=&\int_{S^*}^{S_c} \frac{1}{\dot{S}} \, dS
        =\frac{N}{c_0\eta}\int_{S_c}^{S^*} \frac{1}{S-\bar S}\,dS
        =\frac{N}{c_0 \eta}
        \ln \left[
        \frac{S^*-\bar S}
        {S_c-\bar S}
        \right].
    \end{align*}
    因此，
    \begin{equation*}
    t_2
    =
    t_1+
    \frac{N}{c_0 \eta}
    \ln \left[
    \frac{S^*-\bar S}
    {S_c-\bar S}
    \right]=\int_{S_0}^{S^*}
    \frac{1}{
    -\beta_1^0S\left[
    C_0-\frac{\beta_2^0}{\beta_1^0}S+\rho_1\ln S
    \right]
    }\,dS + \frac{N}{c_0 \eta}
    \ln \left[
    \frac{\frac{\beta c_0}{\gamma N}S^*+ \beta-1}
    {\frac{q_0}{1-q_0}+ \beta}
    \right]
    \end{equation*}
\end{proof}

\begin{theorem}
    对于系统\eqref{eq:s1:dynamics},阈值控制期内易感人数的演化可以严格表示为如下连续显式解析函数：
    \begin{equation}
        S(t) = \left( S^* - \bar S \right) e^{\frac{-c_0\eta}{N}(t - t_1 )} + \bar S
        \label{eq:s1:S-analytic}
    \end{equation}
    隔离控制函数为:
    \begin{equation*}
    q(t) =
    \begin{cases}
    q_0, & t < t_1, \\
    q_c(t) & t_1 \le t \le t_2, \\
    q_0, & t > t_2.       
    \end{cases}
    \end{equation*}
    其中
    \begin{equation}
        q_c(t)= 1 - \frac{1}{\left[\frac{\beta c_0 S^* }{\gamma N} + \beta- 1 \right] e^{\frac{-c_0 \eta}{N} (t-t_1)} + 1-\beta} 
    \end{equation}
\end{theorem}
\begin{proof}
    由上述可知$\dot{S}=-\frac{c_0\eta}{N}(S-\bar S)$ ,对于这样一个关于 $S(t)$ 的一阶常系数线性非齐次常微分方程求解可得:
    \[
        S(t) = C e^{\frac{-c_0 \eta}{N} t} + \bar S
    \]
    其中 $C$ 是积分常数,带入初值条件 $S(t_1) = S^*$。从而：
    \[
    S(t) = \left( S^* - \bar S \right) e^{\frac{-c_0\eta}{N}(t - t_1 )} + \bar S
    \]
    最终，将 $S(t)$ 的解析解代入控制函数$q_c(S(t))=1-\frac{\gamma N}{\beta c_0 S(t)}$，即可得到关于时间的隔离加强控制函数 $q_c(t)$:
    \[
    q_c(t)= 1 - \frac{1}{\left[\frac{\beta c_0 S^* }{\gamma N} + \beta- 1 \right] e^{\frac{-c_0 \eta}{N} (t-t_1)} + 1-\beta}
    \]
\end{proof}
\begin{remark}
    由终端条件$S(t_2) = S_c = \frac{\gamma N}{\beta c_0(1-q_0)}$,带入\eqref{eq:s1:S-analytic}也可以解得控制结束时间$t_2$：
    \begin{equation*}
     t_2= t_1+\frac{N}{c_0 \eta}
     \ln \left[
     \frac{S^*-\bar S}
     {S_c-\bar S}
     \right]
    \end{equation*}
    以及控制持续时间
    \begin{equation*}
    \Delta t = \frac{N}{c_0 \eta}
     \ln \left[
     \frac{S^*-\bar S}
     {S_c-\bar S}
     \right]
    \end{equation*}
\end{remark}
\begin{remark}
    对于$S(t) = C e^{\frac{-c_0 \eta}{N} t} + \bar S$,我们也可以带入给定终端条件 $S(t_2) = S_c$，可得：
    \begin{equation*}
    S(t) = \left( S_c - \bar S \right) e^{\frac{c_0\eta}{N}(t_2 - t )} + \bar S
    \end{equation*}
    于是，再带入$q_c(S(t))=1-\frac{\gamma N}{\beta c_0 S(t)}$,可知阈值控制期内的隔离控制函数也可表示为：
    \begin{align*}
    q_c(t)=1 - \frac{1}{(\tfrac{q_0}{1-q_0}+\beta)e^{\frac{c_0\eta}{N}(t_2 - t )}+1-\beta}
    \end{align*}
\end{remark}

\subsection{情景一实现动态清零所需时间$t_{end}$}
我们假设传染病在$t=t_{end}$时刻在隔离区外被根除(实现动态清零),条件是$I'(t_{end})<0$且$I(t_{end})=1$.
\begin{theorem}\label{thm:s1:tend}
    对于系统\eqref{eq:s1:dynamics},实现动态清零目标所需时间由下式给出
    \[
    t_{end}=t_2+\int_{S_c}^{S(t_{end})}\frac{1}
    {S\left[\beta_2^0 S-\beta_1^0 \rho_1 \ln(S)-
    \beta_1^0 C_2\right]}\, dS .
    \]
    其中
    \begin{equation}
    S(t_{end})=
    -\frac{\beta_1^0 \rho_1}{\beta_2^0}\,
    \operatorname{LambertW}_0
    \left(
    -\frac{\beta_2^0}{\beta_1^0 \rho_1}
    \exp\left(
    \frac{1-C_2}{\rho_1}
    \right)
    \right).
    \label{eq:s1:Send}
    \end{equation}
\end{theorem}
\begin{proof}
    由于在情景一中，当 $t\ge t_2$ 时，系统恢复到常规防控水平
    \[
    c(t)=c_0,\qquad q(t)=q_0.
    \]
    因此，$t\in[t_2,+\infty)$ 上仍满足常规阶段的首次积分。不过此时首次积分常数由
    $t=t_2$ 时刻的状态决定。记:
    \[
    C_2=\eta+\frac{\beta_2^0}{\beta_1^0}S(t_2)-\rho_1\ln S(t_2)=\eta+\frac{\beta_2^0}{\beta_1^0}S_c-\rho_1\ln S_c.
    \]
    于是
    \[
    I(t)
    =
    -\frac{\beta_2^0}{\beta_1^0} S(t)
    +
    \rho_1 \ln S(t)
    +
    C_2,
    \qquad
    t \in [t_2,+\infty).
    \]
    由 $I(t_{end})=1$ 和 $I'(t_{end})<0$ 得到
    \[
    1
    +
    \frac{\beta_2^0}{\beta_1^0} S(t_{end})
    -
    \rho_1 \ln(S(t_{end}))
    =
    C_2 .
    \]
    且$S(t_{end}) < \frac{\gamma}{\beta_2^0}$.使用 Lambert $W$ 函数，求解 $S(t_{end})$ 的方程，我们得到
    \[
    S(t_{end})=
    -\frac{\beta_1^0 \rho_1}{\beta_2^0}\,
    \operatorname{LambertW}_0
    \left(
    -\frac{\beta_2^0}{\beta_1^0 \rho_1}
    \exp\left(
    \frac{1-C_2}{\rho_1}
    \right)
    \right).
    \]
    于是
    \[
    S'(t)=S\left[\beta_2^0 S-
    \beta_1^0 \rho_1 \ln(S)
    -\beta_1^0 C_2
    \right],
    \qquad
    t \in [t_2,+\infty).
    \]
    从时间 $t_2$ 积分到 $t_{end}$，有
    \[
    \int_{S_c}^{S(t_{end})}
    \frac{1}
    {
    S\left[
    \beta_2^0 S
    -
    \beta_1^0 \rho_1 \ln(S)
    -
    \beta_1^0 C_2
    \right]
    }
    \, dS
    =
    \int_{t_2}^{t_{end}} dt .
    \]

    因此
    \[
    t_{end}=t_2+\int_{S_c}^{S(t_{end})}\frac{1}
    {S\left[\beta_2^0 S-\beta_1^0 \rho_1 \ln(S)-
    \beta_1^0 C_2\right]}\, dS .
    \]
\end{proof}





\subsection{情景一总累计感染的解析公式}
从初始时刻到动态清零时刻的总累计感染定义为
\begin{equation}
\Itcum
=
\int_0^{t_{\rm end}}\frac{\beta c(t)}{N}S(t)I(t)\,dt.
\end{equation}
同时，进入社区感染仓室 $I$ 与进入隔离感染仓室 $I_q$ 的累计感染分别为
\begin{equation}
I_{\rm cum}
=
\int_0^{t_{\rm end}}
\frac{\beta c(t)(1-q(t))}{N}S(t)I(t)\,dt,
\end{equation}
和
\begin{equation}
I_{q_{\rm cum}}
=
\int_0^{t_{\rm end}}
\frac{\beta c(t)q(t)}{N}S(t)I(t)\,dt.
\end{equation}
于是
\[
\Itcum=I_{\rm cum}+I_{q_{\rm cum}}.
\]

\begin{theorem}\label{thm:s1:Itcum}
    设情景一阈值控制可以在 $t_1$ 启动，并在 $t_2$ 解除；记
    $S_{\rm end}=S(t_{\rm end})$。则从初始时刻到动态清零时刻的总累计感染为
    \[
    \Itcum
    =
    \frac{\beta}{\beta+q_0(1-\beta)}(S_0-S^*)
    +
    \beta\left[
    (S^*-S_c)+\bar S\ln\frac{S^*-\bar S}{S_c-\bar S}
    \right]
    +
    \frac{\beta}{\beta+q_0(1-\beta)}(S_c-S_{\rm end}).
    \]
\end{theorem}
\begin{proof}
    记总新发感染率为
    \[
    F(t)=\frac{\beta c(t)}{N}S(t)I(t).
    \]
    在第一段常规阶段 $0\le t\le t_1$，有 $c(t)=c_0,\ q(t)=q_0$，故
    \[
    \dot S
    =
    -\frac{c_0[\beta+q_0(1-\beta)]}{N}SI.
    \]
    因而
    \[
    F(t)
    =
    \frac{\beta}{\beta+q_0(1-\beta)}(-\dot S).
    \]
    从 $0$ 到 $t_1$ 积分，并利用 $S(t_1)=S^*$，得到
    \[
    \int_0^{t_1}F(t)\,dt
    =
    \frac{\beta}{\beta+q_0(1-\beta)}(S_0-S^*).
    \]

    在阈值控制阶段 $t_1\le t\le t_2$，有 $I(t)\equiv\eta$，且
    \[
    q_c(t)=1-\frac{\gamma N}{\beta c_0S(t)}.
    \]
    由 $S$ 方程可得
    \[
    \dot S
    =
    -\frac{c_0\eta}{N}(S-\bar S),
    \qquad
    \bar S=\frac{(1-\beta)\gamma N}{\beta c_0}.
    \]
    因此
    \[
    dt
    =
    -\frac{N}{c_0\eta}\frac{dS}{S-\bar S}.
    \]
    又因为此时
    \[
    F(t)=\frac{\beta c_0}{N}S(t)\eta,
    \]
    所以
    \[
    \int_{t_1}^{t_2}F(t)\,dt
    =
    \beta\int_{S_c}^{S^*}\frac{S}{S-\bar S}\,dS
    =
    \beta\left[
    (S^*-S_c)+\bar S\ln\frac{S^*-\bar S}{S_c-\bar S}
    \right].
    \]

    在解除控制后的常规阶段 $t_2\le t\le t_{\rm end}$，仍有
    $c(t)=c_0,\ q(t)=q_0$，因此与第一段相同，
    \[
    F(t)
    =
    \frac{\beta}{\beta+q_0(1-\beta)}(-\dot S).
    \]
    由 $S(t_2)=S_c$ 与 $S(t_{\rm end})=S_{\rm end}$ 得
    \[
    \int_{t_2}^{t_{\rm end}}F(t)\,dt
    =
    \frac{\beta}{\beta+q_0(1-\beta)}(S_c-S_{\rm end}).
    \]

    三段相加即得结论。
\end{proof}

\section{成本与参数敏感性}
\label{sec:cost}

\subsection{成本函数}

为了刻画控制投入，本文使用两个分项归一化成本
\begin{equation}
J_c=\int_{t_1}^{t_2} \frac{\bigl(c_0-c(t)\bigr)_+}{c_0}\,dt,
\end{equation}
\begin{equation}
J_q=\int_{t_1}^{t_2} \frac{\bigl(q(t)-q_0\bigr)_+}{1-q_0}\,dt.
\end{equation}
其中 $(x)_+=\max\{x,0\}$。进一步定义二次加权综合成本
\begin{equation}
J
=
\int_{t_1}^{t_2}
\left[
\left(\frac{\bigl(c_0-c(t)\bigr)_+}{c_0}\right)^2
+
2\left(\frac{\bigl(q(t)-q_0\bigr)_+}{1-q_0}\right)^2
\right]dt.
\end{equation}
该泛函的被积函数为归一化控制强度，\textbf{不含人口规模 $N$}；故不同规模控制单元上的 $J$ 读数处于同一量纲，可直接比较。这一点在第~\ref{sec:dominance}~节比较不同 $N_{\rm eff}$ 上的成本时要用到。

这里隔离率提高项前的系数取为 2，用来表示隔离控制相对于接触率控制的较高边际成本。
在情景一中 $c(t)\equiv c_0$，因此 $J_c=0$；后续数值表和主图只保留综合成本 $J$，
$J_q$ 作为隔离率分项成本在程序输出中保留，用于追溯成本来源。
利用控制期内
\[
\dot S=-\frac{c_0\eta}{N}(S-\bar S),
\]
可将情景一中的 $J_q$ 写为
\begin{equation}
J_q
=
\frac{N}{\eta(1-q_0)}
\int_{S_c}^{S^*}
\frac{q_c(S)-q_0}{c_0(S-\bar S)}\,dS.
\end{equation}
当 $\eta$ 增大时，上限 $S^*$ 下降，同时前面的 $1/\eta$ 下降，故额外累计控制强度随
$\eta$ 增大而下降。对于 $c_0$，积分上限、终点阈值、被积函数以及时间尺度同时改变，
其符号仍存在竞争，需结合后续数值结果判断。



\subsection{情景一的参数敏感性分析}
本节只考察情景一中两个具有直接政策含义的参数：
\[
c_0,\qquad \eta .
\]
其中 $c_0$ 表示常规接触水平，$\eta$ 表示医疗容量阈值。以下分析每次只改变一个参数，
其余参数保持不变，并限定在系统能够首次触碰医疗红线且首次触碰发生在感染人数上升支的局部可行区域。

为便于书写，记
\begin{equation}
a=\frac{\beta_2^0}{\beta_1^0}
=
\frac{\beta(1-q_0)}{\beta+q_0(1-\beta)},
\qquad
\rho_1=\frac{\gamma}{\beta_1^0}
=
\frac{\gamma N}{c_0[\beta+q_0(1-\beta)]},
\end{equation}
以及
\begin{equation}
S_c=\frac{\gamma}{\beta_2^0}
=
\frac{\gamma N}{\beta c_0(1-q_0)}.
\end{equation}
注意到 $a$ 与 $c_0$ 无关，而 $\rho_1$ 与 $S_c$ 均与 $c_0$ 成反比。

在分析 $c_0$ 的影响之前，先固定医疗容量阈值 $\eta$ 以及
$N,S_0,I_0,\beta,\gamma,q_0$，并考虑常规控制
$c(t)\equiv c_0$、$q(t)\equiv q_0$ 下的感染峰值
$I_{\max}^{no}(c_0)$。定义由 $\eta$ 决定的最小接触率为
\begin{equation}
c_{0,\min}(\eta)
=
\inf\left\{
c_0>0:
I_{\max}^{no}(c_0)\geq \eta
\right\}.
\label{eq:s1:c0-min-def}
\end{equation}
由式~\eqref{eq:s1:Imax-no}，并利用
$\beta_2^0/\beta_1^0=a$、$\rho_1=aS_c$，可将常规控制下的峰值写为
\begin{equation}
I_{\max}^{no}(c_0)
=
I_0+a(S_0-S_c)+aS_c\ln\frac{S_c}{S_0}.
\label{eq:s1:Imax-no-c0}
\end{equation}
在感染初期处于增长支的参数范围 $S_c<S_0$ 内，
\begin{equation}
\frac{d I_{\max}^{no}}{dc_0}
=
-\frac{aS_c}{c_0}\ln\frac{S_c}{S_0}>0.
\label{eq:s1:Imax-no-c0-derivative}
\end{equation}
因此，只要给定阈值满足
$I_0<\eta<I_0+aS_0$，式~\eqref{eq:s1:c0-min-def} 所定义的临界值在该分支上存在且唯一，并满足
\begin{equation}
I_{\max}^{no}\!\left(c_{0,\min}(\eta)\right)=\eta.
\label{eq:s1:c0-min-equality}
\end{equation}
后文固定 $\eta$ 时，将 $c_{0,\min}(\eta)$ 简记为 $c_{0,\min}$。于是
\begin{equation}
\begin{aligned}
c_0<c_{0,\min}
&\quad\Longrightarrow\quad I_{\max}^{no}(c_0)<\eta,\\
c_0=c_{0,\min}
&\quad\Longrightarrow\quad I_{\max}^{no}(c_0)=\eta,\\
c_0>c_{0,\min}
&\quad\Longrightarrow\quad I_{\max}^{no}(c_0)>\eta.
\end{aligned}
\label{eq:s1:c0-min-regimes}
\end{equation}
第一种情形下常规控制轨道不触碰医疗红线，因而不启动加强控制；在边界点，常规控制轨道
恰好在峰值处与 $I=\eta$ 相切，此时 $S^*=S_c$ 且 $\Delta t=0$；只有当
$c_0>c_{0,\min}$ 时，轨道才会在感染上升支首次到达 $I=\eta$，并产生正的控制持续时间。
相应地，
\begin{equation}
c_0\downarrow c_{0,\min}^{+}
\quad\Longrightarrow\quad
S^*\to S_c,
\qquad
\Delta t\to0.
\label{eq:s1:c0-min-limit}
\end{equation}
这里 $c_{0,\min}$ 只确定 $\Delta t(c_0)$ 的左端边界；在该边界右侧，
$\Delta t$ 的增减方向仍由下述导数判别式决定，不能由
式~\eqref{eq:s1:c0-min-limit} 推出整个可行区间上的单调性。

\begin{proposition}[启动时间与最大隔离率的参数敏感性]
\label{prop:s1:sensitivity}
在下述结论中，每次只改变一个参数，而其余参数保持不变。
假设所考察参数点位于局部可行区域内，即系统在常规控制下能够首次触碰医疗容量阈值，
且首次触碰发生在感染人数上升支：
\[
c_0>c_{0,\min}(\eta),
\qquad
I_0<\eta<I_{\max}^{no}(c_0),
\qquad
S_0>S^*>S_c>\bar S.
\]
则情景一中的启动时间和最大隔离率满足
\[
(t_1)_\eta>0,
\qquad
(t_1)_{c_0}<0,
\]
以及
\[
(q_{\max})_\eta<0,
\qquad
(q_{\max})_{c_0}>0.
\]
\end{proposition}

\begin{proof}
由引理~\ref{lem:s1:Sstar-sensitivity}，有
$S^*_\eta<0$ 与 $S^*_{c_0}>0$。启动时间可写为
\begin{equation}
t_1
=
\frac{N}{c_0\bigl[\beta+q_0(1-\beta)\bigr]}
\int_{S^*}^{S_0}
\frac{1}{S I(S;c_0)}\,dS,
\qquad
I(S;c_0)
=
I_0-a(S-S_0)+\rho_1\ln\frac{S}{S_0}.
\end{equation}
由于积分下端满足 $I(S^*;c_0)=\eta$，对 $\eta$ 求导得到
\begin{equation}
\frac{\partial t_1}{\partial \eta}
=
-\frac{N}{c_0\bigl[\beta+q_0(1-\beta)\bigr]}
\frac{1}{S^*\eta}
\frac{\partial S^*}{\partial \eta}>0.
\end{equation}
因此，医疗容量越高，系统需要更长时间才触碰红线。对 $c_0$ 求导时，
\[
\frac{\partial I(S;c_0)}{\partial c_0}
=
-\frac{\rho_1}{c_0}\ln\frac{S}{S_0}>0,
\qquad S\in(S^*,S_0),
\]
从而
\begin{align}
\frac{\partial t_1}{\partial c_0}
=&
-\frac{N}{c_0^2\bigl[\beta+q_0(1-\beta)\bigr]}
\int_{S^*}^{S_0}\frac{1}{S I(S;c_0)}\,dS
-\frac{N}{c_0\bigl[\beta+q_0(1-\beta)\bigr]}
\frac{1}{S^*\eta}
\frac{\partial S^*}{\partial c_0} \nonumber\\
&-\frac{N}{c_0\bigl[\beta+q_0(1-\beta)\bigr]}
\int_{S^*}^{S_0}
\frac{I_{c_0}(S;c_0)}{S I^2(S;c_0)}\,dS
<0.
\end{align}
这说明常规接触水平越高，触碰医疗红线的时间越早。

最后考虑最大隔离率。情景一的边界控制律可写为
\[
q_c(S)=1-\frac{\gamma N}{\beta c_0S},
\qquad
\frac{dq_c}{dS}
=
\frac{\gamma N}{\beta c_0S^2}>0.
\]
控制期内 $S(t)$ 单调下降，因此 $q_c(t)$ 在控制期内单调下降，最大值出现在启动时刻 $t_1$：
\[
q_{\max}
=
q_c(t_1)
=
1-\frac{\gamma N}{\beta c_0S^*}.
\]
于是
\[
(q_{\max})_\eta
=
\frac{\gamma N}{\beta c_0(S^*)^2}S^*_\eta<0,
\]
以及
\[
(q_{\max})_{c_0}
=
\frac{\gamma N}{\beta c_0^2(S^*)^2}
\left(S^*+c_0S^*_{c_0}\right)>0.
\]
\end{proof}

控制时长的四参数方向集中见推论~\ref{cor:s1:duration-sensitivity}。其中，
$c_0$ 的 $\Theta$ 判据只给出固定其余参数后给定参数点处的局部导数符号；
由于 $S^*,S_c,\bar S$ 均随 $c_0$ 变化，该判据不推出
$\Delta t$ 在整个可行 $c_0$ 区间上的全局单调性。
$q_{\max}$ 则刻画启动加强控制时需要达到的最大隔离率；
医疗容量阈值越高，$q_{\max}$ 越低，而常规接触水平越高，$q_{\max}$ 越高。

上一节已经给出总累计感染 $\Itcum$ 的三段解析公式。该指标中，$\eta$ 的提高会降低控制持续时间和初始控制强度，
但也意味着系统在更高感染水平下运行；$c_0$ 的提高会增强传播速度，同时又使控制更早启动。
因此总累计感染对 $c_0,\eta$ 的单调性同样不宜仅凭解析式作全局判断，后续应作为数值分析的重点指标。

\section{数值验证（$N=763$）}
\label{sec:num}
本节数值示例取
\[
N=763,\quad S_0=762,\quad I_0=1,\quad
\beta=0.155,\quad c_0=10,\quad q_0=0.01526,\quad \gamma=0.3504.
\]
常规控制下的再生数为:
\[
R_0=\frac{\beta c_0(1-q_0)}{\gamma}\approx 4.3560.
\]

医疗容量阈值仍设为总人数的 $5\%$，即
$\eta=0.05N=38.15$。
如表~\ref{tab:results}和图~\ref{fig:scenario1:single-sim}所示，理论计算与数值模拟吻合。数值模拟中使用的是由解析推导得到的时间域开环控制函数 $q_c(t)$；其中 $S(t)$ 为理论阈值控制期轨迹，并非数值积分中的实时反馈状态。受控情形下，在 $[t_1,t_2]$ 内感染人数沿医疗红线 $\eta$ 形成平台，最大偏差约为 $1.07\times 10^{-7}$；常规控制情形下感染峰值约为 $300.3726$，显著高于医疗容量阈值。这说明所构造的 $q_c(t)$ 能够实现精确压平曲线。
\begin{figure}[h]
    \centering
    \caption{阈值控制与常规控制情形的时间演化对比}
    \includegraphics[width=0.7\textwidth]{figures/optimal_control_with_quarantine_panels.pdf}
    \label{fig:scenario1:single-sim}
\end{figure}

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{情景一数值模拟中的参数、理论结果与数值验证}
  \label{tab:results}
  \renewcommand{\arraystretch}{1.18}
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{llcllc}
    \toprule
    \multicolumn{6}{c}{\textbf{模型参数}} \\
    \midrule
    $\gamma$ & 0.3504 & \quad & $R_0$ & 4.3560 & \\
    $\beta$  & 0.1550 & \quad & $c_0$ & 10.0000 & \\
    $N$      & 763.0000 & \quad & $q_0$ & 0.0153 & \\
    $\eta$   & 38.1500 & \quad & $S_c$ & 175.1602 & \\
    $S_0$    & 762.0000 & \quad & $I_0$ & 1.0000 & \\
    \midrule

    \multicolumn{6}{c}{\textbf{理论结果}} \\
    \midrule
    切入点 & \multicolumn{2}{l}{$S^*=708.3470$}
    & 启动时间 & \multicolumn{2}{l}{$t_1=3.1754$ 天} \\
    解除时间 & \multicolumn{2}{l}{$t_2=9.0780$ 天}
    & 控制时长 & \multicolumn{2}{l}{$\Delta t=5.9026$ 天} \\
    最大隔离率 & \multicolumn{2}{l}{$q_c(t_1)=0.7565$}
    & 终点隔离强度 & \multicolumn{2}{l}{$q_c(t_2)=q_0=0.0153$} \\
    \midrule

    \multicolumn{6}{c}{\textbf{数值峰值}} \\
    \midrule
    受控情形
    & \multicolumn{2}{l}{$\max I(t)=38.1500$}
    & 平台误差
    & \multicolumn{2}{l}{$\max_{[t_1,t_2]}|I-\eta|=1.07\times10^{-7}$} \\
    常规控制情形
    & \multicolumn{2}{l}{$\max I(t)=300.3726$}
    & 峰值时间
    & \multicolumn{2}{l}{$t=6.7065$ 天} \\
    \bottomrule
  \end{tabular}
\end{table}
在该基准下，图~\ref{fig:s1:inflection-diagnose} 给出隔离控制律的拐点诊断。$q_c(t)$ 偏离其首尾直线仅约 $4.4\%$（面板 (a)），因此拐点在 $q_c$ 曲线上几乎不可辨；但它在一阶导 $q_c'(t)$ 上表现为一个明显的极小值（面板 (b)，谷底比两端深约 $1.5$--$1.8$ 倍），在二阶导 $q_c''(t)$ 上表现为过零变号（面板 (c)）。这印证第~\ref{sec:s1:inflection-curvature}小节的结论：要展示拐点应画 $q_c'$，而非期望 $q_c$ 本身出现肉眼可见的弯曲。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.98\textwidth]{scenario1_inflection_diagnose.pdf}
    \caption{$N=763$ 基准下 $q_c(t)$ 的拐点三面板诊断：(a) $q_c$ 与首尾直线、拐点隔离强度 $\qinf$；(b) $q_c'$，拐点为其极小值；(c) $q_c''$，拐点处过零变号。竖点划线为拐点时刻 $t_{\mathrm{inf}}$。}
    \label{fig:s1:inflection-diagnose}
\end{figure}

为了进一步考察参数对情景一控制策略的影响，我们进行数值分析。与前述理论分析一致，本节重点考察两个政策参数 $c_0$ 与 $\eta$。第一组数值实验固定 $\beta,\gamma,q_0,N,S_0,I_0,\eta$，改变常规接触水平 $c_0$；第二组数值实验固定 $\beta,\gamma,c_0,q_0,N,S_0,I_0$，改变医疗容量阈值 $\eta$。对基准阈值 $\eta=0.05N$，由式~\eqref{eq:s1:c0-min-equality} 数值求得
\[
c_{0,\min}(0.05N)\approx3.3270.
\]
为了覆盖可行边界附近的上升支、极大值及其后的下降支，全局景观采用
\[
c_0=2.3,2.4,\ldots,14,
\qquad
\frac{\eta}{N}=0.2\%,0.22\%,\ldots,5\%,
\]
共 $118\times241=28438$ 个参数点。对每组参数，程序首先判断系统在常规防控背景下是否能够触及 $I=\eta$。若不可行，则不计算后续指标；相应点在一维曲线中不连接，在二维景观中留白。若可行，则输出切入点 $S^*$、常规防控临界阈值 $S_c$、启动时间 $t_1$、控制时长 $\Delta t$、启动时刻所需最大隔离率
\[
q_{\max}=q_c(t_1),
\]
清零时间 $t_{\rm end}$，以及综合成本 $J$ 和总累计感染 $\Itcum$。表~\ref{tab:scenario1_c0_summary} 固定 $\eta/N=5\%$，其中首个代表点取非网格值 $c_{0,\min}+0.02\approx3.3470$；表~\ref{tab:scenario1_eta_summary} 固定 $c_0=10$。图~\ref{fig:scenario1_u_time_c0} 固定 $\eta/N=5\%$，图~\ref{fig:scenario1_u_time_eta} 固定 $c_0=10$；图~\ref{fig:scenario1_summary_c0} 取 $\eta/N=0.2\%,0.6\%,1\%,2\%$，图~\ref{fig:scenario1_summary_eta} 则取 $c_0=5,8,10,12$ 并扫描完整阈值区间。相应结果见表~\ref{tab:scenario1_c0_summary}--\ref{tab:scenario1_eta_summary} 和图~\ref{fig:scenario1_u_time_c0}--\ref{fig:scenario1_heatmaps_c0_eta}。

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{固定 $\eta/N=5\%$ 时情景一中不同 $c_0$ 下的关键控制指标。第一行取 $c_0=c_{0,\min}+0.02\approx3.3470$，其余行为主文代表值。}
  \label{tab:scenario1_c0_summary}
  \input{../table/scenario1_c0_summary_table.tex}
\end{table}

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{固定 $c_0=10$ 时情景一中不同医疗容量阈值下的关键控制指标，其中 $\eta/N=0.2\%,0.6\%,1\%,2\%$。}
  \label{tab:scenario1_eta_summary}
  \input{../table/scenario1_eta_summary_table.tex}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/scenario1_u_time_c0.pdf}
    \caption{固定 $\eta/N=5\%$ 时，$c_0=4,5,8,10,12$ 下隔离率 $q_c(t)$ 的绝对时间轨迹。空心三角形和同色短竖线分别标出 $t_1$ 和 $t_2$；灰色虚线为 $q_0$，蓝色点线为 $\qinf$，暗红圆点为内部拐点。$c_0=4$ 时 $q_{\max}<\qinf$，故控制期内不存在内部拐点。}
    \label{fig:scenario1_u_time_c0}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/scenario1_u_time_eta.pdf}
    \caption{固定 $c_0=10$ 时，$\eta/N=0.2\%,0.6\%,1\%,2\%$ 下隔离率 $q_c(t)$ 的绝对时间轨迹。空心三角形和同色短竖线分别标出 $t_1$ 和 $t_2$；灰色虚线为 $q_0$，蓝色点线为 $\qinf$，暗红圆点为内部拐点。}
    \label{fig:scenario1_u_time_eta}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/c0_sensitivity_selected_eta.pdf}
    \caption{情景一中关键指标随 $c_0\in[2.3,14]$ 的变化。四条曲线对应 $\eta/N=0.2\%,0.6\%,1\%,2\%$，每条曲线只显示其可行部分；面板 (b) 的暗红圆点为各曲线在离散网格上的 $\Delta t$ 极大值。六个面板依次为 $t_1,\Delta t,t_{\rm end},q_{\max},J,\Itcum$。}
    \label{fig:scenario1_summary_c0}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/eta_sensitivity_selected_c0.pdf}
    \caption{情景一中 $c_0=5,8,10,12$ 时关键指标随 $\eta/N\in[0.2\%,5\%]$ 的变化，横轴采用对数尺度。六个面板依次为 $t_1,\Delta t,t_{\rm end},q_{\max},J,\Itcum$。}
    \label{fig:scenario1_summary_eta}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/scenario1_heatmaps_c0_eta.pdf}
    \caption{情景一参数景观：横轴为 $\eta/N\in[0.2\%,5\%]$，纵轴为 $c_0\in[2.3,14]$，四个面板依次为 $t_1$、$\Delta t$、$t_{\rm end}$ 与 $\Itcum$。白色区域表示常规防控轨道不能触及 $I=\eta$，黑色实线为解析可行边界 $c_{0,\min}(\eta)$，红色虚线为相应指标的等值线。}
    \label{fig:scenario1_heatmaps_c0_eta}
\end{figure}

%\clearpage

图~\ref{fig:scenario1_u_time_c0}--\ref{fig:scenario1_u_time_eta} 展示了不同参数组在绝对时间轴上启动、维持和解除加强隔离的全过程。图~\ref{fig:scenario1_u_time_c0} 中，$c_0=4$ 时 $q_{\max}=0.3413<\qinf\approx0.4083$，控制曲线始终位于拐点高度以下，因而没有内部拐点；$c_0=5,8,10,12$ 时则各有唯一内部拐点。图~\ref{fig:scenario1_u_time_eta} 中四条曲线的拐点均落在同一水平线 $\qinf$ 上，这是因为 $\qinf=1-1/[2(1-\beta)]$ 只由 $\beta$ 决定，而 $\eta$ 主要改变启动时刻与平台时间尺度。

从表~\ref{tab:scenario1_c0_summary} 和图~\ref{fig:scenario1_summary_c0} 可以看出，在固定 $\beta,\gamma,q_0,N,S_0,I_0,\eta$ 且位于各自可行区间时，随着常规接触水平 $c_0$ 增大，切入点 $S^*$ 上升、启动时间 $t_1$ 提前、所需最大隔离率 $q_{\max}$ 增大。这分别与引理~\ref{lem:s1:Sstar-sensitivity} 的 $S^*_{c_0}>0$ 以及命题~\ref{prop:s1:sensitivity} 的 $(t_1)_{c_0}<0$、$(q_{\max})_{c_0}>0$ 一致。控制时长 $\Delta t$ 则在完整可行区间上先升后降。以表~\ref{tab:scenario1_c0_summary} 的 $\eta/N=5\%$ 切片为例，
\[
\Delta t(3.3470)=2.0432,\qquad
\Delta t(5)=7.5778,\qquad
\Delta t(12)=5.3007\ {\rm d}.
\]
代表表中的 $c_0=5$ 已接近峰位，而完整 $0.1$ 步长网格的离散极大值出现在 $c_0=5.1$，对应 $\Delta t=7.5788$ 天。图~\ref{fig:scenario1_summary_c0} 中 $\eta/N=0.2\%,0.6\%,1\%,2\%$ 四条曲线的离散峰位分别约为 $c_0=4.3,4.4,4.4,4.6$，相应最大控制时长约为 $211.5435,69.8417,41.5169,20.2813$ 天。这些结果同时显示式~\eqref{eq:s1:c0-min-limit} 给出的左端趋势和推论~\ref{cor:s1:duration-sensitivity} 中导数符号可能改变的两个分支。清零时间 $t_{\rm end}$、成本 $J$ 和总累计感染 $\Itcum$ 还受到控制后衰减阶段和累计流量的影响，不能由 $\Delta t$ 的变化方向推出其全局单调性。

表~\ref{tab:scenario1_eta_summary} 给出固定 $c_0=10$ 的四个代表阈值，图~\ref{fig:scenario1_summary_eta} 则把比较扩展到 $c_0=5,8,10,12$ 和完整的 $\eta/N\in[0.2\%,5\%]$ 区间。对每个固定 $c_0$，随着医疗容量阈值提高，系统更晚触碰红线，故 $t_1$ 增大；触碰时易感者消耗更多，故 $S^*$ 下降；同时 $\Delta t$ 与 $q_{\max}$ 下降。这分别与引理~\ref{lem:s1:Sstar-sensitivity} 的 $S^*_\eta<0$、命题~\ref{prop:s1:sensitivity} 的 $(t_1)_\eta>0$、$(q_{\max})_\eta<0$ 以及推论~\ref{cor:s1:duration-sensitivity} 的 $(\Delta t)_\eta<0$ 一致。固定 $c_0=10$，当 $\eta/N$ 从 $0.2\%$ 增至 $2\%$ 时，
\[
\Delta t:152.0574\to15.0431,\qquad
t_{\rm end}:180.5821\to46.8029,
\]
\[
J:60.8005\to5.8888,\qquad
\Itcum:173.0314\to233.7810.
\]
在当前参数区间内，提高阈值显著缩短平台和清零时间、降低成本，但总累计感染随平台高度上升而增加；这一方向来自平台高度、平台时长和后期衰减的共同作用，不作为对任意参数区域的全局结论。对每个固定 $c_0$，$q_{\max}$ 随 $\eta$ 的变化相对缓慢，而图中不同曲线之间的主要差异来自 $c_0$。在所选的 $c_0=5,8,10,12$ 上，$\Delta t$ 在完整阈值范围内始终按 $5>8>10>12$ 排列，没有发生曲线交叉。

最后，图~\ref{fig:scenario1_heatmaps_c0_eta} 将上述一维切片扩展到 $(\eta/N,c_0)$ 平面。黑色实线给出解析边界 $c_{0,\min}(\eta)$；其下方白色区域表示常规防控轨道不能触及 $I=\eta$，因而无需启动加强隔离控制。该边界随 $\eta$ 增大而上移，说明医疗容量较高或常规传播强度较低时，参数点可能进入阈值不可达区。图中的有色区域只报告可行情形下的 $t_1$、$\Delta t$、$t_{\rm end}$ 和 $\Itcum$，红色虚线用于读取相应指标的等值水平。





\section{西安真实疫情应用}
\label{sec:xian}

本章基于 He、Tang 和 Xiao~\cite{He2023TDINN} 给出的西安疫情 TDINN 控制函数和真实日报数据，把第~\ref{sec:s1}节推导的情景一阈值控制实例化到西安参数，并与 TDINN 控制、常规控制进行比较。TDINN 控制更接近短期严格控制，即同时降低接触率并提高隔离率使疫情尽快下降；情景一阈值控制则是理论对照方案：在不降低接触率的前提下，只通过提高隔离率使社区感染者 $I(t)$ 不超过医疗容量阈值 $\eta$。该比较用于说明医疗容量、控制强度和累计感染规模之间的取舍，并为第~\ref{sec:dominance}节的占优分析提供 TDINN 基准。沿用统一记号：社区感染者 $I(t)$、新增社区感染 $I_{\rm new}(t)$、新增隔离感染 $I_{q_{\rm new}}(t)$、累计社区感染 $I_{\rm cum}(t)$、累计隔离感染 $I_{q_{\rm cum}}(t)$，以及总累计感染 $\Itcum(t)=I_{\rm cum}(t)+I_{q_{\rm cum}}(t)$。

\subsection{真实数据与 TDINN 控制函数}
真实病例数据取自 \texttt{真实数据/Xianguankong.xlsx}，包含日期、社区发现病例数 $I_{\rm new}(t)$ 和隔离发现病例数 $I_{q_{\rm new}}(t)$，覆盖 2021 年 12 月 9 日至 2022 年 1 月 17 日共 40 天，其中社区发现 583 例、隔离发现 1467 例，合计 2050 例（图~\ref{fig:xian_observed_daily}）。TDINN 控制曲线取自文献~\cite{He2023TDINN} Table 3 中西安的拟合结果 $c_2(t),q_2(t)$：
\begin{equation}
c_{\rm TDINN}(t)=(12.8872-3.4625)e^{-(0.0463t)^2}+3.4625,\qquad
q_{\rm TDINN}(t)=(0.3230-0.9844)e^{-(0.0452t)^2}+0.9844,
\end{equation}
因此常规初始控制水平为 $c_0=12.8872,\ q_0=0.3230$。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\textwidth]{xian_observed_daily_cases.pdf}
    \caption{西安真实疫情日报数据。$I_{\rm new}(t)$ 为每日新增社区感染者，$I_{q_{\rm new}}(t)$ 为每日新增隔离感染者。}
    \label{fig:xian_observed_daily}
\end{figure}

\subsection{模型与初值拟合}
数值模拟使用第~\ref{sec:model}节的
包含接触追踪与隔离的扩展 SIR 型模型
（下文简称 SIQR 型模型），并额外引入累计仓室
$I_{\rm cum}(t),I_{q_{\rm cum}}(t)$ 记录社区与隔离累计感染：
\begin{equation}
\dot I_{\rm cum}(t)=\frac{\beta c(t)(1-q(t))S(t)I(t)}{N},\qquad
\dot I_{q_{\rm cum}}(t)=\frac{\beta c(t)q(t)S(t)I(t)}{N}.
\end{equation}
总人口取西安市 2021 年末常住人口 $N=13{,}163{,}000$；传播参数固定为文献~\cite{He2023TDINN} Table 1 的估计值 $\beta=0.1498,\ \gamma=0.2953,\ \delta_q=0.3531$。由于原文未给出拟合初值，本文设 $R(0)=0$，从而 $N=S_0+I_0$，只用最小二乘估计 $I_0$ 并令 $S_0=N-I_0$；拟合准则为每日新增 $I_{\rm new},I_{q_{\rm new}}$ 与累计 $I_{\rm cum},I_{q_{\rm cum}}$ 四项均方误差之和。拟合结果见表~\ref{tab:xian_initial_fit}。

表中 $I_0$ 是模型初始感染者数，不应直接理解为现实整数病例：观测起点未必是真实传播起点，且早期增长较快，最小二乘会给出很小的 $I_0$，应解释为给定时间原点和控制函数下的模型校准值。作为对照，若强行取 $I_0=1$，模型预测 40 天累计病例约 $1.08\times10^6$，远高于真实数据（表~\ref{tab:xian_i0_sensitivity}）。

\begin{table}[htbp]
    \centering
    \caption{固定参数后的初始条件估计。}
    \label{tab:xian_initial_fit}
    \input{../table/xian_initial_fit_table.tex}
\end{table}

\begin{table}[htbp]
    \centering
    \caption{$I_0=1$ 对照设定下的初值敏感性说明。}
    \label{tab:xian_i0_sensitivity}
    {\input{../table/xian_i0_sensitivity_table.tex}}
\end{table}

\subsection{情景一阈值控制的西安实例}
阈值 $\eta$ 应尽量反映现实医疗容量。西安市 2021 年末常住人口约 1316.30 万~\cite{XianStat2021}；《2022 年陕西省卫生健康统计年鉴》给出 2021 年西安市卫生机构床位 79426 张~\cite{ShaanxiHealth2022}，约为 $0.006N$。考虑疫情期间不可能将全部床位用于收治，按约三分之一可调配估算，取代表性阈值
\begin{equation}
\eta=0.002N=26{,}326.
\end{equation}
按第~\ref{sec:s1}节，常规控制下临界易感人数 $S_c=\gamma N/[\beta c_0(1-q_0)]=2{,}974{,}125.41$；数值求得触碰红线时刻 $t_1=16.90$、启动点 $S^*=13{,}020{,}440.75$。平台期由理论开环控制 $q_c(t)=1-\gamma N/[\beta c_0 S(t)]$（其中 $S(t)=\bar S+(S^*-\bar S)e^{-c_0\eta(t-t_1)/N}$，$\bar S=\gamma N(1-\beta)/(\beta c_0)$）维持 $I(t)\equiv\eta$，解除时刻
\[
t_2=t_1+\frac{N}{c_0\eta}\ln\frac{S^*-\bar S}{S_c-\bar S}\approx101.97,
\]
控制时长约 $85.07$ 天；启动隔离率 $q_c(t_1)=0.8454$，终点 $q_c(t_2)=q_0=0.3230$。数值积分中平台段最大偏差约 $2.89\times10^{-8}$，相对 $\eta$ 可忽略，说明理论控制函数能把 $I(t)$ 精确维持在阈值线上。

\subsection{比较指标与三策略数值结果}
比较 TDINN 控制、情景一阈值控制和常规控制，每类策略模拟到各自清零时间（$I(t)$ 超过 $1$ 后再次降到 $\le 1$）。控制成本按实际实施时间积分：TDINN 从疫情开始到清零，情景一只计平台段 $[t_1,t_2]$，常规控制无额外成本。沿用第~\ref{sec:cost}节的成本口径，记归一化分项 $J_c,J_q$ 与二次加权总成本 $J$（$w_c=1,\ w_q=2$）。

图~\ref{fig:xian_panels} 给出 $\eta=0.002N$ 时三策略的动态比较（$I(t)$、每日新增与真实日报、$c(t)$ 与 $q(t)$）；图~\ref{fig:xian_cumulative} 给出社区、隔离和总累计感染；图~\ref{fig:xian_Re} 给出有效再生数 $R_e(t)=\beta c(t)(1-q(t))S(t)/(\gamma N)$。常规控制 $R_e(0)\approx4.43$，感染快速增长；TDINN 通过降低 $c(t)$ 并提高 $q(t)$ 使 $R_e$ 较快降到 $1$ 以下；情景一在平台期使 $R_e\approx1$，平台后 $S(t)$ 降到临界水平附近，$R_e<1$。主要指标见表~\ref{tab:xian_summary}。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{xian_control_comparison_panels.pdf}
    \caption{$\eta=0.002N$ 时 TDINN 控制、情景一阈值控制与常规控制的动态比较：$(a)$ 社区感染者 $I(t)$，$(b)$ 每日新增社区感染者 $I_{\rm new}(t)$，$(c)$ 每日新增隔离感染者 $I_{q_{\rm new}}(t)$，$(d)$ 接触率 $c(t)$，$(e)$ 隔离率 $q(t)$。$(a)$--$(c)$ 主面板采用对数纵轴，内嵌小图采用线性纵轴显示低量级区间；真实数据点只放在 $(b)$、$(c)$ 的内嵌小图中。浅灰点线标记观测结束日，弱化的深红虚线标记阈值控制的启动与解除时刻。}
    \label{fig:xian_panels}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{xian_control_cumulative.pdf}
    \caption{三策略下累计感染规模的比较：$(a)$ 社区累计感染 $I_{\rm cum}(t)$，$(b)$ 隔离累计感染 $I_{q_{\rm cum}}(t)$，$(c)$ 总累计感染 $\Itcum(t)$。三个面板采用适合零起点数据的对称对数纵轴。}
    \label{fig:xian_cumulative}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{xian_effective_reproduction_number.pdf}
    \caption{三策略下有效再生数 $R_e(t)$ 的比较。深红虚线标记临界水平 $R_e=1$。}
    \label{fig:xian_Re}
\end{figure}

\begin{table}[htbp]
    \centering
    \footnotesize
    \caption{$\eta=0.002N$ 下三类控制策略的数值比较。（表内 $\max I$、$\Itcum$ 为直接模拟取整值。）}
    \label{tab:xian_summary}
    \resizebox{0.7\textwidth}{!}{\input{../table/xian_control_comparison_results_table.tex}}
\end{table}

在 $\eta=0.002N$、全市人口口径下，情景一阈值控制把社区感染者峰值精确限制在 $\eta=26{,}326$，且完全不降低接触率（$J_c=0$），但需维持约 $85.07$ 天额外隔离，清零约 $258.11$ 天，总累计感染显著高于 TDINN；TDINN 的峰值（约 $152$）和总累计感染（约 $2097$）都低得多，但同时使用了接触率和隔离率两种控制。值得注意的是，此时情景一的二次加权成本 $J\approx40.77$ 反而低于 TDINN 的 $J\approx49.35$，而峰值与累计却更高——说明二者的优劣是指标相对的。TDINN 的全市固定拟合／重构参照值 $I_{\rm peak}^{\rm T}\approx151.90$、$J^{\rm T}\approx49.35$、$\Itcum^{\rm T}\approx2096.76$（表~\ref{tab:xian_summary} 报直接模拟取整 $152/2097$）将作为第~\ref{sec:dominance}节占优分析的 TDINN 基准。

情景一阈值控制成本占优：情景一完全不动接触率（$J_c=0$），其成本全部来自平台期的隔离加强，而 TDINN 同时动用接触率与隔离率两个通道，二次加权（$w_q=2$）下隔离项占比更高。因此在 $\eta=0.002N$ 这一代表阈值上，情景一以“更高的峰值与累计、更长的时程”换来“更低的加权控制成本”——优劣与所选指标集相关的，不能据 $J$ 一项判定综合占优。

图~\ref{fig:xian_cumulative} 进一步显示三者累计规模的悬殊：情景一到清零的总累计感染达 $\sim2.38\times10^{6}$，而 TDINN 仅 $\sim2.1\times10^{3}$，相差三个数量级。这一差距并非控制效率的直接体现，而主要来自全市 $N=13\,163\,000$ 的易感池——平台期贴边维持使 $I(t)\equiv\eta$ 长期不衰，累计新发正比于所消耗的易感者，池越大、平台越长，累计越高。此点在第~\ref{sec:dominance}节以 $\Itcum=N\,h(\theta)$ 的标度结构精确刻画。

图~\ref{fig:xian_Re} 把三策略压制传播的方式差异摊开：常规控制 $R_e(0)\approx4.43$ 一路失控；TDINN 同时压 $c(t)$、抬 $q(t)$，使 $R_e$ 较快跌破 $1$；情景一在平台期把 $R_e$ 精确顶在 $\approx1$。须强调平台期 $R_e\approx1$ 是策略定义使然（“贴边维持”即令新发与移出相抵）.情景一真正的代价出现在平台之后 $S(t)$ 缓慢降到临界水平、$R_e$ 才最终 $<1$ 的那段长尾里。

\subsection{阈值 \texorpdfstring{$\eta$}{eta} 的敏感性}
取 $\eta/N\in\{0.0001,0.0002,0.0005,0.001,0.0015,0.002,0.003,0.004,0.005,0.0075,0.01\}$ 扫描，结果见表~\ref{tab:xian_eta_sens} 与图~\ref{fig:xian_eta_sens}，可概括为
\[
\eta\downarrow\ \Rightarrow\ t_1\downarrow,\qquad \Delta t\uparrow,\qquad J\uparrow,\qquad t_{\rm end}\uparrow.
\]
启动时间变化不大：$\eta/N$ 从 $0.01$ 降到 $0.0001$，$t_1$ 仅从第 $18.55$ 天提前到第 $13.93$ 天。但控制时长与清零时间对 $\eta$ 很敏感：$\eta/N$ 从 $0.01$ 降到 $0.002$，控制时长从 $16.61$ 增到 $85.07$ 天；降到 $0.0001$ 时增到 $1710.66$ 天，清零 $2209.55$ 天。原因是平台期让 $I(t)$ 贴边维持而非立即下降，$\eta$ 越低易感者消耗越慢。显然提高 $\eta$是缩短平台、使情景一在给定指标上接近 TDINN 的一种方式（见第~\ref{sec:dominance}节）。反过来$\eta$ 越低、平台越长且发散。若要把社区峰值压到西安真实疫情的量级（$\approx152$），所需的平台时长将不再是数十天，而是若干年——这一定量结果与其对易感池规模 $N$ 的依赖，正是下一节在 $(\eta,c_0)$ 图谱上要精确定位的内容。

\begin{table}[htbp]
    \centering
    \footnotesize
    \caption{不同医疗阈值 $\eta$ 下情景一阈值控制的数值指标。}
    \label{tab:xian_eta_sens}
    \resizebox{0.7\textwidth}{!}{\input{../table/xian_eta_sensitivity_table.tex}}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{xian_eta_sensitivity.pdf}
    \caption{不同相对医疗阈值 $\eta/N$ 下情景一阈值控制的敏感性：$(a)$ 启动隔离率 $q_c(t_1)$，$(b)$ 二次加权总成本 $J$，$(c)$ 总累计感染 $\Itcum$，$(d)$ 控制时长 $\Delta t$ 与清零时间 $t_{\rm end}$。横轴采用对数尺度，$(b)$--$(d)$ 的纵轴采用对数尺度；水平参照线给出 TDINN 控制的对应固定指标。}
    \label{fig:xian_eta_sens}
\end{figure}

\FloatBarrier
\subsection{接触率 \texorpdfstring{$c_0$}{c0} 与阈值 \texorpdfstring{$\eta$}{eta} 的二维图谱}
图~\ref{fig:xian_heatmaps} 在 $(\eta,c_0)$ 平面上给出情景一四个关键指标的二维图谱，横轴为绝对
医疗阈值 $\eta$（对数）。两条竖直参照线标出本节关心的两个操作点：右线 $\eta=0.002N=26326$
是我们在前几小节采用的代表阈值，左线 $\eta=I_{\rm peak}^{\rm T}=151.90$ 是西安真实疫情的社区峰值。
二者在 $\Delta t$ 面板（图~\ref{fig:xian_heatmaps}a）上的落差，是理解本章的关键。


在西安全市口径（$N=13\,163\,000$，$c_0=12.8872$）下，由控制时长闭式
\[
\Delta t=\frac{N}{c_0\eta}\ln\frac{S^{*}-\bar S}{S_c-\bar S}
\]
可直接读出两个操作点的代价。取 $\eta=0.002N=26326$ 时 $\Delta t\approx85$~d；而若把峰值压到真实
水平 $\eta=151.90$，则 $S^{*}\approx1.3162\times10^{7}$、对数因子 $\approx2.205$，得
\[
\Delta t(\eta=151.90)\approx1.48\times10^{4}\ \text{d}\approx 40.6\ \text{年}.
\]
也就是说，在全市易感池里“精确压平到真实峰”并非缩短疫情，而是把一个持续四十年的平台强加于系统。
$\Delta t\propto N/(c_0\eta)$ 使然：峰压得越低（$\eta$ 越小）、池子越大（$N$ 越大），平台越长，二者由同一
比例 $\theta=\eta/N$ 绑死、无法解耦。这解释了为何本章及第~\ref{sec:dominance}节的实例只能退而取虚高的
$\eta=0.002N$（比真实峰高约 $170$ 倍）：并非该阈值对应任何现实医疗红线，而是全市口径下才能让平台
回到可接受时程的选择。相应地，第~\ref{sec:dominance}节命题中“峰值 $\eta<I_{\rm peak}^{\rm T}$”一项应读作
设计约束，而非情景一相对 TDINN 的效果优势。


上述悖论的根源在易感池的选取。在 $N=13\,163\,000$、而全程总感染仅约 2050 的拟合里，$S(t)\approx N$
几乎不动——易感者耗竭这一机制根本没有启动，整条疫情曲线由时变控制 $c(t),q(t)$ 产生，SIR 结构
近乎惰性。作为“若不封控、全城暴露会如何”的反事实，全市 $N$ 给出的常规控制峰值（$\sim1.38\times10^{6}$）
与累计（$\sim4.6\times10^{6}$）是有意义的；但对情景一阈值控制而言它是错的池子，因为该策略的作用
机制恰恰是易感耗竭，而这一机制在全市池里要数十年才能走完。换言之，让常规控制灾难化的池，正是让
阈值控制失效的池。这促使我们在第~\ref{sec:dominance}节把有效混合人口 $N_{\rm eff}$ 作为一条独立坐标，
分辨“反事实常规控制作用的大池”与“阈值控制真正能生效的小池”，从而解开本节的悖论。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{xian_heatmaps.pdf}
    \caption{$(\eta,c_0)$ 平面上情景一阈值控制四个关键指标的二维图谱，横轴为绝对医疗阈值 $\eta$（对数）：$(a)$ 控制时长 $\Delta t$，$(b)$ 清零时间 $t_{\rm end}$，$(c)$ 总累计感染 $\Itcum$，$(d)$ 二次加权总成本 $J$。各面板采用与图~\ref{fig:scenario1_heatmaps_c0_eta} 一致的 \texttt{parula} 色图；时间指标与成本采用对数颜色归一化，总累计感染采用线性颜色归一化，等值线标注各指标水平。两条竖直参照线标出两个操作点：$\eta=I_{\rm peak}^{\rm T}=151.90$（西安真实社区峰值）与 $\eta=0.002N=26326$（代表阈值）；$(a)$ 面板上二者的 $\Delta t$ 落差量化了正文所述“在全市池里压到真实峰需约 40 年平台”的悖论。}
    \label{fig:xian_heatmaps}
\end{figure}

综合本章：情景一阈值控制并非无条件优于 TDINN 控制，而是在给定医疗容量 $\eta$ 下用隔离率守住社区感染者阈值；其相对 TDINN 的优劣取决于所选指标集以及比例 $\eta/N$。这一条件性将在第~\ref{sec:dominance}节精确刻画。


\section{尺度不变性与有效人口占优}
\label{sec:dominance}

西安疫情的真实易感规模 $N_{\rm eff}$ 是未知的。
第~\ref{sec:xian}~节沿用文献~\cite{He2023TDINN} 取全市常住人口
$1.3163\times10^7$，是数据所限下的代用上界——
一次局部暴发中实际参与混合的人群必然远小于全市人口。
本节因此把 $N_{\rm eff}$ 当作\emph{待定参数}，回答：
它取何值时，情景一阈值控制会不劣于实际发生的干预？

两个端点的性质不同，须分开陈述：
数据与模型恒等式把 $N_{\rm eff}$ \textbf{部分识别}为
$[N_{\rm floor},+\infty)$（式~\eqref{eq:dom:Nfloor}）；
性能条件 (i)(ii) 另给出使阈值控制不劣的上界 $N^\ast$。
二者的交集 $[N_{\rm floor},N^\ast]$ 非空，即为本节主结论——
\textbf{若真实 $N_{\rm eff}$ 落于其中，则阈值控制不劣于实际干预}。
相应地，本节的比较不是把某一策略“搬到”不同规模的人群上，
而是对同一次疫情的未知规模参数作条件判断；
条件 (i) 中 $\eta$ 与 $I_{\rm peak}^{\rm T}$ 均为该次疫情的绝对人数，
本就处于同一量纲，直接比较。

在此定位下，本节刻画情景一阈值控制在一组给定比较指标上
不劣于 TDINN (全市固定拟合参照)的参数区域。
下述命题采用固定归一化初值
$S_0/N$ 与 $I_0/N$ 的理论口径。
在成本／时长约束给出的比例下界低于常规控制峰值分数的
非退化情形下，存在临界有效人口 $N^\ast(T_{\max})$，
且占优区域非空当且仅当 $N<N^\ast(T_{\max})$；
若该比例下界不低于常规控制峰值分数，
则占优区域对所有 $N$ 均为空。
临界人口由传播参数、常规控制水平、归一化初值口径、
成本权重、允许控制时长 $T_{\max}$
与 TDINN(全市固定拟合参照)共同决定。

定义:
\begin{equation}
  \theta:=\frac{\eta}{N}\quad(\text{阈值比例}),\qquad
  i_{\max}^{no}:=\frac{I_{\max}^{no}}{N}\quad(\text{常规控制峰值分数}),
  \label{eq:dom:ratios}
\end{equation}
其中 $I_{\max}^{no}$ 由式~\eqref{eq:s1:Imax-no} 给出。
在固定归一化初值
$s_0=S_0/N$ 与 $i_0=I_0/N$
的条件下，
$S_c,\bar S,I_{\max}^{no}$
均随 $N$ 同比例缩放，
故
$i_{\max}^{no}=I_{\max}^{no}/N$
不依赖 $N$；
它依赖于
$\beta,\gamma,c_0,q_0,s_0,i_0$。
结论是条件性的，且强依赖所选指标集（见推论~\ref{cor:cum}）。

引理~\ref{lem:dom:scaling}
及由其导出的直线边界，
采用固定归一化初值
$(s_0,i_0)$
的理论口径；
在该口径下，
$\Delta t,J,q_{\max}$
等强度量对 $(N,\eta)$ 的依赖精确地只通过
$\theta=\eta/N$ 实现。

本节全程采用固定\emph{绝对}初值 $I_0=1.00663\times10^{-3}$
（第~\ref{sec:xian}~节的全市标定值）的单一口径，跨 $N_{\rm eff}$ 不重新拟合，
故 $i_0=I_0/N$ 随 $N$ 变化。取绝对而非归一化初值，是因为本节把 $N_{\rm eff}$
当作\emph{同一次疫情}的未知池规模：两条臂描述的是同一次播种，理应共享同一个
绝对初值。若改固定 $i_0$，小池会分到荒谬的绝对种子（$N=10^4$ 时 $7.6\times10^{-7}$ 人），
其绝对时刻也无法与画在全市尺度的 TDINN 参照对齐。

该口径的代价可以精确写出。由式~\eqref{eq:model:first-integral} 的首次积分，
$i_0$ 与 $\theta$ 只以 $(\theta-i_0)$ 的组合出现，故
$\Delta t,J,q_{\max},s^\ast$ 偏离纯 $\theta$ 依赖的相对量级恰为 $i_0/\theta$。
本节 $N\ge N_{\rm floor}=1.06\times10^{4}$，即 $i_0\le9.5\times10^{-8}$，
而 $\theta\ge\theta_{\rm dur}(150\mathrm d)=1.14\times10^{-3}$，
故该偏离不超过 $8\times10^{-5}$；数值上实测跨 $N$ 相对极差为
$\Delta t$ 的 $2.4\times10^{-7}$、$J$ 的 $5.9\times10^{-7}$。
因此引理~\ref{lem:dom:scaling} 在本口径下以该精度成立。
唯一真正随 $N$ 变的是启动时间 $t_1\approx\frac{1}{r}\ln\frac{\theta}{i_0}$，
这是该口径的定义性质而非退化：池越小、同一批种子占的比例越大、触发越早。

在固定归一化初值及其余参数不变时，
$\Delta t,J,q_{\max}$
等强度量只经
$\theta=\eta/N$
进入。
因此，对这些强度量而言，
提高医疗容量阈值 $\eta$
与降低有效混合人口 $N$
都通过增大 $\theta$ 起作用。

但二者对完整占优区域并非完全等价：
提高 $\eta$ 会同时抬高平台的绝对峰值，
并逼近固定上界
$I_{\rm peak}^{\rm T}$；
降低 $N$ 则不改变给定平台的绝对高度 $\eta$。
此外，总累计感染与清零时刻还带有绝对尺度依赖。
故本文只把提高 $\eta$ 和降低 $N$
称为作用于强度量的两条同向杠杆，
不称其为完整占优意义下的无条件等价杠杆。

TDINN 基准（全市固定拟合参照）:
TDINN 曲线是对西安已发生疫情过程及其干预强度的
模型拟合与反演。
其时间函数
$c_{\rm TDINN}(t),q_{\rm TDINN}(t)$
与初值 $I_0$
在西安全市人口口径下由同一份真实日报数据校准，
并由拟合／重构轨迹计算得到
社区峰值
$I_{\rm peak}^{\rm T}=151.90$、
二次加权成本
$J^{\rm T}=49.35$
（$w_c=1,w_q=2$）、
总累计感染
$\Itcum^{\rm T}=2096.76$
与清零时刻
$t_{\rm end}^{\rm T}=45.27\,\mathrm d$。

这四个量是一组基于 TDINN 拟合／重构结果得到的
模型参照量，而不是四个直接观测量；
其中 $J^{\rm T}$ 进一步由拟合得到的
$c_{\rm TDINN},q_{\rm TDINN}$
积分计算。
本文直接使用的观测量是每日新增社区病例与隔离病例，
见第~\ref{sec:xian}~节。

这四个量的性质并不相同，这一区别在第~\ref{sec:dom:limits}~节 (2) 展开：
$I_{\rm peak}^{\rm T},\Itcum^{\rm T},t_{\rm end}^{\rm T}$
是对已发生疫情的重构，其值由每日新增社区与隔离病例这同一份数据锁定；
而 $J^{\rm T}$ 由拟合得到的
$c_{\rm TDINN},q_{\rm TDINN}$ 积分而来。

本节将上述四个数固定为
西安全市口径下的 TDINN 固定拟合参照，
不随 $N_{\rm eff}$ 重算。
相对地，情景一阈值控制与常规控制是反事实策略，
在各自 $N_{\rm eff}$ 上求解；
其归一化初值 $(s_0,i_0)$ 跨 $N_{\rm eff}$ 固定，不重新拟合。
因此，本节回答的正是开头所述的问题：
未知的 $N_{\rm eff}$ 取何值时，
情景一阈值控制的指标不劣于该次疫情实际达成的水平。

\subsection{标度不变性}

\begin{lemma}[对 $N$ 的标度不变性]\label{lem:dom:scaling}
固定归一化初值 $S_0/N,\,I_0/N$。则情景一阈值控制的平台控制时长 $\Delta t$、二次加权成本 $J$、
最大隔离率 $q_{\max}$ 以及峰值分数 $\eta/N$ 对 $(N,\eta)$ 的依赖，均只通过 $\theta=\eta/N$ 实现；
特别地 $s^*:=S^\ast/N$ 只依赖 $\theta$。
\end{lemma}
\begin{proof}
记 $s_c:=S_c/N=\gamma/[\beta c_0(1-q_0)]$、$\bar s:=\bar S/N=\gamma(1-\beta)/(\beta c_0)$，
二者不含 $N$。将式~\eqref{eq:s1:Sstar-lambert} 的 Lambert 幅角改写：由
$\rho_1=\gamma N/\{c_0[\beta+q_0(1-\beta)]\}$、
$\frac{\beta_2^0/\beta_1^0}{\rho_1}=1/S_c$ 与
$C_0=I_0+\tfrac{\beta_2^0}{\beta_1^0}S_0-\rho_1\ln S_0$，
\[
  -\frac{C_0-\eta}{\rho_1}
  = \ln S_0-\frac{S_0}{S_c}-\frac{I_0-\eta}{\rho_1}
  = \ln N+\left[
  \ln\frac{S_0}{N}-\frac{S_0/N}{s_c}
  -\frac{c_0[\beta+q_0(1-\beta)]}{\gamma}
  \left(\frac{I_0}{N}-\theta\right)
  \right].
\]
于是幅角可写为
\[
-\frac{1}{s_c}
\exp\left[
\ln\frac{S_0}{N}-\frac{S_0/N}{s_c}
-\frac{c_0[\beta+q_0(1-\beta)]}{\gamma}
\left(\frac{I_0}{N}-\theta\right)
\right],
\]
其中 $e^{\ln N}$ 已与 $S_c=Ns_c$ 中的 $N$ 抵消。该式不含 $N$，
仅依赖 $\theta$ 与归一化初值，故
$s^*=-s_c W_{-1}(\cdot)$ 也只依赖 $\theta$。将其代入
$\Delta t=\frac{N}{c_0\eta}\ln\frac{S^\ast-\bar S}{S_c-\bar S}
 =\frac{1}{c_0\theta}\ln\frac{s^*-\bar s}{s_c-\bar s}$、
$q_{\max}=1-\frac{\gamma N}{\beta c_0 S^\ast}=1-\frac{\gamma}{\beta c_0\,s^*}$，
以及（由 $\dot S=-\frac{c_0\eta}{N}(S-\bar S)$、$dt=-\frac{N}{c_0\eta}\frac{dS}{S-\bar S}$）
\begin{equation}
  J=\frac{2N}{c_0\eta(1-q_0)^2}\int_{S_c}^{S^\ast}\frac{\big(q_c(S)-q_0\big)^2}{S-\bar S}\,dS
   =\frac{2}{c_0\theta(1-q_0)^2}\int_{s_c}^{s^*}\frac{\big(q_c(N s)-q_0\big)^2}{s-\bar s}\,ds,
  \label{eq:dom:Jtheta}
\end{equation}
其中 $q_c(N s)=1-\gamma/(\beta c_0 s)$ 不含 $N$。四者右端均只含 $\theta$。
\end{proof}

\begin{remark}
式~\eqref{eq:dom:Jtheta} 的推导不使用 $S_0\approx N$，只需初值以归一化形式固定。
本文实际采用的是固定\emph{绝对}初值口径（第~\ref{sec:dominance}~节开头），
其 $i_0=I_0/N$ 随 $N$ 变，故引理的假设不严格成立；但由首次积分，$i_0$ 与 $\theta$
只以 $(\theta-i_0)$ 出现，偏离量级为 $i_0/\theta$，在本节范围内不超过 $8\times10^{-5}$
（实测跨 $N$ 相对极差 $\lesssim6\times10^{-7}$），故引理的结论以该精度成立。
$\theta_{\rm cost}$ 与 $N^\ast$ 更不敏感：$i_0$ 在 $10^{-11}$ 与 $10^{-7}$ 之间变化时，
二者的相对变化在七位有效数字内不可见。真正随 $N$ 变的只有启动时间 $t_1$，
以及经 $t_1$ 进入的清零时刻 $t_{\rm end}$，见第~\ref{sec:dom:limits}~节 (5)。
\end{remark}

\subsection{有效人口标度与结构不变量的数值验证}
\label{sec:scaling}

引理~\ref{lem:dom:scaling} 的标度不变性可由西安 SIQR 模型的数值实验直接验证。固定传播参数 $\beta,\gamma,\delta_q$ 与 TDINN 控制函数，把人口规模解释为有效混合人口 $N_{\rm eff}$，在两组口径下考察阈值控制指标随 $(N_{\rm eff},\eta)$ 的变化。本节记归一化状态 $s=S/N$、$i=I/N$（时间仍以天计，故为人口归一化而非无量纲）。

\textbf{同比例阈值（固定 $\theta$）.} 固定绝对初值 $I_0$ 与 $\theta=\eta/N_{\rm eff}=0.002$，令 $N_{\rm eff}\in\{5\times10^4,\dots,1.316\times10^7\}$。六个尺度下 $\Delta t\approx85.07$、隔离率轨迹 $q_c(t)$ 与平台结束时的累计感染分数完全重合，与引理~\ref{lem:dom:scaling} 一致：$\Delta t$ 跨 $N_{\rm eff}$ 的相对极差为 $2.4\times10^{-7}$。由于本节固定的是绝对初值，$i_0=I_0/N$ 随 $N$ 变，启动时间 $t_1=(1/r)\ln(\theta/i_0)$ 因而由 $N_{\rm eff}=5\times10^{4}$ 处的 $11.39$ 天升到全市的 $16.90$ 天。折叠须相应地在平移时间 $t-t_1$ 上考察（图~\ref{fig:neff_collapse}）：这正是恰当的比较基准，因为引理管的是 $\Delta t,J,q_{\max},s^\ast$，本就不含绝对时刻。在 $t-t_1$ 上的折叠误差：$i$ 约 $3\times10^{-10}$，$q$ 约 $9\times10^{-6}$（后者受轨迹按日采样的线性插值限制，非口径所致）。

\textbf{固定绝对阈值（改变 $\theta$）.}
固定 $\eta=100$，把 $N_{\rm eff}$ 从 $1.316\times10^7$ 降到
$5\times10^4$，则 $\theta=\eta/N_{\rm eff}$ 增大，平台控制时长由
$\Delta t\approx2.25\times10^4$ 天缩短到约 $85$ 天
（图~\ref{fig:neff_time}）。
这是降低 $N_{\rm eff}$ 通过增大
$\theta=\eta/N_{\rm eff}$
缩短平台期的数值体现。
对
$\Delta t,J,q_{\max}$
等强度量而言，
这一作用与提高 $\eta$ 同向，
均通过增大 $\theta$ 实现；
但提高 $\eta$ 还会抬高绝对峰值，
因此二者在完整占优条件下并非无条件等价。

两组结构实验说明，$\Delta t,J$ 等强度量只经 $\theta$ 进入
（本节口径下精确到 $O(i_0/\theta)\lesssim8\times10^{-5}$）。具体地，
\[
\Delta t\approx\frac{1}{c_0\theta}\ln\frac{s^*-\bar s}{s_c-\bar s}\approx\frac{\text{const}}{c_0\theta},
\]
其中对数因子在 $\theta\in[5\times10^{-4},8\times10^{-3}]$ 内仅由 $2.20$ 变到 $2.15$。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{neff_scaling_collapse.pdf}
    \caption{固定绝对初值 $I_0$ 与 $\theta=0.002$ 时不同 $N_{\rm eff}$ 的归一化感染轨迹 $i$ 与隔离率 $q$ 在平移时间 $t-t_1$ 上的折叠（$i_0=I_0/N$ 随 $N$ 变，故 $t_1$ 由 $11.39$ 升到 $16.90$ 天；引理不含绝对时刻，$t-t_1$ 是恰当基准）。白心圆为各 $N_{\rm eff}$ 下绝对判据 $I=1$ 对应的不同清零终点。}
    \label{fig:neff_collapse}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{neff_time_metrics.pdf}
    \caption{固定绝对阈值口径下 $t_1,\Delta t,T_{\rm clear}$ 随 $N_{\rm eff}$ 的变化；$N_{\rm eff}$ 越小、$\theta=\eta/N_{\rm eff}$ 越大，平台控制时长越短。}
    \label{fig:neff_time}
\end{figure}

\textbf{结构不变量.} 第~\ref{sec:s1}节给出的平台期隔离率拐点高度 $\qinf=1-\frac{1}{2(1-\beta)}$ 只依赖传播概率 $\beta$。在上述实验上用均匀采样 $q_c(t)$ 的二阶差分独立定位拐点：固定 $\beta=0.1498$ 时，$\eta\in\{80,100,150\}$ 与 $N_{\rm eff}\in\{4,5,6\}\times10^4$ 六个校验点的数值拐点高度均为 $\qinf\approx0.4119$（与理论值最大绝对误差约 $8\times10^{-9}$），确认 $\qinf$ 不随 $N_{\rm eff},\eta$ 变化；改变 $\beta$ 时数值拐点高度沿 $1-\frac{1}{2(1-\beta)}$ 移动。$\qinf(\beta)$ 与 $q_0$ 的交点即定理~\ref{thm:s1:inflection} 下界 $\qinf>q_0$ 的失效边界，等价于 $(1-\beta)(1-q_0)=\tfrac12$；代入 $q_0=0.3230$ 精确得 $\beta_{\max}=0.261448$，与数值一致。越过该边界（$\beta>\beta_{\max}$）拐点经 $t_2$ 端掉出、控制期内 $q_c''<0$ 全程成立，对应第~\ref{sec:s1:inflection-position}小节的``全程凹''结构。此处的反演只是解析控制律下的一致性检查，不作为含噪数据上的参数估计。

\subsection{单调性}

\begin{lemma}[$\theta$ 单调性]\label{lem:dom:mono}
在可行区间
$\theta\in(i_0,i_{\max}^{no})$
上，$\Delta t$ 与 $J$ 均关于 $\theta$
严格单调递减。
此外，沿任意满足
\[
0<i_0<\theta,\qquad
i_0\to0,\qquad
\theta\to0
\]
的可行极限序列，若 $s_0>s_c$，
则
\[
\Delta t\to+\infty,
\qquad
J\to+\infty.
\]
\end{lemma}
\begin{proof}
由引理~\ref{lem:dom:scaling}，$\Delta t,J$ 是 $\theta$ 的函数；固定 $N$ 时 $\theta$ 与 $\eta$ 同增，
故其对 $\theta$ 的单调性等同于对 $\eta$ 的单调性。推论~\ref{cor:s1:duration-sensitivity} 已给出
$(\Delta t)_\eta<0$，引理~\ref{lem:s1:Sstar-sensitivity} 给出 $S^\ast_\eta<0$。对 $J$：由式~\eqref{eq:dom:Jtheta}，前因子 $1/(c_0\theta)$
关于 $\theta$ 严格递减，而积分上限 $s^*$ 随 $\theta$ 递减（$S^\ast_\eta<0$）使非负被积区间收缩，
故 $J$ 严格递减。

下面证明低阈值极限下的发散。
沿任意满足
$0<i_0<\theta$、
$i_0\to0$、
$\theta\to0$
的可行序列，首次触碰方程的连续性给出
\[
  s^\ast\longrightarrow s_0.
\]
由于假设 $s_0>s_c>\bar s$，
有
\[
  \ln\frac{s^\ast-\bar s}{s_c-\bar s}
  \longrightarrow
  \ln\frac{s_0-\bar s}{s_c-\bar s}
  >0.
\]
因此
\[
  \Delta t
  =
  \frac{1}{c_0\theta}
  \ln\frac{s^\ast-\bar s}{s_c-\bar s}
  \longrightarrow+\infty.
\]

对成本，式~\eqref{eq:dom:Jtheta} 中的积分满足
\[
\int_{s_c}^{s^\ast}
\frac{\bigl(q_c(Ns)-q_0\bigr)^2}
     {s-\bar s}\,ds
\longrightarrow
K_0
:=
\int_{s_c}^{s_0}
\frac{\bigl(q_c(Ns)-q_0\bigr)^2}
     {s-\bar s}\,ds.
\]
由
$q_c(Ns_c)=q_0$，
且 $q_c(Ns)>q_0$ 对所有
$s\in(s_c,s_0]$ 成立，
同时 $s-\bar s>0$，
故 $K_0>0$。于是
\[
  J(\theta)
  \sim
  \frac{2K_0}
       {c_0(1-q_0)^2}\,
  \frac{1}{\theta}
  \longrightarrow+\infty.
\]
\end{proof}

由引理~\ref{lem:dom:mono}，若 $J^{\rm T}$、$T_{\max}$ 落在相应值域内，则下列阈值唯一存在：
\begin{equation}
  J\big(\theta_{\rm cost}\big)=J^{\rm T},\qquad
  \Delta t\big(\theta_{\rm dur}(T_{\max})\big)=T_{\max}.
  \label{eq:dom:thetadefs}
\end{equation}
为简化记号，以下记
\begin{equation}
  \theta_{\rm bind}(T_{\max})
  :=
  \max\!\bigl(
    \theta_{\rm cost},
    \theta_{\rm dur}(T_{\max})
  \bigr).
  \label{eq:dom:theta-bind}
\end{equation}
不致混淆时简写为 $\theta_{\rm bind}$。

\subsection{占优区域与临界有效人口}

\begin{proposition}[占优区域]\label{prop:dom:region}
情景一阈值控制同时满足
\emph{(i)} $I_{\rm peak}^{\rm thr}=\eta<I_{\rm peak}^{\rm T}$、
\emph{(ii)} $J\le J^{\rm T}$、
\emph{(iii)} $\Delta t\le T_{\max}$，
当且仅当
\begin{equation}
  \theta_{\rm bind}(T_{\max})
  \ \le\
  \frac{\eta}{N}
  \ <\
  \min\!\left(
    \frac{I_{\rm peak}^{\rm T}}{N},
    i_{\max}^{no}
  \right).
  \label{eq:dom:band}
\end{equation}

式~\eqref{eq:dom:band} 所给区间对 $\eta$ 非空，
当且仅当
\begin{equation}
  \theta_{\rm bind}(T_{\max})<i_{\max}^{no}
  \qquad\text{且}\qquad
  N<N^\ast(T_{\max})
  :=
  \frac{I_{\rm peak}^{\rm T}}
       {\theta_{\rm bind}(T_{\max})}.
  \label{eq:dom:Nstar}
\end{equation}
特别地，若
$\theta_{\rm bind}(T_{\max})\ge i_{\max}^{no}$，
则该占优区间对所有 $N$ 均为空。
\end{proposition}
\begin{proof}
条件 (i) 等价于
\[
  \frac{\eta}{N}
  <
  \frac{I_{\rm peak}^{\rm T}}{N}.
\]
由引理~\ref{lem:dom:mono} 与
式~\eqref{eq:dom:thetadefs}，
条件 (ii) 等价于
\[
  \frac{\eta}{N}\ge\theta_{\rm cost},
\]
条件 (iii) 等价于
\[
  \frac{\eta}{N}\ge\theta_{\rm dur}(T_{\max}).
\]
再与非退化触发条件
\[
  \frac{\eta}{N}<i_{\max}^{no}
\]
取交，即得式~\eqref{eq:dom:band}。

该区间非空当且仅当
\[
  \theta_{\rm bind}(T_{\max})
  <
  \min\!\left(
    \frac{I_{\rm peak}^{\rm T}}{N},
    i_{\max}^{no}
  \right).
\]
这等价于同时满足
\[
  \theta_{\rm bind}(T_{\max})<i_{\max}^{no},
  \qquad
  \theta_{\rm bind}(T_{\max})
  <
  \frac{I_{\rm peak}^{\rm T}}{N}.
\]
由于 $\theta_{\rm bind}(T_{\max})>0$，
第二个不等式等价于
\[
  N<
  \frac{I_{\rm peak}^{\rm T}}
       {\theta_{\rm bind}(T_{\max})}
  =N^\ast(T_{\max}).
\]
因此区间非空当且仅当
式~\eqref{eq:dom:Nstar} 的两个条件同时成立。
若
$\theta_{\rm bind}(T_{\max})\ge i_{\max}^{no}$，
则区间对任意 $N$ 均为空。
\end{proof}

\begin{remark}
条件 (i) 中 $I_{\rm peak}^{\rm thr}=\eta$ 是策略定义使然（平台把峰值精确压到 $\eta$），
故 (i) 实为设计约束 $\eta<I_{\rm peak}^{\rm T}$，而非控制效果的比较；占优的实质内容在 (ii)(iii)
（成本、时长）及后续推论的广延量约束，不应将峰值一项读作方法优势。
\end{remark}

\begin{corollaryn}\label{cor:ceiling}
由引理~\ref{lem:dom:mono}，
$\theta_{\rm dur}(T_{\max})$
随 $T_{\max}$ 增大而单调递减，且
\[
  \theta_{\rm dur}(T_{\max})\longrightarrow0
  \qquad
  (T_{\max}\to\infty).
\]
因此
\[
  \theta_{\rm bind}(T_{\max})
  =
  \max\!\bigl(
    \theta_{\rm cost},
    \theta_{\rm dur}(T_{\max})
  \bigr)
  \longrightarrow
  \theta_{\rm cost}.
\]

若
$\theta_{\rm cost}<i_{\max}^{no}$，
则在满足
$\theta_{\rm bind}(T_{\max})<i_{\max}^{no}$
的非退化范围内，
\[
  N^\ast(T_{\max})
  =
  \frac{I_{\rm peak}^{\rm T}}
       {\theta_{\rm bind}(T_{\max})}
\]
随 $T_{\max}$ 单调不减，并且
\[
  N^\ast(T_{\max})
  \longrightarrow
  N^\ast_\infty
  :=
  \frac{I_{\rm peak}^{\rm T}}
       {\theta_{\rm cost}}.
\]
故即使允许任意长平台期，
有效人口上界仍被成本约束封顶于
$N^\ast_\infty$。

若
$\theta_{\rm cost}\ge i_{\max}^{no}$，
则即使令 $T_{\max}\to\infty$，
命题~\ref{prop:dom:region} 的占优区仍对所有 $N$ 为空。
\end{corollaryn}

\begin{corollaryn}\label{cor:cum}
在命题~\ref{prop:dom:region} 的占优区域上再加约束
\emph{(iv)} $\Itcum^{\rm thr}\le\Itcum^{\rm T}$。由定理~\ref{thm:s1:Itcum}，
到清零的总累计感染为
\[
  \Itcum^{\rm thr}=N\,h(\theta,N),\qquad \theta=\eta/N,
\]
其退出段因绝对清零判据 $I=1$ 带有弱 $1/N$ 依赖，故 $h=h(\theta,N)$。
与仅经 $\theta$ 进入的强度量（峰值、成本、时长）不同，累计是广延量：其占优边界
\begin{equation}
  N\,h(\theta,N)=\Itcum^{\rm T}
  \label{eq:dom:cum-feasible}
\end{equation}
在 $(N,\eta)$ 平面上是一条\emph{弧线}而非过原点直线，占优侧（$\Itcum^{\rm thr}\le\Itcum^{\rm T}$）
为其小 $N$ 一侧。因此累计约束把占优区域整体收缩。

对每个固定 $N$，$h(\theta,N)$ 关于 $\theta$ 单调不减，则该弧从大 $\theta$ 一侧削去占优区域，
而累计占优边界的下边缘 $\theta=\theta_{\rm bind}(T_{\max})$ 最小，故弧与下边缘相交处才把带完全截断。
记 $F_{T_{\max}}(N):=N\,h\!\bigl(\theta_{\rm bind}(T_{\max}),N\bigr)$，该交点的有效人口
$N_{\rm cum}^\ast(T_{\max})$ 由
\begin{equation}
  F_{T_{\max}}\!\bigl(N_{\rm cum}^\ast(T_{\max})\bigr)=\Itcum^{\rm T}
  \label{eq:dom:Ncumstar}
\end{equation}
确定。于是纳入累计后占优带对 $\eta$ 非空，当且仅当
$N<\min\{N^\ast(T_{\max}),\,N_{\rm cum}^\ast(T_{\max})\}$（与式~\eqref{eq:dom:Nstar} 取交）：
二者无普遍大小关系，须逐例数值比较。该弧的逐点求根与 $N_{\rm cum}^\ast$ 的数值唯一性口径
见第~\ref{sec:dom:xian}~节。
\end{corollaryn}

\paragraph{最小相容易感池。}
未知的 $N_{\rm eff}$ 有一个由观测直接反推的下界，且该下界与控制策略无关。
由式~\eqref{eq:model:full}，
\[
  \dot S_q=\frac{(1-\beta)c(t)q(t)}{N}SI,
  \qquad
  \dot I_{q_{\rm cum}}=\frac{\beta c(t)q(t)}{N}SI
  \quad\Longrightarrow\quad
  \dot S_q=\frac{1-\beta}{\beta}\,\dot I_{q_{\rm cum}},
\]
而 $S_q$ 只有流入、无回流。易感者的三条去向为社区感染、隔离感染与被追踪隔离
的未感染者，故该次疫情消耗的易感者总数为
\begin{equation}
  I_{\rm cum}^{\rm T}+I_{q_{\rm cum}}^{\rm T}+S_q^{\rm T}
  =I_{\rm cum}^{\rm T}+\frac{I_{q_{\rm cum}}^{\rm T}}{\beta}
  =605.40+\frac{1491.36}{0.1498}
  =1.0561\times10^{4}.
  \label{eq:dom:Sconsumed}
\end{equation}
该恒等式\textbf{与 $N$、$c(t),q(t)$ 及速率函数的具体形式全无关}——它只用到两个
转移通道的比值。再由 $S_0\le N$ 即得
\begin{equation}
  N_{\rm eff}\ \ge\ N_{\rm floor}
  :=I_{\rm cum}^{\rm T}+\frac{I_{q_{\rm cum}}^{\rm T}}{\beta}
  =1.06\times10^{4}.
  \label{eq:dom:Nfloor}
\end{equation}
这是本节区间的下端，也是对未知量 $N_{\rm eff}$ 的一个下界估计。%
\footnote{$N_{\rm floor}$ 依赖 $\beta$：$\beta=0.20$ 时为 $8062$，$0.25$ 时为 $6571$，
$0.30$ 时为 $5577$。第~\ref{sec:dom:xian}~节说明区间的\emph{宽度}对 $\beta$ 稳健。}
更弱地，由 $h<1$，当 $N<\Itcum^{\rm T}=2096.76$ 时 $Nh<N<\Itcum^{\rm T}$ 自动成立，
此时“累计不劣”只是算术必然、与控制优劣无关；式~\eqref{eq:dom:Nfloor} 比这一朴素界
紧 $5.04$ 倍，因为后者只计感染者、漏掉了被追踪隔离的未感染者 $S_q$。

在 $N=N_{\rm floor}$ 处该次疫情恰好把易感池耗尽（罹患率 $100\%$），故下端是极端情形：
越靠近它，“实际疫情消耗了整个混合池”这一隐含假设越强
（另见第~\ref{sec:dom:limits}~节 (4)）。

\begin{corollaryn}[纳入清零时间的收缩]\label{cor:clr}
情景一到动态清零的时刻 $t_{\rm end}$ 由定理~\ref{thm:s1:tend} 给出。与累计不同，
$t_{\rm end}$ 不是 $\theta$ 的函数：平台段贴边维持使 $S$ 的消耗速率正比于 $\eta$，
而退出段经 $S_{\rm end}$（式~\eqref{eq:s1:Send}，清零判据 $I=1$ 即 $i=1/N$）带绝对尺度，
故 $t_{\rm end}=t_{\rm end}(\theta,N)$，数值上在固定 $\theta$ 下随 $N$ 递增。若再要求
\emph{(v)} $t_{\rm end}^{\rm thr}\le t_{\rm end}^{\rm T}$，
则在占优带内该约束给出一条 $(N,\eta)$ 平面上的曲线边界
\begin{equation}
  t_{\rm end}\big(\eta/N,\,N\big)=t_{\rm end}^{\rm T},
  \label{eq:dom:clrarc}
\end{equation}
其占优侧为小 $N$ 一侧；记其与给定 $\eta$ 的交点为 $N_{\rm clr}(\eta)$。由式~\eqref{eq:dom:Nfloor}，
有信息量的区间为 $N\in[\,N_{\rm floor},\,N_{\rm clr}(\eta)\,]$。本文参数下
$\max_\eta N_{\rm clr}(\eta)=5166<N_{\rm floor}=1.06\times10^{4}$，
故该区间为\emph{空集}：不存在同时满足下界与清零不劣的有效人口
（第~\ref{sec:dom:xian}~节）。
\end{corollaryn}

\begin{remarkn}[强度量与广延量的二分]\label{rem:dom:dichotomy}
在固定归一化初值的理论口径下，
$\Delta t$、$J$、$q_{\max}$ 与峰值分数等强度量只经
$\theta=\eta/N$ 进入，其等值边界在
$(N,\eta)$ 双对数平面上是斜率 $1$ 的射线。
但命题~\ref{prop:dom:region} 的绝对峰值比较
$\eta<I_{\rm peak}^{\rm T}$ 给出水平线，
故完整占优条件并非与池规模无关。
总累计感染 $\Itcum$ 与清零时刻 $t_{\rm end}$
还带绝对尺度，其等值边界是弧线。
这一二分是
第~\ref{sec:dom:figure}~节两个面板的组织原则：面板 (a) 画直线族，面板 (b) 画弧线族。
\end{remarkn}

\subsection{西安参数下的实例}
\label{sec:dom:xian}

取 $\beta=0.1498,\gamma=0.2953,c_0=12.8872,q_0=0.3230$，$S_0/N\approx1$。
TDINN 基准取到清零口径下的西安全市固定拟合／重构参照值
$I_{\rm peak}^{\rm T}=151.90$、$J^{\rm T}=49.35$、$\Itcum^{\rm T}=2096.76$
（口径见第~\ref{sec:dominance}~节开头）。
由式~\eqref{eq:s1:Imax-no} 得 $i_{\max}^{no}\approx0.1047$。
数值求解式~\eqref{eq:dom:thetadefs}：
\[
  \theta_{\rm dur}(45\text{d})\approx3.762\times10^{-3},\quad
  \theta_{\rm dur}(90\text{d})\approx1.891\times10^{-3},
\]
\[
  \theta_{\rm cost}\approx1.656\times10^{-3},\quad
  \theta_{\rm dur}(150\text{d})\approx1.137\times10^{-3},
\]
且由 $\theta_{\rm dur}(T_{\max})$ 的单调性，
对本文考察的
$T_{\max}\in\{45,60,90,150,\infty\}\,\mathrm d$，
\[
  \theta_{\rm bind}(T_{\max})
  \le
  \theta_{\rm dur}(45\mathrm d)
  =
  3.762\times10^{-3}
  <
  i_{\max}^{no}=0.1047.
\]
故命题~\ref{prop:dom:region} 中的非退化前提
$\theta_{\rm bind}(T_{\max})<i_{\max}^{no}$
在上述西安参数下均成立。

相应的临界有效人口
$N^\ast(T_{\max})=I_{\rm peak}^{\rm T}/\theta_{\rm bind}(T_{\max})$ 为
\[
  N^\ast(45\text{d})\approx4.04\times10^{4},\quad
  N^\ast(60\text{d})\approx5.37\times10^{4},\quad
  N^\ast(90\text{d})\approx8.03\times10^{4},\quad
  N^\ast_\infty\approx9.17\times10^{4}.
\]
由 $\theta_{\rm dur}(T)\,T\approx0.170$ 近似为常数，$\theta_{\rm dur}=\theta_{\rm cost}$ 发生在
$T_{\max}\approx103$ d：$T_{\max}$ 大于该值时成本约束绑定
（$N^\ast(T_{\max})=N^\ast_\infty$），
小于该值时时长约束绑定。特别地 $N^\ast(150\text{d})=N^\ast_\infty$。

\paragraph{主结论：使阈值控制不劣的有效人口区间。}
只取命题~\ref{prop:dom:region} 的 (i)(ii) 两项——峰值不超过实际干预所达到的
$I_{\rm peak}^{\rm T}$、成本不超过 $J^{\rm T}$——即
$\theta_{\rm cost}N\le\eta<I_{\rm peak}^{\rm T}$，与下界~\eqref{eq:dom:Nfloor} 取交得
\begin{equation}
  N_{\rm eff}\in\bigl[\,N_{\rm floor},\,N^\ast_\infty\,\bigr]
  =\bigl[\,1.06\times10^{4},\ 9.17\times10^{4}\,\bigr],
  \label{eq:dom:main-interval}
\end{equation}
一个宽约 $8.7$ 倍的区间。若西安此次疫情的有效混合规模落于其中，则存在阈值 $\eta$，
使情景一阈值控制的社区峰值不超过 $151.90$、二次加权成本不超过 $49.35$，
且\textbf{完全不下调接触率}（$J_c=0$）。

其代价是平台时长：在成本边界上 $\Delta t(\theta_{\rm cost})=102.83$ 天。
该数与上述绑定切换点 $T_{\max}\approx103$ d 重合并非巧合——按定义，
$T_{\max}=\Delta t(\theta_{\rm cost})$ 时恰有 $\theta_{\rm dur}=\theta_{\rm cost}$，
即“主结论的时长代价”与“时长约束不再起作用的临界时长”是同一个量。
时长本身不是硬约束，而是由政策变量 $T_{\max}$ 编码的第三项考虑；
取有限 $T_{\max}$ 只收紧区间上端（$45$ d 收到 $4.04\times10^{4}$、
$60$ d 收到 $5.37\times10^{4}$、$90$ d 收到 $8.03\times10^{4}$；
$T_{\max}\gtrsim103$ d 后成本绑定、上端封顶于 $9.17\times10^{4}$），下端不变。

\paragraph{区间宽度对 $\beta$ 的稳健性。}
传播概率 $\beta$ 与接触率 $c_0$ 只有乘积 $\beta c_0$ 可由数据识别。
沿该脊（$\beta c_0=1.9305$）在 $\beta\in[0.10,0.30]$ 上扫描，
区间两端同步平移而宽度几乎不变：
\[
  \frac{N^\ast_\infty}{N_{\rm floor}}\in[\,8.669,\ 8.688\,],
  \qquad\text{极差 }0.22\%,
\]
（$\beta=0.10/0.1498/0.20/0.30$ 处 $N^\ast_\infty=13.48/9.17/7.00/4.83\times10^4$、
$N_{\rm floor}=1.55/1.06/0.81/0.56\times10^4$）。
故主结论的\emph{宽度}不受 $\beta$ 不可识别性的影响，$\beta$ 只决定区间的位置。
这是所考察范围内的数值发现而非精确恒等式：$\beta N^\ast_\infty$ 与
$\beta N_{\rm floor}$ 各自随 $\beta$ 漂移约 $7.6\%$ 与 $7.8\%$，两者都不严格
正比于 $1/\beta$，只是漂移幅度接近。

两条广延量边界（推论~\ref{cor:cum}、\ref{cor:clr}）由求解器逐点求根得到，均为微弯弧线而非竖直线：
\begin{itemize}
  \item \textbf{累计边界}
  $N\,h(\theta,N)=\Itcum^{\rm T}$：
  在图示范围 $\eta\in[10,151.90]$ 内，自
  $(N,\eta)=(9471,151.90)$ 延伸至 $(12177,10)$。
  其中 $\eta=10$ 是本文绘图区间的下限，
  并非该等值线的自然数学终点。
  在 $\eta=100$ 处，$N_{\rm cum}\approx1.010\times10^{4}$。
  沿弧 $h$ 由 $0.221$（$\eta=151.90$ 端）降到 $0.172$（$\eta=10$ 端）。
  当前保存的 16 个累计弧扫描点满足：
  $N$ 随 $\eta$ 严格递减，
  $\eta/N$ 随 $\eta$ 严格递增，
  且相对成本线仅有一次符号变化。
  因此在这些保存点及其插值范围内，
  累计弧与成本线存在唯一数值交点
  $(N,\eta)\approx(11764,19.48)$。
  将这一成本绑定口径下的数值临界人口记为
  \[
    N_{\rm cum,\infty}^\ast
    \approx1.17\times10^4.
  \]
  穿出后等值线继续存在，但 $\mathcal{W}_{\rm cum}$
  的实际边界已改由成本线给出。
  这里的唯一性只是在当前保存扫描与插值范围内观察到的数值现象，
  不扩写为 $F_\infty$ 在一般参数范围内严格递增的结构定理。
  \item \textbf{清零边界} $t_{\rm end}=t_{\rm end}^{\rm T}$：在图示范围内自
  $(865.9,\,10)$ 延伸到 $(5165.7,\,151.90)$，$\eta=100$ 处
  $N_{\rm clr}\approx3.97\times10^{3}$。该弧的全段最大值 $N=5166$ 仍低于
  下界 $N_{\rm floor}=1.06\times10^{4}$ 达 $2.0$ 倍，
  故在本文口径下\textbf{清零占优区为空集}：不存在同时满足
  $N\ge N_{\rm floor}$ 与 $t_{\rm end}\le t_{\rm end}^{\rm T}$ 的有效人口。
\end{itemize}
与下界~\eqref{eq:dom:Nfloor} 取交后，纳入总累计的可行区间收缩为
$N\in[1.06\times10^{4},\,1.18\times10^{4}]$——一条宽度不足 $1.12$ 倍的窄条，
其上端由成本线绑定（累计弧在 $(11762,19.48)$ 穿出楔形），下端由 $N_{\rm floor}$ 绑定。
该窄条紧贴下界，含义是：只有当真实混合池几乎就等于被消耗掉的易感人数时，
阈值控制在总累计上才也不劣。
数值上
$N_{\rm cum,\infty}^\ast/N^\ast_\infty\approx0.13$；
这只是当前西安扫描中的量级比较，
不替代推论~\ref{cor:cum} 对一般
$N_{\rm cum}^\ast(T_{\max})$ 所列的条件。
第~\ref{sec:scaling}节折叠使用到 $t_2$ 的累计分数（$\theta$-only）；
此处与局限中的 $h$ 是到清零的三段量，
退出段经 $s_{\rm end}$ 带弱 $1/N$ 依赖。

作为定位，$N=5\times10^{4}$、$\eta=100$（$\theta=0.002$）落在
命题~\ref{prop:dom:region} 的占优带内（取 $T_{\max}=150$ d）：峰值 $100<151.90$、
$J=40.77<49.35$、$\Delta t=85.07$ 天；但其到清零累计 $Nh\approx9.03\times10^{3}>\Itcum^{\rm T}$
且清零 $t_{\rm end}\approx176$ d $>45.27$ d，(iv)(v) 均不满足，与推论~\ref{cor:cum}、\ref{cor:clr} 一致。
此处的 $J$ 与 $\Delta t$ 同第~\ref{sec:xian}~节全市口径下同 $\theta$ 的取值逐位相同，
这正是引理~\ref{lem:dom:scaling} 在单一口径下严格成立的直接体现。

\begin{remark}[指标依赖性与物理口径]
占优是相对一组指标而言的：在 $\{$峰值,成本$\}$ 下区间为
$[1.06\times10^4,\,9.17\times10^4]$；纳入总累计后收缩到
$[1.06\times10^4,\,1.18\times10^4]$，后者对应有界小混合池。
值得一提的是，$10^4$–$10^5$ 正是中国城市一个街道或大型社区的常住人口量级，
西安全市有一百余个街道——区间落在真实的行政与混合单元尺度上，
这本身是对该区间合理性的一个旁证。
这样的小 $N_{\rm eff}$ 在物理上分两类，二者对
“仅提高隔离率、$J_c=0$”这一表述的支撑并不相同：
\begin{itemize}
  \item \emph{天然有界单元}（校园、厂区、邮轮）：小 $N_{\rm eff}$ 是既成的结构边界、非规划者制造，
  守住社区感染者阈值确实只需隔离率一种控制，$J_c=0$ 合法，占优结论成立；
  \item \emph{封控造出的小池}（封控小区）：小 $N_{\rm eff}$ 靠空间分区/封控实现，而该分区本身改变跨区
  接触结构、构成一种接触控制，其代价并未计入 $J$；此时“$J_c=0$”须扣除分区接触控制成本，占优结论
  相应打折。
\end{itemize}
这一分类在本节的定位下有更尖锐的一层含义：$N_{\rm eff}$ 是该次疫情\emph{在实际干预之下}
的有效混合规模，而实际干预包含大幅接触率下调；若第二类情形成立，则两条臂共用同一
$N_{\rm eff}$ 的前提被阈值控制自身破坏。这一内生性见第~\ref{sec:dom:limits}~节 (2b)。
\end{remark}

\subsection{局限与口径}
\label{sec:dom:limits}
(1) 占优结论依赖所选指标集：主结论式~\eqref{eq:dom:main-interval} 只含峰值与成本两项。
情景一在总累计上还须满足推论~\ref{cor:cum} 给出的条件，其区间收缩为
$[N_{\rm floor},\,N_{\rm cum,\infty}^\ast]$，宽度不足 $1.12$ 倍；
清零时刻上则因 $\max_\eta N_{\rm clr}(\eta)<N_{\rm floor}$ 而为空集。
两者一律取到清零口径，与 $\Itcum^{\rm T},t_{\rm end}^{\rm T}$
的积分／终止终点一致。

(2) 参照量对 $N_{\rm eff}$ 的稳健性。 本节把 $N_{\rm eff}$ 当作待定参数，故须问：
四个 TDINN 参照量本身是否依赖于所假设的 $N$？
$I_{\rm peak}^{\rm T},\Itcum^{\rm T},t_{\rm end}^{\rm T}$ 是对已发生疫情的重构，
其值由每日新增社区与隔离病例这同一份数据锁定；任何在该数据上拟合良好的模型都须复现它们，
故这三项在本节涉及的 $N$ 范围内可视为数据锚定的常量。
$J^{\rm T}$ 不同：它由拟合得到的 $c_{\rm TDINN}(t),q_{\rm TDINN}(t)$ 积分而来，
而传播项经 $S/N$ 携带 $N$ 依赖——在全市口径下 $S/N\approx1$，
在 $N_{\rm eff}\sim10^4$ 的池上该近似不再成立。
因此本节区间的\emph{上端} $N^\ast_\infty=I_{\rm peak}^{\rm T}/\theta_{\rm cost}$
的精度受限于 $J^{\rm T}$ 的这一层 $N$ 依赖，本文未予定量（见第~\ref{sec:discussion}~节未来工作）；
\emph{下端} $N_{\rm floor}$ 由式~\eqref{eq:dom:Sconsumed} 这一与 $N$ 无关的解析恒等式给出，
不受此影响。

(2b) 有效混合规模的内生性。 本节把 $N_{\rm eff}$ 视为该次疫情的外生规模参数，
在两种策略下取同一值。但实际干预包含大幅接触率下调，其本身即在压缩有效混合范围；
情景一阈值控制不动接触率（$J_c=0$），其面对的有效混合池应\emph{大于}实际干预下的
$N_{\rm eff}$。故本节区间宜读作\textbf{必要条件}：偏差方向是确定的——真实池更大意味着
阈值控制的实际表现劣于此处所算，区间上端因而偏乐观。
要消除该内生性，需要能把接触控制强度映射到有效混合规模的模型，超出本文范围。

(3) 反事实轨迹的性质。 本节不对任何 $N_{\rm eff}$ 做拟合，故不存在拟合残差随 $N$ 退化的问题；
初值取第~\ref{sec:xian}~节标定的绝对值 $I_0=1.00663\times10^{-3}$，两条臂共享同一次播种。
但阈值控制是未发生的反事实，其轨迹在任何 $N$ 上都只能是结构性推演、而非标定过的预测：
数据只约束了实际发生的那一条控制路径，未发生的路径无从校验。

(4) 下界处的参数可信度与触发域墙。 由式~\eqref{eq:dom:Nfloor}，
$N_{\rm floor}=I_{\rm cum}^{\rm T}+I_{q_{\rm cum}}^{\rm T}/\beta=1.06\times10^{4}$。
越靠近该下界，$S/N$ 越偏离 $1$，而文献~\cite{He2023TDINN} 的参数正是在
$S/N\approx1$ 的全市口径下标定的——故区间下端也是借用参数最不可靠的一端；
这与 (2) 中 $J^{\rm T}$ 的 $N$ 依赖是同一件事的两端。
此外，本口径下 $i_0=I_0/N$ 随池缩小而增大，故启动时间
$t_1=(1/r)\ln(\theta/i_0)$ 随 $N$ 减小而提前（$\theta=0.002$ 时由全市的
$16.90$ 天降到 $N=10^4$ 的 $9.80$ 天）。这不是退化而是该口径的定义性质：
同一批种子在更小的池中占更大比例、更早触及阈值。相应地，广延量边界中
只有清零弧经 $t_1$ 对口径敏感（见 (5)）。

(5) 初值口径的影响。 命题的直线族边界对初值口径几乎免疫：$\theta_{\rm cost}$ 与
$N^\ast$ 在 $i_0$ 跨 $10^{-11}$ 至 $10^{-7}$ 时的变化在七位有效数字内不可见，
$\Delta t$ 与 $J$ 的跨 $N$ 相对极差不超过 $6\times10^{-7}$；累计弧同样逐点不变。
唯一敏感的是清零时刻 $t_{\rm end}$，它经启动时间
$t_1\approx\frac{1}{r}\ln\frac{\theta}{i_0}$
（$r=\beta c_0(1-q_0)-\gamma=1.012\,\mathrm{d}^{-1}$，即 $i_0$ 每变十倍 $t_1$ 变 $2.28$ 天）
对口径呈对数敏感：若改用固定归一化初值 $i_0=7.65\times10^{-11}$，清零弧的
全段最大值由 $5166$ 移到 $2439$。两种口径下该弧都整体位于
$N_{\rm floor}=1.06\times10^{4}$ 之下（分别低 $2.0$ 倍与 $4.3$ 倍），
故“清零占优区为空集”这一结论不依赖初值口径的选择。

(6) 小 $N$ 的物理实现分两类（自然有界单元 vs 封控造池），后者靠空间分区/封控实现，该分区本身
即一种接触控制、成本未计入 $J$；第~\ref{sec:dom:xian}~节的“指标依赖性与物理口径”注记已分层讨论，
其对本节定位的含义即上文 (2b) 的内生性问题。

(7) $\Delta t$ 有界但可长达数十至逾百天，“可接受”由政策变量 $T_{\max}$ 编码；
图~\ref{fig:dom} (a) 的阴影楔形对应 $T_{\max}\to\infty$（成本封顶）口径，
有限 $T_{\max}$ 的边界见同图两条时长线。

\subsection{占优区域图的构造}
\label{sec:dom:figure}

在 $(N,\eta)$ 双对数平面上，命题~\ref{prop:dom:region} 的每条约束都是直线：峰值约束为水平线，
其余为过原点射线（双对数下斜率 $1$）。
\begin{align}
  \text{峰值线：}\ &
  \eta=I_{\rm peak}^{\rm T}
  &&(\text{占优侧 }\eta<I_{\rm peak}^{\rm T}),\\
  \text{成本线：}\ &
  \eta=\theta_{\rm cost}N
  &&(\text{占优侧 }\eta\ge\theta_{\rm cost}N),\\
  \text{时长线：}\ &
  \eta=\theta_{\rm dur}(T_{\max})N
  &&(\text{占优侧 }\eta\ge\theta_{\rm dur}(T_{\max})N),\\
  \text{触发线：}\ &
  \eta=i_{\max}^{no}N
  &&(\text{可行侧 }\eta<i_{\max}^{no}N).
\end{align}
占优楔形为
\[
\mathcal{W}_{\rm pcd}
=
\left\{
(N,\eta):
\theta_{\rm bind}(T_{\max})N
\le\eta
<
\min\!\bigl(
  I_{\rm peak}^{\rm T},
  i_{\max}^{no}N
\bigr)
\right\}.
\]
下界取成本／时长中较陡者，上界取峰值线／触发线中较低者。
其闭包的临界交点为
下界射线与
$\eta=I_{\rm peak}^{\rm T}$
的交点
$(N^\ast(T_{\max}),I_{\rm peak}^{\rm T})$；
由于峰值上界是严格不等式，该交点本身不属于占优集合。

按注记~\ref{rem:dom:dichotomy} 的二分，图~\ref{fig:dom} 分两个面板：面板 (a) 画\emph{直线族}，
即上述四条强度量边界；面板 (b) 在同一坐标下画\emph{弧线族}，即推论~\ref{cor:cum} 的累计边界与
推论~\ref{cor:clr} 的清零边界，二者围出的区域满足
$\mathcal{W}_{\rm cum}=\mathcal{W}_{\rm pcd}\cap\{N h\le \Itcum^{\rm T}\}\cap\{N\ge \Itcum^{\rm T}\}$、
$\mathcal{W}_{\rm clr}=\mathcal{W}_{\rm pcd}\cap\{t_{\rm end}\le t_{\rm end}^{\rm T}\}\cap\{N\ge \Itcum^{\rm T}\}$，
本文参数下 $\mathcal{W}_{\rm clr}\subset\mathcal{W}_{\rm cum}\subset\mathcal{W}_{\rm pcd}$。
两个面板上以同色标记点与图~\ref{fig:panel_eta_lever}、\ref{fig:panel_N_lever} 的采样角色联动：
圆点为 $\eta$ 杠杆（固定 $N_{\rm eff}=2\times10^{4}$）的三个展示取样，方点为 $N$ 杠杆（固定 $\eta=100$）
的五个展示取样。
同一三条 $\theta$ 边界在其他有效人口下的轨迹及拐点投影见附录~\ref{app:panel_eta_N}。

面板 (a) 的阴影楔形以成本线为下界，对应 $T_{\max}\to\infty$ 的封顶口径（顶点 $N^\ast_\infty$）；
两条时长线（$45$ d、$150$ d）画在其中，用以显示有限 $T_{\max}$ 下边界将如何抬高或压低，
以及轨迹族在跨越成本线时的位置。面板 (b) 的骨架线改用中性灰以突出两条弧线；
累计弧穿出楔形后的一段、以及位于下界 $N<N_{\rm floor}$ 的各段，均以虚线示意
“等值线继续存在但已不构成有效边界”。两个面板都画出竖直的下界线
$N=N_{\rm floor}=1.06\times10^{4}$：该下界是式~\eqref{eq:dom:Nfloor} 对未知 $N_{\rm eff}$ 的
约束，对全节一律适用，而非只作用于广延量约束。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_dom_combined.pdf}
  \caption{$(N_{\rm eff},\eta)$ 平面上情景一阈值控制相对 TDINN 全市固定拟合参照的占优区域，双对数坐标。
  \textbf{(a) 直线族（强度量）}：峰值线 $\eta=I_{\rm peak}^{\rm T}=151.90$（红）、成本线
  $\eta=\theta_{\rm cost}N$（蓝）、两条时长线 $\eta=\theta_{\rm dur}(T_{\max})N$（$45$ d 虚线、
  $150$ d 点划线）与触发线 $\eta=i_{\max}^{no}N$（点线，$i_{\max}^{no}=0.1047$）。
  阴影为占优楔形 $\mathcal{W}_{\rm pcd}$，以成本线为下界，对应 $T_{\max}\to\infty$ 口径，
  顶点 $N^\ast_\infty\approx9.17\times10^{4}$；由 $\theta_{\rm dur}(T)T\approx0.170$，
  $T_{\max}\lesssim103$ d 时改由时长线绑定，例如 $N^\ast(45\text{d})\approx4.04\times10^{4}$。
  \textbf{(b) 弧线族（广延量）}：累计边界（蓝青，$N h=\Itcum^{\rm T}$）在图示范围内自
  $(9471,151.90)$ 延伸至 $(12177,10)$，其中 $\eta=10$ 是绘图区间下限而非等值线的自然终点；
  清零边界（暖灰棕，$t_{\rm end}=t_{\rm end}^{\rm T}$，自 $(865.9,10)$ 到
  $(5165.7,151.90)$）。累计弧在 $(11762,19.48)$ 穿出楔形，其后一段以淡虚线示意等值线
  延续但不再构成 $\mathcal{W}_{\rm cum}$ 的边界。
  两面板中的竖直实线为最小相容易感池 $N_{\rm floor}=1.06\times10^{4}$
  （式~\eqref{eq:dom:Nfloor}），其左侧斜纹区与观测不相容；清零弧\emph{全段}位于该线左侧
  （最大值 $N=5166$），故清零占优区为空集、全线以虚线绘出；累计占优区收缩为
  $N\in[1.06,1.18]\times10^{4}$ 的窄条。
  骨架线（峰值·成本·时长·触发）以中性灰重绘以突出弧线。两面板的圆点与方点分别对应
  图~\ref{fig:panel_eta_lever} 与图~\ref{fig:panel_N_lever} 的采样角色，同色联动。
  $\eta=100$ 处 $N_{\rm cum}\approx1.010\times10^{4}$、$N_{\rm clr}\approx3.97\times10^{3}$。}
  \label{fig:dom}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figures/fig_panel_A.pdf}
  \caption{$\eta$ 杠杆：固定有效人口 $N_{\rm eff}=2\times10^{4}$，沿三条由引理~\ref{lem:dom:scaling}
  的理论口径得到的边界参照线取阈值 $\eta=\theta N_{\rm eff}$ 的情景一阈值控制轨迹。
  各条平台的高度即其 $\eta$；按 $\eta$ 升序，三条参照线依次为
  dur 150 d（$\theta_{\rm dur150}=1.137\times10^{-3}$，$\eta=22.7$）、
  cost（$\theta_{\rm cost}=1.656\times10^{-3}$，$\eta=33.1$）、
  dur 45 d（$\theta_{\rm dur45}=3.762\times10^{-3}$，$\eta=75.2$）。
  (a) 社区感染者 $I(t)$（对数纵轴）：三条平台分别贴合各自的 $\eta$，且因
  $N_{\rm eff}<N^\ast(45\mathrm d)$ 全部落在 $I_{\rm peak}^{\rm T}$ 之下（红虚线）。
  平台上的同色实心圆为 $q_c(t)$ 拐点时刻 $t_{\rm inf}$ 在 $I(t)$ 上的投影
  $(t_{\rm inf},\eta)$，并非 $I(t)$ 自身的拐点。在本文的单一口径下启动时间
  $t_1$ 随 $N_{\rm eff}$ 变（本图 $N_{\rm eff}=2\times10^{4}$ 处三条参照线依次为
  $9.92/10.29/11.12$ d），故绝对 $t_{\rm inf}$ 在不同 $N_{\rm eff}$ 图之间平移；
  平台内相对位置
  $\lambda=(t_{\rm inf}-t_1)/(t_2-t_1)\approx0.861$ 以及距平台结束的时间
  $t_2-t_{\rm inf}\approx20.8/14.3/6.3$ d（依次对应 dur 150 d/cost/dur 45 d）
  基本不随 $N_{\rm eff}$ 改变（见附录~\ref{app:panel_eta_N}）。
  灰虚线为同一 $N_{\rm eff}$ 下的常规控制，其峰值 $\approx0.105\,N_{\rm eff}$、到动态清零的
  总累计感染 $\approx0.348\,N_{\rm eff}$——两者都是与池规模无关的固定比例（随 $N_{\rm eff}$
  线性放大，此处 $N_{\rm eff}=2\times10^4$ 即 $2093/6970$）；其中累计约为右上角 inset 中策略柱的两倍，
  若入柱图会压扁三条策略柱的排序，故只以闭式给出、不在图中另绘；黑实线为 TDINN 在西安全市口径下
  的原始拟合轨迹（峰值 $151.90$、清零 $45.27$ d），作为固定拟合参照，不随
  $N_{\rm eff}$ 重算（见第~\ref{sec:dominance}~节 TDINN 基准的口径说明）。
  (b) 隔离率 $q_c(t)$：三条松弛曲线与 TDINN 的上升高斯 $q_{\rm TDINN}(t)$；
  各条彩色实线均包含控制前和 $t_2$ 后直到自身动态清零的 $q_0=0.3230$ 阶段，
  以及 $t_1\leq t\leq t_2$ 的阈值控制段；彩色竖直虚线连接 $t_1$ 处从
  $q_0$ 到 $q_c(t_1^+)$ 的瞬时跳跃。曲线按动态清零时刻从长到短叠绘，使短清零轨迹
  在重合的 $q_0$ 段上优先可见；各曲线在 $q_0$ 上的同色短竖线标出自身动态清零时刻；灰虚线为拐点高度
  $\qinf=1-1/[2(1-\beta)]=0.4119$。
  白心圆为各条曲线的凹凸拐点，均落在 $\qinf$ 上，与其只依赖 $\beta$、
  不依赖 $(N_{\rm eff},\eta)$ 的结论一致。右上角内嵌柱图按 $\eta$ 递增排列，给出动态清零
  $I(t)\leq1$ 时的总累计感染 $\Itcum$，柱色随 $\eta$ 加深，纵轴从零起算；
  黑虚线为 TDINN 全市固定拟合参照 $\Itcum^{\rm T}=2096.76$。三条均高于该参照且随
  $\eta$ 递增；此处 $N_{\rm eff}=2\times10^4$ 已超过累计占优临界
  $N_{\rm cum,\infty}^\ast\approx1.17\times10^4$（推论~\ref{cor:cum} 及第~\ref{sec:dom:xian}~节），
  故累计约束在三条上均不成立。}
  \label{fig:panel_eta_lever}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{figures/fig_panel_B.pdf}
  \caption{$N$ 杠杆：固定绝对阈值 $\eta=100$，在五个有效人口下的情景一阈值控制轨迹。
  五条平台高度相同（均为 $\eta$），故 $N_{\rm eff}$ 无法在纵轴上直读，各曲线对应的 $N_{\rm eff}$ 已随角色名列于图例；五个取值取自各条边界与 $\eta=100$ 的交点：
  clear $\le45.3$ d（$N_{\rm eff}=3{,}970$）、cumulative（$10{,}102$）、
  dur 45 d（$26{,}584$）、cost（$60{,}381$）、
  dur 150 d（$87{,}944$）。其中 clear 成员的平台仅 $\Delta t=6.3$ d，近于退化。
  (a) $I(t)$（对数纵轴）：五条平台\emph{全部恒在} $\eta=100$（灰虚线），与池规模无关，
  这是强度量只通过 $\theta=\eta/N_{\rm eff}$ 进入的直接体现；平台\emph{时长}则随
  $N_{\rm eff}$ 增大而显著拉长。平台上的同色实心圆与图~\ref{fig:panel_eta_lever} 同义，
  为 $q_c(t)$ 拐点时刻 $t_{\rm inf}$ 在 $I(t)$ 上的投影 $(t_{\rm inf},\eta)$，
  并非 $I(t)$ 自身的拐点；此处五点同落在 $I=\eta$ 一线上横向铺开，
  可直接读出 $t_{\rm inf}$ 随 $N_{\rm eff}$ 的推移。灰色带为常规控制在 $N_{\rm eff}\in[3{,}970,\,87{,}944]$（与上列五个取值同范围）
  上取 $8$ 条几何间隔轨迹的逐点最小--最大包络，仅绘制到全部成员均未清零为止；
  两条灰虚线为区间端点的代表轨迹。带宽即该反事实策略对池规模的敏感度，
  与阈值控制平台的零带宽形成对照。带内每条常规控制的峰值 $\approx0.105\,N_{\rm eff}$、
  到动态清零的总累计感染 $\approx0.348\,N_{\rm eff}$，均为与池规模无关的固定比例（随 $N_{\rm eff}$
  线性放大，最大成员 $N_{\rm eff}=87{,}944$ 处约为 $9200/30600$）；其中累计约为 inset 中同规模策略柱的两倍，
  故只以闭式给出、不在图中另绘。
  黑实线为 TDINN 在西安全市口径下的原始拟合轨迹
  （峰值 $151.90$、清零 $45.27$ d），作为全市固定拟合参照只画一条，
  不随 $N_{\rm eff}$ 重算；红虚线为其峰值 $I_{\rm peak}^{\rm T}$。
  (b) $q_c(t)$：五条松弛曲线与 TDINN 的 $q_{\rm TDINN}(t)$；每条彩色实线均包含
  控制前和 $t_2$ 后直到自身动态清零的 $q_0$ 阶段，以及 $t_1\leq t\leq t_2$
  的阈值控制段，彩色竖直虚线连接 $t_1$ 处从 $q_0$ 到 $q_c(t_1^+)$ 的瞬时跳跃。
  曲线按动态清零时刻从长到短叠绘，使短清零轨迹在重合的 $q_0$ 段上优先可见；
  各曲线在 $q_0$ 上的同色短竖线标出自身动态清零时刻。
  白心圆的拐点同样全部落在
  $\qinf=0.4119$，给出跨 $N_{\rm eff}$ 的不变性，与图~\ref{fig:panel_eta_lever}
  的跨 $\eta$ 不变性互补。右上角内嵌柱图按 $N_{\rm eff}$ 递增排列，给出动态清零
  $I(t)\leq1$ 时的总累计感染 $\Itcum$；五柱依次约为
  $945/2097/5016/10784/15410$，黑虚线为 TDINN 全市固定拟合参照
  $\Itcum^{\rm T}=2096.76$。因此 clear 边界远低于该参照，cumulative 边界按定义
  恰好达到该参照，而其余三条边界均高于该参照。}
  \label{fig:panel_N_lever}
\end{figure}


\subsection{接触率通道：$c_0$ 对阈值控制律的结构影响}
\label{sec:dom:c0}

第~\ref{sec:dominance}~节的占优分析中，$(N_{\rm eff},\eta)$ 只通过 $\theta=\eta/N$ 进入强度量（引理~\ref{lem:dom:scaling}），故 $\eta$ 杠杆与 $N$ 杠杆是同一条 $\theta$ 轴的两种走法。常规接触水平 $c_0$ 则是独立于 $\theta$ 的第三条轴：$s_c,\bar s\propto1/c_0$，而 $s^*$ 经 Lambert $W_{-1}$ 同时依赖 $\theta$ 与 $c_0$，因此改变 $c_0$ 一般会破坏 $\theta$ 坍缩。本节固定西安传播参数 $\beta,\gamma,q_0$、固定 $N_{\rm eff}=2\times10^4$ 与 $\theta=0.002$（即 $\eta=40$），初值取全节统一的绝对值 $I_0=1.00663\times10^{-3}$ 并跨 $c_0$ 保持不变，只连续改变 $c_0$。

由于 $c_0$ 改变即改变接触结构，第~\ref{sec:xian}~节锁定于全市 $c_0=12.8872$ 拟合的 TDINN 参照不再可比，故本节不与 TDINN 或常规控制对照，只考察阈值控制律自身随接触环境的结构响应；这也是本节为敏感性分析、而非占优分析的原因。相应地，$c_0$ 应理解为外生的接触环境（如低接触的有界单元与高密度城市之别），而非可自由调节的旋钮：若较低 $c_0$ 由封控／社交限制实现，则该分区本身即一种接触控制、其代价并未计入 $J_c$，此时``$J_c=0$''的表述须相应扣除（与第~\ref{sec:dom:xian}~节的两类小池讨论同源）。

$c_0$ 对轨迹与控制律的作用。 图~\ref{fig:c0-panel} 给出五条代表轨迹。因 $\theta,N$ 固定，各平台高度恒为 $\eta$；随 $c_0$ 增大，系统更快触及红线（$t_1$ 提前）、上升支更陡，启动隔离率 $q_{\max}=1-\gamma/(\beta c_0 s^*)$ 单调升高，与命题~\ref{prop:s1:sensitivity} 的 $(t_1)_{c_0}<0$、$(q_{\max})_{c_0}>0$ 一致。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{c0_sensitivity_panel.pdf}
    \caption{固定 $N_{\rm eff}=2\times10^4$、$\theta=0.002$（$\eta=40$）、西安传播参数，沿 $c_0$ 取五个代表值的阈值控制轨迹。$(a)$~$I(t)$：平台同高 $=\eta$，$c_0$ 增大使 $t_1$ 提前、上升支变陡；实心圆为 $q_c(t)$ 拐点时刻 $t_{\mathrm{inf}}$ 在平台上的投影 $(t_{\mathrm{inf}},\eta)$，非 $I(t)$ 自身拐点。$(b)$~$q_c(t)$：$q_{\max}$ 随 $c_0$ 展开，白圈（内部拐点）全部落在 $\qinf=0.4119$；$c_0=3.43,3.49$ 因 $q_{\max}<\qinf$ 全程凸、无内部拐点。右上柱图为各情景到自身动态清零 $I(t)=1$ 的总累计感染 $\Itcum=I_{c,\mathrm{cum}}+I_{q,\mathrm{cum}}$，在这五个代表点上随 $c_0$ 递增。不画 TDINN 与常规控制：$c_0$ 改变了接触结构，固定参照不可比。}
    \label{fig:c0-panel}
\end{figure}

在本节固定 $\theta,\beta,q_0$ 的单参数切片中，
 $c_0$ 经上界 $q_{\max}>\qinf\Leftrightarrow s^*>2\bar s$ 决定拐点是否从 $t_1$ 端掉出
,这是 $c_0$ 通道与当前 $\theta$ 扫描区间的主要差别(当前$\theta$对拐点存在与否影响不大)。但内部拐点的存在性并非由 $c_0$ 唯一决定：
下界由 $\beta,q_0$ 决定，上界还经 $S^*$ 受到 $c_0,\eta,\beta,q_0$ 及初值影响。
图~\ref{fig:c0-panel} 中 $c_0=3.43,3.49$ 虽能触发控制，但 $q_{\max}<\qinf$，$q_c(t)$ 全程凸、无内部拐点；$c_0=3.59$ 起拐点出现。据此可在扫描区间内数值定出拐点存在边界。

平台隔离率拐点高度的三重不变性。存在拐点时，其高度 $\qinf=1-\tfrac{1}{2(1-\beta)}$ 只依赖 $\beta$：图~\ref{fig:c0-panel} 中所有白圈仍落在同一条 $\qinf=0.4119$ 上。结合 $\eta$ 杠杆（图~\ref{fig:panel_eta_lever}）与 $N$ 杠杆（图~\ref{fig:panel_N_lever}），可得 $\qinf$ 对 $\eta,N,c_0$ 三者全不变，只有 $\beta$ 能移动它。

$\Delta t$ 随 $c_0$ 的非单调性。 与 $\Delta t$ 关于 $\theta$ 严格单调递减（引理~\ref{lem:dom:mono}）不同，$\Delta t$ 关于 $c_0$ 无固定符号（推论~\ref{cor:s1:duration-sensitivity}）：由式~\eqref{eq:s1:t2} 在触发边界处 $\Delta t\to0$，随 $c_0$ 增大先升至一个脊、再降。图~\ref{fig:c0-panel} 与图~\ref{fig:c0-scan} 显示，本节参数下 $\Delta t$ 在这五个代表点上依次为 $30.2,37.8,47.5,103.2,85.1$ 天——先随 $c_0$ 上升、越过脊后回落，脊约在 $c_0\approx6.61$（$\Delta t\approx103.2$ 天）。该脊并非数值巧合：由推论~\ref{cor:s1:duration-sensitivity}，$\partial\Delta t/\partial c_0$ 的符号由判别式 $\ln(S_0/S^*)\gtrless\Theta$ 决定，脊 $\partial\Delta t/\partial c_0=0$ 恰是判别式取等 $\ln(S_0/S^*)=\Theta$ 的解，其中 $\Theta=\frac{S^*-S_c}{S_c}\left[\frac{S^*-\bar S}{S^*}\ln\frac{S^*-\bar S}{S_c-\bar S}-1\right]$。因此图~\ref{fig:c0-phase} 的 $\Delta t$ 脊线是该解析判据在 $(c_0,\theta)$ 平面上的零集，而非仅由数值极值定位。因此 $c_0$ 与 $\theta$ 虽同为作用于 $q_{\max}$、峰值的同向杠杆，在 $\Delta t$ 上却有定性差别。

清零时刻是唯一全程单调的时间量。 $t_{\rm end}=t_1+\Delta t+\text{尾段}$ 三项中，
$t_1$ 与尾段都随 $c_0$ 增大而单调缩短，唯 $\Delta t$ 非单调；数值上前两项的降幅压倒后者，
故 $t_{\rm end}$ 在整个扫描区间内\emph{严格单调递减}（图~\ref{fig:c0-scan}(d)），
由 $c_0=3.43$ 处的约 $368$ 天降到 $c_0=30$ 的约 $102$ 天。
最能说明该压倒关系的是跨越 $\Delta t$ 脊的一段：由 $c_0=3.43$ 到 $6.61$，
$\Delta t$ 增加约 $73$ 天，但 $t_1$ 由 $211.7$ 天降到 $28.3$ 天（减少约 $183$ 天），
净效果仍是 $t_{\rm end}$ 下降约 $144$ 天。即在低接触环境中，
阈值控制"平台短但迟迟不清零"，代价并不在平台本身而在漫长的触发前与退出后两段。

成本与总累计的非单调性及其错位。 $J$ 与 $\Itcum$ 同样先升后降，但极值位置与 $\Delta t$ 脊并不重合：
$\Delta t$ 的脊在 $c_0=6.61$，$\Itcum$ 的极大在 $c_0=13.88$（$3610$），
$J$ 的极大在 $c_0=18.12$（$42.80$），三者依次右移。
$J$ 的非单调性来自式~\eqref{eq:dom:Jtheta} 中两项相反的作用：前因子 $1/(c_0\theta)$ 随 $c_0$ 下降，
而 $q_{\max}$ 升高、且 $s_c\propto1/c_0$ 下移使积分区间 $[s_c,s^\ast]$ 拉长；
$c_0\lesssim18$ 时后者主导，之后前因子主导。
一个实用的推论是：在 $c_0\in[10,25]$ 内 $J$ 仅在 $36$–$43$ 之间变动，
即高接触环境下阈值控制的成本对 $c_0$ 相当不敏感。
另值得一提，西安基线 $c_0=12.8872$ 几乎正落在 $\Itcum$ 的极大点上
（$3608.6$ 对极大值 $3610.2$，相差 $0.04\%$）：在本节的单参数切片内，
接触率无论升降，到清零的总累计感染都只会下降。这三处极值的错位说明，
$c_0$ 不存在一个同时优化时长、成本与累计的取值。

图~\ref{fig:c0-phase} 把上述结构统一到 $(\theta,c_0)$ 平面：触发边界、拐点边界 $s^*=2\bar s$ 与 $\Delta t$ 脊线三条曲线划分出各结构区，图~\ref{fig:c0-panel} 的三个代表 $c_0$ 正是这三条边界与 $\theta=0.002$ 水平线的交点。需强调本图只报告阈值控制律的结构量，不含占优边界。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.72\textwidth]{c0_sensitivity_phase.pdf}
    \caption{$(\theta,c_0)$ 平面上阈值控制律的结构相图。三条曲线为触发边界 $c_{0,\mathrm{trig}}(\theta)$、内部拐点边界 $s^*=2\bar s$ 与控制时长脊线 $\partial\Delta t/\partial c_0=0$，灰色等值线为 $\Delta t$（天）。图~\ref{fig:c0-panel} 的三个代表 $c_0$ 为三条边界与 $\theta=0.002$（点线）的交点，同色圆点标出。本图仅报告结构量，不含占优边界，亦不画 $J$ 或 $\eta=I^{\rm T}_{\rm peak}$ 参照线。}
    \label{fig:c0-phase}
\end{figure}


\section{讨论与局限}
\label{sec:discussion}

本文在上述扩展 SIR／SIQR 型框架下给出了情景一阈值控制的闭式结构：启动点 $S^\ast$ 的 Lambert $W$ 表达、
平台期开环控制律 $q_c(t)$、控制时长 $\Delta t$、清零时刻 $t_{\rm end}$ 与总累计感染的三段公式，
并在西安数据上与 TDINN 控制、常规控制作了条件性比较。以下按“结论成立条件—参数依赖—
边界情形—可改进方向”整理。

\paragraph{权衡结构。}
比较结果是\emph{指标相对}的，不存在单一意义上的优劣。在全市人口口径且取代表阈值
$\eta=0.002N$ 时，阈值控制把社区感染者
峰值精确压到 $\eta$ 且完全不降低接触率（$J_c=0$），二次加权成本反低于 TDINN
（$40.77$ 对 $49.35$）；但因平台期贴边维持、易感者消耗缓慢，控制时长与清零时刻显著拉长，
总累计感染也远高。这一权衡的来源在式~\eqref{eq:dom:Jtheta} 与 $\Delta t\approx\text{const}/(c_0\theta)$
中是显式的：降低 $\eta$ 同时降低峰值与提高时间代价，二者由同一个 $\theta$ 控制，无法解耦。

\paragraph{尺度结构与两条杠杆。}
引理~\ref{lem:dom:scaling} 表明，
在固定归一化初值的理论口径下，
$\Delta t,J,q_{\max}$ 与峰值分数
只经 $\theta=\eta/N$ 进入。
因此，提高 $\eta$ 与降低 $N_{\rm eff}$
是作用于这些强度量的两条同向杠杆。

二者对完整占优区域并非完全等价：
提高 $\eta$ 会同时逼近固定绝对峰值上界
$I_{\rm peak}^{\rm T}$，
而降低 $N_{\rm eff}$ 不改变给定平台高度；
累计感染和清零时刻还带有绝对尺度。
在本文全程采用的固定绝对初值单一口径下，
强度量的 $\theta$ 不变性成立到 $O(i_0/\theta)\lesssim8\times10^{-5}$。
注记~\ref{rem:dom:dichotomy} 的直线族／弧线族二分
正是上述区别的几何表现。

\paragraph{开环控制的脆弱性。}
$q_c(t)$ 按理论 $S_{\rm th}(t)$ 构造，是时间开环函数而非状态反馈。其代价是对模型误差与
参数偏移没有自校正能力：若真实 $S(t)$ 偏离理论轨迹，$I(t)$ 将偏离阈值 $\eta$ 而控制律不会察觉。
本文未评估该敏感性，也未引入观测噪声或参数不确定性。设计带反馈修正的变体、
并比较其在模型失配下的稳健性，是直接的下一步。

\paragraph{有效人口的解释限度。}
$N_{\rm eff}$ 是该次疫情的未知规模参数，而非可自由调节以偏袒某策略的旋钮。其物理实现分两类：
天然有界单元（校园、厂区、邮轮）与封控造出的小池。后者带来一个更尖锐的问题：
$N_{\rm eff}$ 是\emph{在实际干预（含大幅接触率下调）之下}的有效混合规模，
而阈值控制的前提恰是不下调接触率，其面对的池应更大。
故第~\ref{sec:dominance}~节的区间宜读作必要条件，其偏差方向已知
（第~\ref{sec:dom:limits}~节 (2b)）——这是当前结论最薄弱的一环。

\paragraph{与 TDINN 比较的口径。}
西安疫情的真实易感规模 $N_{\rm eff}$ 未知，全市常住人口只是数据所限下的代用上界。
本文因此把 $N_{\rm eff}$ 当作待定参数，给出使阈值控制不劣的取值区间
$[1.06\times10^{4},\,9.17\times10^{4}]$：下端由观测经解析恒等式反推，
上端由峰值与成本两项性能条件给出。其中 $I_{\rm peak}^{\rm T},\Itcum^{\rm T},t_{\rm end}^{\rm T}$
由数据锚定、对 $N$ 稳健，而 $J^{\rm T}$ 经 $S/N$ 带一层未予定量的 $N$ 依赖
（第~\ref{sec:dom:limits}~节 (2)）。

\paragraph{未来工作。}
（i）\emph{独立估计 $N_{\rm eff}$}：用接触调查、手机信令／出行数据或封控单元规模等外部信息，
独立估计西安此次疫情的有效混合规模，检验其是否落在上述区间内。
这是把本文的条件性结论转为经验结论的直接途径；
（ii）\emph{定量 $J^{\rm T}$ 的 $N$ 依赖}：把 TDINN 控制重标定到候选 $N_{\rm eff}$
（可用 $S/N$ 的闭式修正或全参数重拟合），给出区间上端 $N^\ast$ 的误差棒；
（iii）在情景二、情景三（同时调节 $c$ 与 $q$）下重做同一套尺度分析，检验二分结构是否保持；
（iv）多城多病验证：在另一城市／病种的平行数据上重做本套分析，可检验 $\theta$ 阈值与 $\qinf$ 等结构不变量的可移植性；
（v）引入观测噪声与参数不确定性，评估开环控制律的稳健边界。

\clearpage
\appendix
\section{不同有效人口下的 $\eta$ 杠杆补充图}
\label{app:panel_eta_N}

本附录固定三条由引理~\ref{lem:dom:scaling} 的理论口径得到的
$\theta$ 参照线：
\[
\theta_{\rm dur150}=1.137\times10^{-3},\qquad
\theta_{\rm cost}=1.656\times10^{-3},\qquad
\theta_{\rm dur45}=3.762\times10^{-3}.
\]
绘图与正文采用同一口径：归一化初值 $(s_0,i_0)$ 跨 $N_{\rm eff}$ 固定、不重新拟合，
故上述三条线即各 $N_{\rm eff}$ 上\emph{精确}的等成本／等时长边界，
不存在需要另行说明的漂移。

各平台上的同色实心圆是 $q_c(t)$ 拐点时刻在 $I(t)$ 上的投影，而非
$I(t)$ 自身拐点。
$\lambda\approx0.861$
与各参照线上的
$t_2-t_{\rm inf}$
不随 $N_{\rm eff}$ 改变；
启动时间 $t_1$ 则随 $N_{\rm eff}$ 变（$i_0=I_0/N$ 随 $N$ 变），
故绝对拐点时刻 $t_{\rm inf}$ 在不同 $N_{\rm eff}$ 图之间平移，不作对齐。
右上 inset 均从零起算，
但正文图~\ref{fig:panel_eta_lever} 与以下三图的纵轴上限分别按各自累计量级自适应；
跨图比较应读取纵轴刻度与柱顶数值，不能直接比较柱高。

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/fig_panel_A_N1e4.pdf}
  \caption{$N_{\rm eff}=10^4$ 时的 $\eta$ 杠杆。三条边界参照线到动态清零的总累计感染分别为
  $1746.5$、$1781.8$ 与 $1884.8$，均低于 TDINN 全市固定拟合参照 $2096.76$；但其动态清零时间
  分别约为 $224.3$、$172.7$ 与 $105.2$ d，仍显著长于 TDINN 的 $45.27$ d。因此这里只能说
  总累计感染这一单项指标较低，不能推出综合占优。另因 $N_{\rm eff}=10^4$ 已略低于
  最小相容易感池 $N_{\rm floor}=1.06\times10^{4}$，本图仅作尺度结构的边界示例。}
  \label{fig:panel_eta_N1e4}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/fig_panel_A_N40377.pdf}
  \caption{$N_{\rm eff}=40{,}377\approx N^\ast(45\mathrm d)$ 时的 $\eta$ 杠杆。dur 45 d 参照线的平台
  高度 $\eta=151.898$，数值上近似达到 TDINN 峰值参照
  $I_{\rm peak}^{\rm T}=151.90$，对应峰值约束的临界情形；dur 150 d 与 cost 平台仍位于
  该参照线下方。}
  \label{fig:panel_eta_N40377}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/fig_panel_A_N91727.pdf}
  \caption{$N_{\rm eff}=91{,}727\approx N^\ast_\infty$ 时的 $\eta$ 杠杆。cost 参照线的平台
  高度 $\eta=151.900$，数值上近似达到 $I_{\rm peak}^{\rm T}=151.90$；dur 45 d 平台
  $\eta=345.077$ 已超过该参照，故峰值约束在该边界上不再成立，而 dur 150 d 平台仍低于参照。}
  \label{fig:panel_eta_N91727}
\end{figure}

\clearpage

\section{接触率通道的连续扫描}
\label{app:c0_scan}

本附录给出第~\ref{sec:dom:c0}~节 $c_0$ 通道的连续扫描全貌。它与正文图~\ref{fig:xian_heatmaps} 的
$(\eta,c_0)$ 景观在 $\theta=\eta/N$ 上有重叠，此处为固定池规模 $N_{\rm eff}=2\times10^4$、$\theta=0.002$
下 $c_0$ 通道的一维完整对照。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{c0_sensitivity_scan.pdf}
    \caption{固定 $N_{\rm eff}=2\times10^4$、$\theta=0.002$ 下阈值控制指标随背景接触率 $c_0$ 的连续扫描。
    上排为时间分解，三者相加即第四个：$(a)$~启动时间 $t_1$、$(b)$~平台时长 $\Delta t$、
    $(c)$~解除控制后的尾段、$(d)$~清零时刻 $t_{\rm end}=t_1+\Delta t+\text{尾段}$。
    下排为结构量与结局量：$(e)$~$q_{\max}$、$(f)$~拐点相对位置 $\lambda$、
    $(g)$~二次加权成本 $J$、$(h)$~到动态清零的总累计感染 $\Itcum$。
    灰色竖线／阴影标出触发边界 $c_0=3.334$、拐点出现边界 $c_0=3.543$ 与 $\Delta t$ 脊 $c_0=6.607$；
    同色圆点为图~\ref{fig:c0-panel} 的五个代表 $c_0$。
    四个时间量中只有 $\Delta t$ 非单调，其余三个及 $t_{\rm end}$ 全程单调递减；
    $J$ 与 $\Itcum$ 各自在 $c_0=18.1$ 与 $13.9$ 处取极大后缓降。
    $J$ 与 $\Itcum$ 仅作该池规模下的结构结果，不叠加任何 TDINN 参照线。}
    \label{fig:c0-scan}
\end{figure}



\section{拐点内部存在性的 $(c_0,\beta)$ 统一图}
\label{app:c0_beta}

本附录把第~\ref{sec:s1:inflection-position}~节的内部拐点存在判据 $s_c<2\bar s<s^*$ 画在 $(c_0,\beta)$ 平面上，作为图~\ref{fig:s1:inflection-landscape} 沿 $\beta$、$c_0$ 两行的二维统一；$\beta$ 在此为病原情景变量，其余口径（$\theta=0.002$、$q_0=0.3230$、固定绝对初值）与正文一致。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\textwidth]{c0_beta_existence.pdf}
    \caption{$(c_0,\beta)$ 平面上内部拐点的存在域（填色）：由上界 $2\bar s=s^*$（$t_1$ 端，实线，随 $c_0,\beta$ 弯曲）与下界 $\beta_{\max}=0.2614$（$t_2$ 端，水平虚线，只由 $\beta,q_0$ 定）围合而成。西安参数 $(c_0,\beta)=(12.8872,0.1498)$ 落在域内；灰色点线标出图~\ref{fig:c0-phase} 的 $c_0$ 截面所在 $\beta$。纯存在性；$\beta$ 为病原情景变量，$\theta$、$q_0$ 固定；不作任何策略比较，无 TDINN。}
    \label{fig:c0-beta-existence}
\end{figure}

\clearpage

% ===== 参考文献（Step 6 补全文献综述引用；当前仅含正文已直接引用的条目） =====
\printbibliography



\end{document}

