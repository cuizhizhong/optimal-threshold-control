# AGENTS.md

## 用途

本文件记录 `E:\work\draft` 项目的总体研究上下文、论文草稿写作口径和协作规范。后续处理本项目时，应先遵守本文件；进入具体子目录后，再同时遵守该子目录下的 `AGENTS.md`。

当前项目仍处于理论分析、数值实验和论文草稿整理阶段。不要急于下结论，也不要急于证明某个控制策略一定优于另一个策略。当前更重要的任务是把模型假设、控制律推导、参数依赖、数值现象和局限性整理清楚。

当前主论文工程为：

- `paper_elegantpaper_relayout/flatten_curve_analysis_cn.tex`
- `paper_elegantpaper_relayout/flatten_curve_analysis_cn.pdf`
- `paper_elegantpaper_relayout/elegantpaper.cls`



当前重要实验模块包括：

- `code/`：情景一、情景二等 MATLAB 数值模拟；
- `xian_control_comparison/`：西安 TDINN 控制、情景一阈值控制和常规控制的比较实验；
- `hainan_control_comparison/`：海南疫情控制比较实验，结构类似西安模块，但当前重点仍以西安阈值控制分析为主；
- `真实数据/`：原始数据文件目录，处理数据时优先从这里读取，不要直接修改原始数据文件；
- `scenario1_inflection/`：情景一 $q_c(t)$ 拐点分析的计算与配图模块（Python），产出主论文 §4.3/§4.4/§6 的三张拐点图；含修改记录 `README.md`；
- `xian_dom/`：§8 有效人口占优分析的绘图与求解脚本（Python），产出主论文 §8.7 的三张占优图（图 20–22）；含 `README.md`。

其中，西安比较的具体执行细则由 `xian_control_comparison/AGENTS.md` 管理；根目录只保留总原则和论文草稿层面的上下文。

若根目录规范与子目录 `AGENTS.md` 对同一问题给出不同细节，以子目录 `AGENTS.md` 为准；根目录只提供总研究口径、术语和编辑原则。

## 近期进展与修改记录

- **2026-08-29 §8 第二轮：审阅意见修订 + 术语/篇幅（已完成并编译通过，54 页，无未定义引用）**：（一）**两处逻辑修订**。①式~\eqref{eq:dom:main-interval} 的端点开闭：命题 8.4 的 (i) 原为严格 $\eta<I_{\rm peak}^{\rm T}$，与主结论的闭区间 $[N_{\rm floor},N^\ast_\infty]$ 冲突；现放松为 $\eta\le I_{\rm peak}^{\rm T}$——本节讲的是「不劣」，(ii)–(v) 本就非严格，放松后五项一律非严格、命题内部自洽，闭区间即正确。连带改到八处（式 `eq:dom:band` 拆掉混合开闭的 `min`、`eq:dom:Nstar` 改 $N\le N^\ast$、推论 8.6 改 $N\le\min\{\cdot\}$、$\mathcal{W}_{\rm pcd}$ 定义、四条线的占优侧、§7 与注记 8.8 的两处转述）；顶点 $(N^\ast,I_{\rm peak}^{\rm T})$ 现**属于**占优集合。**触发条件 $\eta/N<i_{\max}^{no}$ 保持严格**（可行性要求，非「不劣」比较）。②**$J^{\rm T}$ 的 $N$ 依赖**：原写「传播项经 $S/N$ 携带 $N$ 依赖」是错的，且与 §5.1「该泛函的被积函数为归一化控制强度，不含人口规模 $N$」直接矛盾——$J$ 的被积函数只有 $c(t),q(t)$。现改用**纯基准解读**：TDINN 四个参照量都是已发生疫情的固定经验参照，不随 $N_{\rm eff}$ 反事实重算；成本公式新加 `\label{eq:cost:J}` 供 §8 引用，表 10 删掉「是否随 $N_{\rm eff}$ 变」一列（四行全「否」），§8.8「参照量对 $N_{\rm eff}$ 的依赖」整段与 §9 未来工作 (ii) 删除（其余顺移）。（二）**§8.7 两处订正**：「$c_0$ 与 $\theta$ 同为作用于 $q_{\max}$ 与峰值的同向杠杆」符号写反——论文自己已给出 $(q_{\max})_\eta<0$ 与 $(q_{\max})_{c_0}>0$（命题~\ref{prop:s1:sensitivity}），且平台高度在该切片中恒为 $\eta$、不随 $c_0$ 变；改写为「二者都改变控制结构，但作用方向与单调性不同」。$t_{\rm end}$「严格单调递减」降级为数值观察（「数值扫描显示，在本文参数范围内……本文未证明它在一般参数下成立」）。（三）**术语**：标度／尺度不变性 → **规模不变性**（引理 8.1 改名「对人口规模 $N$ 的不变性」，§8/§8.2/§8.5 标题、§7/§9/附录的「尺度结构」同步）；峰值分数／累计分数 → **峰值比例／累计比例**；「只经 $\theta$ 进入」→**「只与 $\theta$ 有关」**（14 处）；接触率通道 → **接触率 $c_0$ 的敏感性分析**（§8.7 与附录标题）；绝对尺度 → 绝对规模。全文「尺度」只剩「全市尺度／时间尺度」两种用法。（四）**新增附录 B「$\beta$ 稳健性与两条弧线边界的数值细节」**（原附录 B/C 顺移为 C/D，图 28/29 顺移为 29/30），把 β 扫描与两条弧的端点、逐点取值、16 点唯一性论证移入；正文只留结论（区间宽度对 β 稳健、纳入累计收缩到 $[1.06,1.18]\times10^4$、清零占优区为空集）。§8 正文 8715→8349 字（为改写前 9373 字的 89%）。**逐数字对账：无数字丢失，仅新增 $i_{\max}^{no}N^\ast_\infty\approx9.6\times10^3$ 一处（顶点归属的核算）。**（五）**§8 开头三处措辞**：①节引言末句由「不是把某一策略『搬到』不同规模的人群上……本就处于同一量纲」改为正面陈述（说清 $I_{\rm peak}^{\rm T}$ 与 $\eta$ 各是什么、都是社区感染者的绝对人数、改变 $N_{\rm eff}$ 意味着重解反事实轨迹）；②§8.1 初值设定去掉「两条臂／播种／种子」等借喻（§8.8 两处同源用词一并改为「两种策略」「同一组初值」），把主要理由（初始感染人数是疫情本身的属性、与假设的池大小无关）提到明面，并把原来七个字的「其绝对时刻也无法与……TDINN 参照对齐」展开为具体所指——图 21／22 把阈值控制轨迹与固定的 TDINN 轨迹画在同一时间轴上，初始条件不同则横轴时刻无从对照；③**修正一处表述不准确**：原写「$i_0$ 与 $\theta$ 只以组合 $(\theta-i_0)$ 出现，故……相对量级恰为 $i_0/\theta$」，但本文取 $S_0=N-I_0$ 即 $s_0=1-i_0$（`xian_dom/caliber.py:78`），$i_0$ 经 $s_0$ 再进入一次（该通道扰动 $(\gamma-B_2)i_0/B_1\approx-0.19\,i_0$，约为主通道的五分之一），且各指标传递时各带 $O(1)$ 弹性，故改为「主要以差 $(\theta-i_0)$ 进入……相对误差为 $O(i_0/\theta)$」，与全文其余四处的 $O(i_0/\theta)$ 一致。本轮不增删任何数字。（六）**收回过量的 `\paragraph`**：上一步把 §8 的行内小标题由 3 个加到 21 个（而 §1–§7 各为 0、§9 为 6），等于把连续论证切成了带标签的清单——与本次改写要消除的「罗列感」同源。现收到 6 个，只保留 §8.8 的小结与五条局限（与 §9 的用法对齐）；§8.1（2）、§8.2（1）、§8.5（3）、§8.6（5）、§8.7（4）共 15 处改为连续行文，逐处补了过渡句而非简单删标题（如「第一组实验取同比例阈值……」「以上只用了峰值与成本两项。再纳入……区间还会进一步收窄」「四个时间量中，只有清零时刻在整个扫描区间上保持单调」）。附录 B 的 4 个保留——那里本就是并列的数据条目。本步不动任何数字，53 页。

**写作约定（后续沿用）**：行内小标题只用于真正并列、可逐条对照的条目（局限、并列实验、附录数据块）；论证主线一律连续行文，靠过渡句推进，不加标题。

- **2026-08-28 §8 学术化重构（已完成并编译通过，53 页，无未定义引用）**：全节由「理论—数字—局限—图」改排为「设定—理论—数值图像—图像分析—小结/局限」。新小节序：8.1 设定与比较基准（新增**表 10** TDINN 四参照量：值／来源／是否随 $N_{\rm eff}$ 变）、8.2 标度不变性与单调性、8.3 有效人口的下界与占优区域、8.4 总累计与清零的附加约束、8.5 标度不变性的数值验证、8.6 西安参数下的占优区域（旧 8.5+8.7 合并，新增**表 11** θ 与临界人口一览，图 20/21/22 回到结果流程中）、8.7 接触率通道、8.8 小结与局限。旧「最小相容易感池」段升格为带证明的**命题 8.3**，定理号变为 引理 8.1/8.2、命题 8.3/8.4、推论 8.5/8.6/8.7、注记 8.8。图注只保留识别信息，分析搬进正文（图 21 caption 37→18 行、图 22 34→16 行）。**术语去自造化**：强度量／广延量 → 「只依赖 $\theta$ 的指标」／「带绝对规模的指标」，结构不变量 → 拐点高度的不变性，最小相容易感池 → 有效人口下界；并去掉 部分识别／楔形（正文）／骨架线／坍缩／联动／脊／反演／内生性／触发域墙，「口径」由 56 处降到 6 处（均在 §8 外）。**格式**：§8 内 `\textbf` 归零，粗体行内小标题改 `\paragraph`，局限的 `(1)(2)(2b)` 行内编号改命名段落，引号统一全角。**修掉三处实质错误**：前言同时声称「固定归一化初值」与「固定绝对初值」（7fbf8ba→1f5d2b9 的残留，统一为绝对初值，归一化只作引理 8.1 的假设）；累计弧穿成本线的 $11764$ 与 $11762$ 不一致（统一 $11762$）；$N^\ast_{\rm cum,\infty}$ 的 $1.17\times10^4$ 与区间上端 $1.18\times10^4$ 矛盾（统一 $1.18\times10^4$）。§9 与附录 A 同步术语，附录 A 的同一处初值口径矛盾一并修掉。逐数字对账：旧 §8 的全部数字在新 §8 中均在场，例外仅为上述被修正的三个，且无新增数字。§8 字数 9373→8715、仍 14 页（p33–46）。

- **2026-07-24 §8「尺度不变性与有效人口占优」两轮修订（已完成并编译通过，39 页，无未定义引用）**：（一）**归一化记号统一**：§8 全节改为大写=人数、小写=分数（$\sigma_c\to s_c$、$\bar\sigma\to\bar s$、$\iota\to i_{\max}^{no}$、写全的 $S^\ast/N\to s^*$），并修复 `elegantpaper.cls` 将 `corollary`/`remark` 定义为 `\newtheorem*`（无编号）导致 `\label`/`\ref` 捕获杂散计数器、两条推论撞号的问题——在导言区新增带编号的 `corollaryn`/`remarkn` 环境（与定理共用 `theorem` 计数器）。（二）**占优分析重构**：TDINN 基准口径由旧稿"逐 $N$ 只重拟合 $I_0$ 时绝对轨迹近似不变"（数值上不成立，$N\sim10^3$–$10^4$ 漂移 50–114%）改为"固定的现实参照"——$I_{\rm peak}^{\rm T}=151.90$、$J^{\rm T}=49.35$、$I_{t\rm cum}^{\rm T}=2096.76$、$t_{\rm end}^{\rm T}=45.27$ d，发生过的事用固定数、反事实随 $N$ 变；§8.4 新增清零占优推论（**推论 8.6** `cor:clr`）与强度量/广延量二分注记（**注记 8.7** `rem:dom:dichotomy`）；§8.5/§8.6 数值更新与局限重写；§8.7 换成三张零缩放新图（图 20 `fig_dom_combined` + 图 21/22 Panel A/B，均按 `\textwidth`=451.28 bp 渲染）；**§9 讨论与局限**起草。占优分析的绘图/求解脚本置于新建 `xian_dom/`（见其 `README.md`）。

- **2026-07-21 情景一 $q_c(t)$ 拐点分析完整化（已完成并编译通过）**：在原拐点定理（现 **定理 4.3**）基础上补齐三块——拐点位置与两端消失（新 **§4.3**）、曲率幅度与"偏离首尾直线"非单调性（新 **§4.4**）、诊断图（**§6** 图 4）；记号统一新增 $\qinf:=q_c(2\bar S)=1-\frac{1}{2(1-\beta)}$，把 §8.2 原未定义的 $q^\star$ 全部改为 $\qinf$。新增两小节使原 §4.3–§4.5（Ṡ与t₂、清零时间、总累计）顺移为 **§4.5–§4.7**（正文全用 `\ref`，无硬编码节号）。计算与配图代码、数值锚点、逐条改动、遗留项详见 `scenario1_inflection/README.md`。

## 论文草稿总体思路

本项目研究 SIQR 模型下的疫情控制问题，重点关注医疗容量阈值约束下不同控制方式的理论结构和数值表现。并且该项目现在现在主要讨论情景一,先不关心情景二和情景三。

总体写作逻辑应保持为：

1. 从模型设定和医疗阈值约束出发；
2. 分情景推导控制启动时间、维持期控制律、状态轨迹和控制解除条件；
3. 通过参数敏感性分析观察控制律、控制时长、清零时间和累计感染规模如何随参数变化；
4. 通过数值模拟检验理论控制律是否符合推导结果；
5. 再与 TDINN 控制、阈值控制、常规控制等参照策略进行比较；
6. 最后讨论不同策略的成立条件、代价结构、参数依赖和局限。

对 TDINN 控制、阈值控制、常规控制的比较目前只作为数值实验和机制分析，不写成最终胜负判断。应避免把阶段性结果表述为“已经证明阈值控制更好”。

## 写作风格

默认采用“数学博士生研究笔记/论文草稿”风格。

具体要求：

- 不写泛泛综述腔，不使用“深刻揭示”“系统性”“多维度”“重要意义”“赋能”等空泛表达。
- 专业术语必须绑定具体数学对象，例如变量、参数、方程、平衡点、阈值、`R0`、Lyapunov 函数、目标泛函、协态方程或控制约束。
- 默认按“问题—假设—模型—分析—解释—局限—下一步”的逻辑组织内容。
- 对模型假设要说明建模动机、数学后果和可能失效的情形。
- 结论不要写满，要说明成立条件、参数依赖、边界情况和可改进方向。
- 写作语气克制，像研究者在整理思路，而不是宣传式综述。
- 先写模型、公式、指标和数值现象，再给谨慎解释。
- 对尚未确定的结论，写成观察、问题或待检验假设。

推荐表达：

- “在当前参数设定下，数值结果显示……”
- “该现象提示……但仍依赖于……”
- “从公式可以看出，主要影响来自……”
- “这一结论成立的前提是……”
- “该设定可能在……情形下失效。”

避免表达：

- “本文深刻揭示……”
- “该方法显著优于所有现有方法……”
- “系统性证明了控制策略的优越性……”
- “具有重要现实意义……”但没有绑定具体指标或数学对象。

## 核心术语与符号

统一使用以下控制策略名称：

- `情景一阈值控制`：情景一中固定 `c(t)=c0`，通过调节 `q(t)` 维持阈值约束的控制；
- `TDINN控制`：来自 He--Tang--Xiao (2023) 论文学习得到的控制函数；
- `常规控制`：反事实基准，满足 `c(t)=c0` 且 `q(t)=q0`。

不要使用：

- `固定常规控制`
- 用 `无控制` 指代 `常规控制`
- `现实控制`
- `文章学习的现实控制`
- `活动感染者`

仓室和数据符号：

- `I(t)`：社区感染者；
- `I_new(t)`：新增社区感染者；
- `I_q(t)`：隔离感染者；
- `I_{q_{new}}(t)`：新增隔离感染者；
- `I_cum(t)`：累计社区感染；
- `I_{q_{cum}}(t)`：累计隔离感染；
- `I_{t_{cum}}(t)=I_cum(t)+I_{q_{cum}}(t)`：总累计感染。

如果使用基本再生数，正文中统一写作 `R_0`；代码变量名可按语言规范使用 `R0`。

## 情景一阈值控制规则

情景一阈值控制必须保持为理论推导得到的时间开环控制，除非用户明确要求设计新的反馈控制。

控制函数为：

```tex
q_c(t)=1-\frac{\gamma N}{\beta c_0 S_{\rm th}(t)}.
```

其中 `S_th(t)` 是在计算 `S^*`、`t_1` 后得到的理论时间轨迹。

重要规则：

- `q_c(t)` 必须是关于时间 `t` 的开环函数；
- 不要把 `q_c(t)` 写成依赖数值积分实时 `S(t)` 的状态反馈函数；
- 可以通过数值求根得到 `S^*`，这与理论公式等价；
- 数值模拟应检查平台期内 `I(t)` 是否沿阈值 `eta` 运行。

理论阶段为：

1. 常规控制直到 `I(t)=eta`，得到启动时间 `t_1`；
2. 平台控制阶段，使 `I(t)=eta`；
3. 当 `S(t)` 到达 `S_c` 后回到常规控制；
4. 继续模拟到动态清零，即 `I(t)<=1`。

## 控制成本指标

主要成本指标统一记为：

```tex
J
=
\int_0^T
\left[
w_c\left(\frac{(c_0-c(t))_+}{c_0}\right)^2
+
w_q\left(\frac{(q(t)-q_0)_+}{1-q_0}\right)^2
\right]dt.
```

默认权重为：

```tex
w_c=1,\qquad w_q=2.
```

单独的归一化分项成本只作为参考指标保留：

```tex
J_c=\int_0^T \frac{(c_0-c(t))_+}{c_0}\,dt,
```

```tex
J_q=\int_0^T \frac{(q(t)-q_0)_+}{1-q_0}\,dt.
```

其中 `(x)_+=max(x,0)`。不要把 `J_c+J_q` 作为综合成本，也不再使用旧的线性综合指标。`J_c` 和 `J_q` 只用于判断控制投入主要来自接触率降低还是隔离率提高；策略之间的主要成本比较使用二次加权总成本 `J`。

## 数值实验原则

数值实验的目标是观察控制策略的行为边界，而不是只筛选有利结果。

报告结果时优先说明：

- 参数取值；
- 控制启动时间；
- 控制解除时间；
- 控制时长；
- 清零时间；
- 感染峰值；
- 累计感染；
- 控制强度或控制成本；
- 结论成立的参数范围。

探索性结果应单独保存，使用清晰目录或后缀，不覆盖主基准结果。例如：

- `_eta100`
- `_linear`
- `_symlog`
- `threshold_landscape_analysis/`

低阈值长期控制不应直接写成“不合理”。在实验阶段，它可以作为长期低感染水平维持或常态化管控的一种数学情形。是否现实可行，需要结合控制时间、清零时间、累计感染和成本函数进一步判断。

## 西安比较模块

`xian_control_comparison/` 是当前重要实验模块，但其具体规则不在根目录重复维护。

进入该目录处理问题时，应优先阅读：

- `xian_control_comparison/AGENTS.md`

根目录只保留以下总原则：

- 不急于证明情景一阈值控制优于 TDINN 控制；
- 先系统刻画不同 `eta`、成本函数和 `c0` 下的数值现象；
- TDINN 控制、情景一阈值控制和常规控制的比较应写成条件性比较；
- 不用探索性低阈值结果覆盖主基准结果。

## 常用命令

从项目根目录重新生成西安输出：

```powershell
python -B xian_control_comparison\xian_control_comparison.py
```

在西安比较目录下编译西安比较文档：

```powershell
xelatex -interaction=nonstopmode xian_control_comparison.tex
xelatex -interaction=nonstopmode xian_control_comparison.tex
```

从项目根目录编译主论文（使用 biblatex/biber 管理参考文献，需跑 biber）：

```powershell
cd paper_elegantpaper_relayout
xelatex -interaction=nonstopmode flatten_curve_analysis_cn.tex
biber flatten_curve_analysis_cn
xelatex -interaction=nonstopmode flatten_curve_analysis_cn.tex
xelatex -interaction=nonstopmode flatten_curve_analysis_cn.tex
```

主论文有 live citations（`references.bib` + `\printbibliography`），只跑两遍 xelatex 会使 `\cite` 无法解析（显示 `[?]`）。改图、表、引用、标签或公式编号后按上述四步（xelatex→biber→xelatex→xelatex）重编；仅微调正文且未动引用时，两遍 xelatex 即可。（西安比较文档 `xian_control_comparison.tex` 用内嵌 `thebibliography`，不需要 biber，仍是两遍。用 PowerShell 跑 xelatex，不要用 Git Bash——后者会触发 `fwrite: Invalid argument`。）

## 编辑规则

- 保持修改范围小；
- 不要用探索性结果覆盖主基准输出；
- 不要修改历史备份目录，除非用户明确要求；
- 论文正文、公式编号、模板样式和 PDF 验证默认以 `paper_elegantpaper_relayout/flatten_curve_analysis_cn.tex` 为准；不要再同步修改根目录旧稿；
- 修改代码、图例、CSV、LaTeX 表格和正文时，术语必须一致；
- 手工编辑文件使用 `apply_patch`；
- 不要用 shell 写文件技巧修改源文件；
- 如果只是理论和数值实验阶段，回答中不要强行写论文最终结论。
