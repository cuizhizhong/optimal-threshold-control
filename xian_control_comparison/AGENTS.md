# AGENTS.md - 西安控制比较下一阶段协作规范

## 用途

本文件夹用于西安疫情控制策略的理论与数值分析，主要比较三类策略：

- `TDINN控制`：来自 He--Tang--Xiao (2023) 论文学习得到的控制函数；
- `情景一阈值控制`：由本文情景一理论推导得到的阈值控制；
- `常规控制`：反事实基准，满足 `c(t)=c0` 且 `q(t)=q0`。

本文件是西安控制比较问题的局部上下文记忆。处理本文件夹内容时，应同时遵守项目根目录 `AGENTS.md` 和本文件；若涉及西安比较的具体口径，以本文件为准。

当前阶段不是正式写论文结论，而是继续做理论分析、数值实验和图像比较。不要急于证明某个控制策略一定更好，应先把不同参数、不同成本函数和不同控制设定下的数值现象计算清楚。

## 主要文件

主要源文件：

- `xian_control_comparison.py`
- `xian_control_comparison.tex`

主要生成结果：

- `xian_control_comparison.pdf`
- `xian_control_comparison_panels.pdf`
- `xian_control_cumulative.pdf`
- `xian_effective_reproduction_number.pdf`
- `xian_eta_sensitivity.pdf`
- `xian_control_comparison_results_table.tex`
- `xian_eta_sensitivity_table.tex`

低阈值探索目录：

- `low_eta_analysis/`

除非用户明确要求，不要修改历史备份文件夹，例如 `eta002_outputs`。

## 研究目标与当前状态

当前目标不是下结论“情景一阈值控制优于 TDINN 控制”。当前目标是构建单阈值控制的参数图谱，系统分析：

- 阈值 `eta` 如何影响启动时间、控制时长、清零时间和累计感染；
- 阈值 `eta` 如何影响隔离率轨迹 `q_c(t)` 和控制成本；
- 低阈值控制是否表现为长期常态化管控；
- 二次加权成本函数下，TDINN 控制、情景一阈值控制和常规控制的成本排序是否变化；
- 不同常规接触水平 `c0` 下，情景一阈值控制自身的行为边界。

低阈值导致控制时间很长时，不要直接判定其“不合理”。在实验阶段，应先把它作为一种可讨论情形保留：它可能对应长期低感染水平维持或常态化管控。是否现实可行，应在后续结合成本、持续时间和政策含义进一步判断。

目前更严谨的研究口径是：

> 先系统刻画单阈值控制在不同 `eta`、成本函数和 `c0` 下的行为边界，再讨论它相对于 TDINN 控制和常规控制的优劣条件。

## 写作风格

自然语言内容默认采用“数学博士生研究笔记/论文草稿”风格。

要求：

- 语言严谨、直接、可推导；
- 先陈述模型设定、指标定义和计算现象，再给出谨慎解释；
- 不写成宣传式结论；
- 避免“显著优于”“证明更好”“完全更合理”等过早结论；
- 可以使用“这说明”“数值上显示”“在当前设定下”“提示我们需要进一步比较”等研究笔记式表达；
- 对尚未确定的结论，写成问题、观察或待检验假设；
- 如果只是实验结果，不要强行上升为论文最终结论。

## 术语约定

统一使用：

- `TDINN控制`
- `情景一阈值控制`
- `常规控制`

不要使用：

- `固定常规控制`
- 用 `无控制` 指代 `常规控制`
- `现实控制`
- `文章学习的现实控制`
- `活动感染者`

仓室和数据符号：

- `I(t)`：社区感染者；
- `I_new(t)`：新增社区感染者；
- `I_q(t)`：隔离感染者；
- `I_{q_{new}}(t)`：新增隔离感染者；
- `I_cum(t)`：累计社区感染；
- `I_{q_{cum}}(t)`：累计隔离感染；
- `I_{t_{cum}}(t)=I_cum(t)+I_{q_{cum}}(t)`：总累计感染。

## 已经完成的分析

当前项目已经完成了一版西安三策略比较：

- `TDINN控制`；
- `情景一阈值控制`；
- `常规控制`，即 `c(t)=c0, q(t)=q0` 的反事实基准。

基准阈值为：

```tex
\eta = 0.002N = 26326.
```

已有主要结果包括：

- 真实每日新增病例堆叠图；
- 三策略主比较图；
- 累计社区感染、累计隔离感染和总累计感染图；
- 有效再生数图；
- `eta` 敏感性图；
- 三策略比较表；
- `low_eta_analysis/` 中的低阈值压力测试。

当前基准结果显示：TDINN 控制在峰值、累计感染和清零时间方面更强；情景一阈值控制主要体现为只调节隔离率、不降低接触率。成本比较以二次加权总成本 `J` 为主，`J_c` 和 `J_q` 只用于观察控制投入来源。不要把阶段性成本结果直接写成“情景一阈值控制更好”。

低阈值测试已经显示：降低 `eta` 会提前启动控制并降低峰值，但可能显著延长平台期和清零时间。这一现象需要作为下一阶段分析重点，而不是直接作为否定结果。

## 基准阈值

西安主分析的基准阈值为：

```tex
\eta = 0.002N = 26326.
```

这个阈值应理解为由医疗容量换算得到的“社区感染者阈值”，不是 ICU 床位数本身。

该值继续作为已有主基准保留，但下一阶段不再把它作为唯一展示重点。除非用户明确要求，不要用探索性阈值覆盖主基准结果。

不要把主分析阈值直接替换为：

- `0.02N`
- `0.05N`
- `eta=100`

`eta=100` 和其他低阈值目前作为探索性压力测试或阈值响应图谱的一部分，不作为主基准替代值。

## ICU 容量解释

模型中的阈值约束对象是社区感染者 `I(t)`，不是 ICU 患者数。因此若需要解释现实医疗容量，不要直接设：

```tex
\eta = B_{\rm ICU}.
```

更合理的换算方式是：

```tex
\eta_I = \frac{B_{\rm ICU}}{p_{\rm ICU}},
```

其中 `B_ICU` 是 ICU 容量，`p_ICU` 是感染者转为 ICU 需求的比例。

因此，520 或 1300 这类 ICU 床位数不能直接作为 `I(t)` 的最终现实阈值解释。

但当前阶段主要进行理论和数值实验。若用户要求先做低阈值扫描，可以暂时把 `eta=100,520,1300` 等作为数学阈值或压力测试点，不必在实验阶段立即完成现实含义解释。

## 情景一控制实现

除非用户明确要求设计新的控制策略，情景一阈值控制必须保持为理论推导得到的时间开环控制。

控制函数为：

```tex
q_c(t)=1-\frac{\gamma N}{\beta c_0 S_{\rm th}(t)}.
```

其中 `S_th(t)` 是在计算 `S_star` 和 `t1` 后得到的理论时间轨迹。

不要把 `q_c(t)` 写成使用数值积分中实时 `S(t)` 的状态反馈函数。

控制阶段为：

1. 常规控制直到 `I(t)=eta`，得到 `t1`；
2. 平台控制阶段，使 `I(t)=eta`；
3. 当 `S(t)` 到达 `S_c` 后回到常规控制；
4. 继续模拟到动态清零，即 `I(t)<=1`。

## 低阈值现象

一个常见质疑是：`eta=0.002N` 时控制启动较晚。这个判断是对的，因为阈值控制定义为 `I(t)` 达到 `eta` 后才启动。

降低 `eta` 后，必须同时观察启动时间、控制时长、清零时间、累计感染和控制成本，不能只看启动时间。

控制时长公式为：

```tex
\Delta t
=
\frac{N}{c_0\eta}
\log\frac{S^*-\bar S}{S_c-\bar S}.
```

当 `eta` 降低时：

- `t1` 会提前；
- `S_star` 会略微增大；
- 对数项会缓慢增大；
- 主导影响来自 `1/eta`；
- 控制时长和清零时间可能急剧增加。

典型结果：

- `eta=26326`：`t1≈16.90`，控制时长约 `85.07` 天，清零约 `258.11` 天；
- `eta=100`：`t1≈11.38`，控制时长约 `22523.29` 天，清零约 `23748.32` 天。

因此，降低阈值虽然能提前启动，但可能明显延长平台期。当前阶段不要直接把这种结果判定为“不合理”，而应把它作为低阈值长期控制或常态化控制的一种数值现象，继续结合成本函数和持续时间进行比较。

## 下一阶段分析路线

下一阶段优先继续分析单阈值控制，不主动推进双阈值控制，除非用户明确要求。

建议顺序：

1. 将 `low_eta_analysis/` 扩展为“单阈值控制响应图谱”；
2. 选取多个代表性阈值，生成类似图 2 的轨迹图；
3. 扫描 `eta`，分析启动时间、控制时长、清零时间、累计感染和控制成本随阈值变化的规律；
4. 引入二次加权控制成本；
5. 做成本权重敏感性；
6. 做 `(eta,c0)` 二维敏感性；
7. 根据单阈值控制的局限，再考虑是否进入情景三，即同时控制 `c(t)` 和 `q(t)`。

当前阶段不要为了得出结论而挑选参数。优先展示完整现象，再讨论可能解释。

## 阈值响应图谱建议

推荐扫描：

```tex
\eta\in[100,30000].
```

由于跨度较大，建议使用对数取点或对数横坐标。

推荐代表阈值：

```tex
100,\ 520,\ 1300,\ 3200,\ 6500,\ 15000,\ 26326.
```

这些阈值可用于展示：

- 极低阈值下的长期平台控制；
- 直接使用百位或千位阈值时的极端行为；
- 从长期常态化控制到较短期控制之间的过渡；
- 当前主基准 `eta=0.002N` 的结果。

代表性图像建议包含：

- `I(t)`；
- `I_new(t)`；
- `I_{q_{new}}(t)`；
- `q(t)`；
- 必要时加入 `c(t)` 和有效再生数 `R_e(t)`。

敏感性指标建议包含：

```tex
t_1,\quad
t_2,\quad
T_{\rm ctrl}=t_2-t_1,\quad
T_{\rm clear},
```

```tex
\max I,\quad
I_{t_{cum}},\quad
q_c(t_1),\quad
\overline q_c,\quad
J_q,\quad
J.
```

分析时先描述现象，例如：

```tex
\eta\downarrow
\Rightarrow
t_1\downarrow,\quad
T_{\rm ctrl}\uparrow,\quad
T_{\rm clear}\uparrow.
```

不要直接把这种变化写成“好”或“不好”。

## 有效再生数解释

有效再生数定义为：

```tex
R_e(t)=\frac{\beta c(t)(1-q(t))S(t)}{\gamma N}.
```

常规控制下初始值约为：

```tex
R_e(0)\approx 4.43>1.
```

因此，常规控制反事实下可以出现很高的感染峰值。这是模型给出的反事实结果，不表示 TDINN 拟合错误。

情景一阈值控制下：

- 控制启动前，由于 `S(t)` 几乎没有消耗，`R_e(t)` 维持在较高水平；
- 平台期内，设计的 `q_c(t)` 使 `R_e(t)≈1`；
- 平台结束后，`S(t)` 接近 `S_c`，因此 `R_e(t)<1`。

当阈值很低时，`q_c(t)` 下降缓慢，是因为 `I(t)=eta` 很小，导致 `S(t)` 消耗很慢。

## 控制成本指标

主要成本指标统一记为二次加权总成本：

```tex
J
=
\int_0^T
\left[
w_c\left(\frac{(c_0-c(t))_+}{c_0}\right)^2
+
w_q\left(\frac{(q(t)-q_0)_+}{1-q_0}\right)^2
\right]dt.
```

默认权重为：

```tex
w_c=1,\qquad w_q=2.
```

单独的归一化分项成本作为参考指标保留：

```tex
J_c=\int_0^T \frac{(c_0-c(t))_+}{c_0}\,dt,
```

```tex
J_q=\int_0^T \frac{(q(t)-q_0)_+}{1-q_0}\,dt.
```

其中 `(x)_+=max(x,0)`。不要把 `J_c+J_q` 作为综合成本，也不再使用旧的线性综合指标或平均日成本。低阈值控制是否代价较高，主要看总成本 `J`；`J_c` 和 `J_q` 只说明成本来自接触率降低还是隔离率提高。

## c0 敏感性

下一阶段建议扫描：

```tex
c_0\in[6,13],
\qquad
\eta\in[100,30000].
```

输出指标建议包括：

```tex
t_1,\quad
\Delta t,\quad
T_{\rm clear},\quad
I_{t_{cum}},\quad
J.
```

注意：如果改变 `c0`，TDINN 的 `c(t)` 来自文献固定函数，不能简单逐点跟着改变。因此 `c0` 敏感性优先解释情景一阈值控制自身，不急于逐点和 TDINN 控制比较。

如果发现某些 `c0` 区域内单独调节 `q(t)` 无法得到满意控制，再自然引出情景三，即同时控制 `c(t)` 和 `q(t)`。

## 建议新建分析模块

建议新建目录：

```text
threshold_landscape_analysis/
```

建议包含：

- `eta_landscape`：单阈值全范围扫描；
- `cost_weight_analysis`：固定权重二次加权总成本分析；
- `eta_c0_heatmap`：`(eta,c0)` 二维热图；
- 代表性阈值图像输出目录；
- 汇总 CSV 和必要的 LaTeX 表格。

要求：

- 不覆盖当前主基准输出；
- 不覆盖 `low_eta_analysis/` 历史输出；
- 探索性输出使用清晰目录或后缀；
- 先保存完整 CSV，再选择图像和表格；
- 如果生成论文可用图，文件名要稳定、含义清楚。

## 图像约定

主图 2：

- `xian_control_comparison_panels.pdf`
- 前两行使用对数纵轴；
- 内嵌小图使用线性纵轴；
- 真实数据点只放在每日新增病例图中，不放在 `I(t)` 存量曲线中。

对比用图：

- `xian_control_comparison_panels_linear.*`
- `xian_control_comparison_panels_symlog.*`
- `xian_control_comparison_panels_eta100.*`

除非用户明确要求，不要把对比用图替换进论文正文。

对 `eta=100` 图像，可以为可读性截断主轴横坐标。该截断只影响显示范围，不影响模型模拟和表格数值。

## 数据与拟合

西安真实数据：

- `../真实数据/Xianguankong.xlsx`

当前拟合口径：

- 使用人口规模 SIQR 模型，而不是归一化模型；
- 固定 `N=13,163,000`；
- 固定 He--Tang--Xiao Table 1 中的 `beta`、`gamma`、`delta_q`；
- 固定 `R(0)=0`；
- 只拟合 `I0`；
- 令 `S0=N-I0`。

拟合得到很小的 `I0` 是正常现象。应解释为模型校准种子，而不是现实中的整数病例数。

## 常用命令

从项目根目录重新生成西安输出：

```powershell
python -B xian_control_comparison\xian_control_comparison.py
```

在本文件夹下编译西安比较文档：

```powershell
xelatex -interaction=nonstopmode xian_control_comparison.tex
xelatex -interaction=nonstopmode xian_control_comparison.tex
```

修改图、表、引用、标签或公式编号后，LaTeX 应编译两遍。

## 暂不主动推进的方向

暂不主动推进双阈值控制，除非用户明确要求。

双阈值或预警阈值思想可以作为未来扩展保留：

基本思想是：

```tex
\eta_{\rm warn}<\eta_{\rm cap}.
```

其中：

- `eta_cap` 表示医疗容量上限；
- `eta_warn` 表示提前启动控制的预警线。

这个方向可用于回应“基准阈值启动较晚”的问题，同时避免直接降低容量阈值导致平台期过长。

注意：

- 这不再是原始情景一纯解析控制；
- 如果依赖实时 `I(t)`，它属于反馈控制；
- 如果保持开环，则需要新的预设时间或渐进控制设计。

当前阶段先分析单阈值控制，不要因为基准阈值启动较晚就立即切换到双阈值策略。

## 编辑规则

- 保持修改范围小。
- 不要用探索性结果覆盖主基准输出。
- 探索性输出优先使用清晰后缀，例如 `_eta100`、`_linear`、`_symlog`。
- Python 图例、CSV、LaTeX 表格和正文术语必须一致。
- 手工编辑文件使用 `apply_patch`。
- 不要用 shell 写文件技巧修改源文件。
- 如果只是理论和数值实验阶段，回答中不要强行写论文最终结论。
